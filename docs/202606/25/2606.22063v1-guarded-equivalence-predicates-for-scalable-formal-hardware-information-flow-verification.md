---
title: Guarded Equivalence Predicates for Scalable Formal Hardware Information-Flow Verification
title_zh: 用于可扩展形式化硬件信息流验证的带保护等价谓词
authors: "Liangtao Dai, Yimin Gao, Melika Morsali, Mircea R. Stan"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.22063v1"
tags: ["query:sys-security"]
score: 9.0
evidence: 用于隔离的可扩展硬件信息流形式化验证
tldr: 形式化硬件信息流验证依赖自组合产生关系证明义务，但现有全局等价谓词无法捕获控制上下文特定的信号相等性。本文提出带护卫的等价谓词，将上下文不匹配条件作为辅助阻塞义务暴露给PDR引擎，并通过反例提取与状态分割搜索生成候选护卫。实验显示，该方法将两个超时基准转化为快速完成的证明，并在多个基准上大幅缩短验证时间，显著提升可扩展性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22063v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 781, \"height\": 577, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22063v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 779, \"height\": 565, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22063v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 733, \"height\": 509, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22063v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 932, \"height\": 239, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22063v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 852, \"height\": 801, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22063v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1390, \"height\": 455, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22063v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1387, \"height\": 453, \"label\": \"Table\"}]"
motivation: 现有全局等价谓词无法捕获硬件信息流验证中控制上下文特定的信号相等关系，制约了验证可扩展性。
method: 提出带护卫的等价谓词，将上下文不匹配条件作为辅助阻塞义务提交给PDR，并通过CTI局部提取与状态分割搜索生成护卫候选。
result: 在12个IFV基准及两个PDR后端上，将2个超时案例转化为快速完成证明（34.2–89.5秒），其他基准证明时间最高缩短10.8倍。
conclusion: 带护卫的等价谓词有效捕获硬件信息流验证中的上下文关系，显著提高了PDR引擎的可扩展性与证明效率。
---

## 摘要
形式化硬件信息流验证是排除依赖于秘密的功能或时序观察的一种原则性方法，但扩展此类证明仍然困难。自组合将信息流验证归约为对两个电路副本的安全检查，产生的关系证明义务使通用 PDR 引擎难以仅从位级逻辑中发现。最近的基于 PDR 的技术通过副本对称性和全局跨副本等价谓词利用这种复制结构。这些谓词在对应的内部信号在整个可达状态空间中一致时有效，但无法捕获仅在特定控制上下文中相关的等式。我们观察到，此类上下文关系在硬件 IFV 证明中自然出现：内部信号对可能只需要在控制阶段、事务窗口、循环状态或协议区域内一致。我们引入带保护等价谓词以将这些关系暴露给 PDR。验证器不是将提议的上下文等式视为假设，而是将对应的不匹配条件作为辅助阻塞义务提交。保护条件由关系归纳反例通过 CTI 局部提取和状态分割搜索提出；只有经后端证明不可达的候选才会影响证明。在 12 个 IFV 基准测试和两个 PDR 后端上，带保护谓词将两个上下文基线超时转换为在 1800 秒限制下于 34.2 至 89.5 秒内完成的证明，同时在其他基准上将证明时间最多减少了 10.8 倍。

## Abstract
Formal hardware information-flow verification is a principled way to rule out secret-dependent functional or timing observations, but scaling such proofs remains difficult. Self-composition reduces information-flow verification to safety checking over two circuit copies, creating relational proof obligations that are hard for a generic PDR engine to discover from bit-level logic alone. Recent PDR-based techniques exploit this duplicated structure through copy symmetry and global cross-copy equivalence predicates. These predicates are effective when corresponding internal signals agree throughout the reachable state space, but they do not capture equalities that are relevant only in a specific control context. We observe that such contextual relations arise naturally in hardware IFV proofs: an internal signal pair may need to agree only within a control phase, transaction window, loop state, or protocol region. We introduce guarded equivalence predicates to expose these relations to PDR. Rather than treating a proposed contextual equality as an assumption, the verifier submits the corresponding mismatch condition as an auxiliary blocking obligation. Guards are proposed from relational counterexamples-to-induction using CTI-local extraction and state-split search; only candidates proved unreachable by the backend affect the proof. Across 12 IFV benchmarks and two PDR backends, guarded predicates convert two contextual baseline timeouts into completed proofs within 34.2--89.5s under an 1800s limit, while reducing proof time by up to 10.8x on additional benchmarks.

---

## 论文详细总结（自动生成）

### 1. 研究背景与核心问题
- **硬件信息流验证（IFV）的扩展性瓶颈**：通过自组合（self‑composition）可将非干扰性质转化为两个电路副本上的安全性质，但由此产生的关系证明义务（relational proof obligations）对通用 PDR 引擎极难处理。
- **现有方法的局限**：SecIC3 等工作利用副本对称性与全局跨副本等价谓词，但这些谓词仅当对应内部信号在整个可达状态空间中恒等时才有效。硬件中大量相关等式只在特定的控制阶段、事务窗口、循环状态或协议区域内成立，全局等价谓词无法捕获此类上下文关系，导致验证失败或超时。
- **研究动机**：将“仅在上下文中相等”这一特征显式地暴露给 PDR，以消除现有方法的证明盲区，提升硬件 IFV 的可扩展性。

### 2. 方法论
- **核心思想**：引入带保护的等价谓词（guarded equivalence predicates）。不将上下文等式当作假设，而是将带保护的不匹配条件作为辅助阻塞义务（auxiliary blocking obligation）提交给标准 PDR 的立方阻塞（cube blocking）机制。若后端能证明该不匹配区域不可达，则产生导出的学习子句，从而间接建立上下文内的等价性；否则候选被丢弃，不影响健全性。
- **关键技术细节**：
  - **带保护的不匹配立方**：对内部信号 \( X_1, X_2 \) 定义 `neq_X = (X_1 \neq X_2)`；给定控制上下文 \( g \)，构造违例立方 \( V(g,X) = g \land \text{neq}_X \)。若 PDR 阻塞该立方，则 \( g \Rightarrow \text{eq}_X \) 在可达状态上成立。
  - **候选生成来源**：均基于 PDR 内部已产生的信息（CTI，counterexample‑to‑induction），不额外修改转移关系或初始状态。
    - **CTI 局部提取**：从单个关系型 CTI 中分解出上下文文字 \( G_q \) 和不匹配文字 \( N_q \)，按不同策略生成候选（g‑AoN、g‑Maximal、g‑Maximum 模式，分别对应捆绑、单例、有限子集组合）。
    - **状态分割搜索（state‑split search）**：聚合多个关系型 CTI，计算普通状态变量在 CTI 上下文中的出现频率 \( \text{freq}(x) \)，选取 Top‑K 高频变量 \( S_K \)，枚举其完整赋值作为保护 \( g_{K,a} = (S_K = a) \)，再与单个 `neq_X` 组合成候选立方。该搜索显式地构建状态空间分区，以克服单一 CTI 的上下文碎片化问题。
  - **嵌入 PDR 的流程**（见原文 Algorithm 1）：捕获关系型 CTI → 按调度模式生成候选立方 → 排除与初态交叠的候选 → 调用 `BLOCK_CUBE(c, k)` 尝试阻塞 → 成功则保留学到的子句，失败则丢弃。候选生成是启发式的，仅接受由后端阻塞产生证明影响的部分。
- **健全性保证**：所有候选均为普通的谓词立方，提交给未经修改的 PDR 阻塞流程；守卫搜索不影响转移关系、初始条件、目标安全性质或证明规则，健全性完全由后端保证。

### 3. 实验设计
- **基准测试集**：12 个 IFV 基准，包括 10 个来自 SecIC3 标准套件的设计（算术数据通路、加密/控制密集设计、处理器片段）和 2 个诊断型设计 `vctr`、`normacc`（专门构造以凸显上下文保护的需求）。
- **对比方法**：无修改的原始后端、SecIC3 的三种谓词置换模式（AoN、Maximal、Maximum）及其与副本对称的组合，从中选取最佳基线（SecIC3‑best）；在此基础上评估提出的带保护模式（g‑AoN、g‑Maxl、g‑Maxm、g‑StSpl、g‑Hyb 以及对称+保护组合）。
- **后端平台**：ABC‑PDR 和 rIC3 两个不同的 PDR 实现，以验证方法的不依赖性。
- **评价指标**：证明成功时的墙钟时间，超时阈值统一为 1800 秒。

### 4. 资源与算力
- **硬件环境**：单机 CPU 平台（Intel Core i9‑14900K），内存 188 GiB。每个证明进程为单线程，无 GPU 加速。
- **算力使用**：文中未提及 GPU；所有实验均基于 CPU 单核运行，且并行仅为吞吐量方便，报告的时间为单进程墙钟时间。无特别的大规模算力需求，方法在常规开发/验证服务器上即可运行。

### 5. 实验数量与充分性
- **实验覆盖**：12 个基准 × 两个后端 × 多种保护与对称模式（每表中列出 7~8 种配置），相当于数十次独立验证运行。
- **公平性**：所有方法使用同一超时、同一机器、同一 PDR 引擎基底；基线包含当前最先进的 SecIC3 最佳配置；保护模式仅额外产生候选立方，不改变核心验证算法，对比公平。
- **充分性**：实验覆盖设计多样化（算术、密码、控制、诊断），且同时包含已可证明和超时基准，能全面反映加速与“解锁”效果；模式消融分析说明了不同保护生成策略的适用差异。
- **局限性**：未报告多次运行的方差或统计显著性（可能为单次运行），但在形式验证的性能评估中属常见做法，仍具说服力。

### 6. 主要结论与发现
- **解锁困难案例**：保护谓词将两个在最佳基线下 1800 秒内无法完成的基准（ABC‑PDR 上的 `FP_DIV` 和 `normacc`，以及 rIC3 上的对应案例）转化为 34.2～89.5 秒内的成功证明。
- **显著加速**：在已可证明的基准上，最优保护配置相对 SecIC3‑best 加速最高达 10.8 倍（如 `vctr`、`Cache`、`GCD` 等）。
- **模式互补**：CTI 局部搜索在单一 CTI 已暴露充分上下文时表现好；状态分割搜索在上下文分散于多个 CTI 时起关键作用；对称与保护可互补但非普适。
- **未决瓶颈**：`FP_MUL` 在所有模式下仍未解决，提示宽算术或运算器推理存在独立瓶颈。

### 7. 优点
- **紧扣硬件验证特性**：显式建模控制上下文中的等价性，自然匹配硬件 IFV 中安全性质与协议/阶段相关的真实情形。
- **利用引擎内部信息**：通过观察 PDR 的 CTI 生成保护，无需用户提供领域引理或修改电路，工程上易集成。
- **稳健的健全性设计**：候选为普通立方，仅通过标准阻塞程序转化为证明力，不引入假设，确保方法的安全。
- **多后端、多模式全面评估**：在两个主流开源后端上展示了稳定收益，消融实验指明了各模式的适用场景。
- **开销可控**：仅尝试有限数量的候选，且失败候选被立即丢弃，不会过度膨胀搜索空间。

### 8. 不足与局限
- **依然存在未解案例**：`FP_MUL` 等设计暴露该方法无法处理所有瓶颈，需要更强算术推理或与其它技术结合。
- **候选生成策略需工程调优**：不同模式在不同设计上的最佳选择不完全一致，没有单一最优策略；Hybrid 调度为工程性策略，可能引入额外开销。
- **依赖关系型 CTI 质量**：若 PDR 无法产生足够信息量的 CTI，候选生成可能空转或无效，状态分割搜索的 Top‑K 选择也对启发式敏感。
- **实验只包含两个诊断设计**：上下文关系大量存在的真实复杂 SoC 级设计可能仍有未被覆盖的问题模式。
- **缺少 CPU 时间与内存细节**：未报告内存峰值或缓存行为，也未在不同架构上测试；通用性局限于所测环境。
- **未与其它 IFV 方法（如基于符号执行、信息流类型系统）对比**，但论文重点在 PDR 框架内的改进，此定位可接受。

（完）
