---
title: "Echo: Learning-based Matching Decompilation using Trusted Back Translation"
title_zh: Echo：使用可信回译的基于学习的匹配反编译
authors: "Jun Bi, Xiangxin Fang, Aarsh Chaube, José Wesley De Souza Magalhães, Rodrigo C. O. Rocha, Michael O'Boyle"
date: 2026-09-16
pdf: "https://arxiv.org/pdf/2609.18706v1"
tags: ["query:sys-security"]
score: 6.0
evidence: 面向二进制程序的匹配式反编译与静态分析
tldr: 神经反编译器能恢复可读可重编译的源码，但预测难以信任；匹配反编译靠重编译精确匹配提供正确性证据，却难应对未知编译配置下的优化二进制。Echo 提出可信回译：用编译同时做验证与反馈，领域模型生成候选程序及编译配置，重编译后度量汇编相似度并合成代码-配置对，再用规则改写、神经与推理精修修复残余不匹配。在函数级基准上平均精确匹配数达最强基线的 2.43 倍，结构相似度最高；Mirai 上匹配函数数是 GPT-5.6 的 2.75 倍、Codex 的 7.4 倍。
source: arxiv
selection_source: fresh_fetch
motivation: 神经反编译预测难以信任，匹配反编译虽能通过重编译精确匹配提供正确性证据，但在未知编译配置的优化二进制上仍难实现。
method: Echo 以可信回译将编译用作反馈：生成候选程序与编译配置，重编译度量汇编相似度并合成代码-配置对，再用规则改写、神经与推理精修修复不匹配。
result: 相比最强基线，Echo 平均精确匹配数提升 2.43 倍，源码结构相似度最高；在 Mirai 上匹配函数数为 GPT-5.6 的 2.75 倍、Codex 的 7.4 倍。
conclusion: 表明将编译作为可信反馈的迭代搜索可显著提升优化二进制匹配反编译的可验证性与覆盖率。
---

## 摘要
神经反编译器能够从二进制文件中恢复可读且可重新编译的源代码，但其预测仍难以信任。匹配反编译通过搜索重新编译后的汇编与目标完全匹配的源代码来解决这一问题，从而为正确性提供更强证据。然而，对于未知编译配置下的优化二进制文件，精确匹配仍然具有挑战性。我们提出 Echo，一种基于可信回译的匹配反编译系统。我们的核心洞见是，不仅将编译用于验证，还将其作为可信反馈来指导迭代搜索。Echo 首先使用领域特定模型生成候选程序和编译配置。它重新编译这些候选，度量汇编级相似性，并合成有前景的代码-配置对。随后，剩余的不匹配通过基于规则的改写、神经细化和基于推理的细化逐步修复。我们在函数级基准和 Mirai 恶意软件二进制文件上评估 Echo。与最强基线相比，Echo 平均产生多 2.43 倍的精确匹配，并达到与真实源代码最高的结构相似度。在 Mirai 上，Echo 匹配的函数数量分别是 GPT-5.6 和 Codex 的 2.75 倍和 7.4 倍。

## Abstract
Neural decompilers can recover readable and recompilable source code from binaries, but their predictions remain difficult to trust. Matching decompilation addresses this problem by searching for source code whose recompiled assembly exactly matches the target, providing stronger evidence of correctness. However, exact matching remains challenging for optimized binaries under unknown compilation configurations.   We present Echo, a matching decompilation system based on trusted back-translation. Our key insight is to use compilation not only for verification, but also as trusted feedback to guide iterative search. Echo first uses a domain-specific model to generate candidate programs and compilation configurations. It recompiles these candidates, measures assembly-level similarity, and synthesizes promising code-configuration pairs. Remaining mismatches are then progressively repaired using rule-based rewriting, neural refinement, and reasoning-based refinement.   We evaluate Echo on function-level benchmarks and the Mirai malware binary. Compared with the strongest baseline, Echo produces 2.43x more exact matches on average and achieves the highest structural similarity to ground-truth source code. On Mirai, Echo matches 2.75x and 7.4x as many functions as GPT-5.6 and Codex, respectively.