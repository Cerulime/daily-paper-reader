---
title: Extending concurrent separation logic to the hardware level to verify the xv6 OS kernel on RISC-V with AI agents
title_zh: 将并发分离逻辑扩展到硬件层面，以AI代理验证RISC-V上xv6操作系统内核
authors: "M. Frans Kaashoek, Nickolai Zeldovich"
date: 2026-09-03
pdf: "https://arxiv.org/pdf/2609.04043v1"
tags: ["query:sys-security"]
score: 7.0
evidence: 将并发分离逻辑扩展到RISC-V硬件语义验证xv6内核，覆盖页表、TLB与特权级等内存隔离机制
tldr: 针对操作系统内核等系统软件难以在硬件级语义（如页表、特权级、中断、DMA）上验证的问题，提出MachCSL框架，将并发分离逻辑扩展至RISC-V子指令级。利用LLM智能体辅助枯燥的底层推理，成功验证xv6内核（6593行C/汇编），发现9个xv6 bug及1个Sail语义bug，历时77天。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.04043v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1706, \"height\": 1640, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.04043v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1475, \"height\": 721, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.04043v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1688, \"height\": 815, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.04043v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 809, \"height\": 689, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.04043v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 892, \"height\": 480, \"label\": \"Table\"}]"
motivation: 系统软件内核依赖页表、特权级、中断等底层硬件细节，但现有分离逻辑多抽象到指令级之上，难以确保软硬件交互正确。
method: 基于Iris和Sail RISC-V语义，将并发分离逻辑适配到子指令级硬件执行，构建MachCSL框架，并借助LLM智能体处理低层证明细节。
result: 完整验证带多核并发、文件系统和抢占调度的xv6内核，发现9个实现bug和1个Sail模型语义bug。
conclusion: 证明硬件级系统软件验证可行，LLM智能体可显著降低此任务的人力和时间成本。
---

## 摘要
MachCSL是一个在基于Sail RISC-V语义的RISC-V计算机低层语义之上验证系统软件（如操作系统内核）的框架。MachCSL的关键思想在于将基于Iris的并发分离逻辑适配到子指令级别的低层硬件执行推理上：页表转换、TLB、特权级别、配置寄存器、指令取指/译码/执行、陷阱与中断、DMA、共享内存、电源故障等。在此细节级别上进行推理可确保系统软件正确管理所有硬件细节。在如此低抽象级别上验证软件是繁琐的，但基于LLM的代理能够推理此类低层细节。作为案例研究，我们验证了xv6操作系统内核（6,593行C和汇编代码），它提供了传统的Unix系统调用接口（进程、文件系统、文件描述符和抢占式调度），并具有大量的内部并发性（支持多核的细粒度锁、共享内存、中断、DMA等）。在验证过程中，我们在xv6实现中发现了九个错误，以及在Sail RISC-V语义中发现了一个错误。验证工作耗时77天，包括开发MachCSL框架的时间。

## Abstract
MachCSL is a framework for verifying systems software, such as an OS kernel, on top of low-level semantics of a RISC-V computer, based on the Sail RISC-V semantics. The key idea behind MachCSL is to adapt concurrent separation logic, based on Iris, to reasoning about low-level hardware execution at the sub-instruction level: page-table translation, TLB, privilege levels, configuration registers, instruction fetch/decode/execute, traps and interrupts, DMA, shared memory, power failures, etc. Reasoning at this level of detail ensures that the system software correctly manages all of the hardware details.   Verifying software at this low level of abstraction is tedious, but LLM-based agents are capable of reasoning about such low-level details. As a case study, we verify the xv6 OS kernel (6,593 lines of C and assembly code), which provides a traditional Unix system call interface (processes, file system, file descriptors, and preemptive scheduling) and has substantial internal concurrency (multi-core support with fine-grained locking, shared memory, interrupts, DMA, etc.). In the verification process, we uncovered nine bugs in the xv6 implementation, as well as one bug in the Sail RISC-V semantics. The verification effort took us 77 days, including the time to develop the MachCSL framework.