---
title: "Complets: Universal Compartmentalisation and Programming Model For Arm Permission Overlay Extension 2"
title_zh: 完整隔离体：适用于 Arm 权限叠加扩展 2 的通用隔离与编程模型
authors: Vasily A. Sartakov
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05569v1"
tags: ["query:sys-security"]
score: 9.0
evidence: 基于内存保护键的进程内隔离机制
tldr: Arm Permission Overlay Extension 2（POE2）通过空间与时间索引增强进程内内存隔离，但其硬件交互复杂，直接编程极易出错。本文提出Complets通用隔离与编程模型，将时空索引抽象为高级访问控制原语，提供强安全保证。该模型自然支持进程内分区软件的典型模式，为POE2系统提供了安全易用的高层次抽象，降低了构建隔离应用的难度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05569v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 604, \"height\": 601, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05569v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1419, \"height\": 541, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05569v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 631, \"height\": 563, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05569v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1555, \"height\": 353, \"label\": \"Table\"}]"
motivation: Arm POE2虽提供强进程内隔离，但直接编程需处理复杂的硬件权限交互，极易引入安全缺陷。
method: 提出Complets通用编程模型，将POE2的空间与时间索引抽象为高层访问控制原语，简化编程。
result: 模型实现强安全保证，并能自然表达进程内分区软件的典型模式，验证了其适用性。
conclusion: Complets为POE2系统提供安全易用的编程抽象，显著降低构建隔离应用的复杂度，提升可靠性。
---

## 摘要
Arm 权限叠加扩展（POE）是一种基于内存保护密钥的进程内隔离机制。该机制将虚拟内存划分为多个区域，其访问权限可在不调用更高异常级别的情况下重新配置。由于 POE 不强制特权分离，基于 POE 的保护域仅适用于软件故障隔离技术。POE2 是一种大幅扩展了 POE1 的隔离机制。在处理内存保护密钥的同时，它还引入了新的寄存器和表，用于规范在与不同密钥关联的保护域内执行的代码所允许的操作。权限由执行代码的空间索引（即与代码关联的保护密钥）、所访问内存的空间索引、操作类型以及线程上下文的一个新属性——时间索引共同决定。POE2 提供比 POE1 更强的安全保证，但也带来了相当大的架构复杂性。有效权限源自多个硬件组件的交互，使得直接编程并不容易且容易产生细微错误。本文对 POE2 进行了详细分析，并为基于 POE2 的系统引入了一种具有强安全模型的通用编程模型。该模型抽象了空间和时间索引的复杂性，同时支持使用进程内隔离构建分区软件的典型模式。

## Abstract
Arm Permission Overlay Extension (POE) is an intra-process isolation mechanism based on memory protection keys. This mechanism partitions virtual memory into regions whose access permissions can be reconfigured without invoking higher exception levels. Because POE does not enforce privilege separation, POE-based protection domains are applicable only as a software fault-isolation technique.   The POE2 is an isolation mechanism that substantially extends POE1. Dealing with memory protection keys, it also introduces new registers and tables that regulate the operations permitted for code executing within protection domains associated with different keys. A permission is determined by the spatial index of the executing code (i.e., the protection key associated with code), the spatial index of the accessed memory, the operation type, and a new attribute of the thread context: its temporal index.   POE2 provides stronger security guarantees than POE1, but also introduces considerable architectural complexity. Effective permissions arise from the interaction of multiple hardware components, making direct programming non-trivial and prone to subtle errors. In the paper, we present a detailed analysis of POE2 and introduce a universal programming model with a strong security model for POE2-based systems. The model abstracts the complexity of spatial and temporal indices while enabling typical patterns of partitioned software constructed using intra-process isolation.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **背景**：传统进程间隔离依赖 MMU，通信须经内核介入，引入显著的性能开销（MMU tax）。Arm Permission Overlay Extension 1（POE1）基于内存保护密钥实现进程内隔离，避免了内核切换，但其缺陷在于**缺乏特权分离**——隔离域内的代码可以修改其他域的访问权限，因此仅能用于软件故障隔离，无法抵御恶意代码。
*   **问题**：新推出的 POE2 虽然通过引入**空间索引（POIndex）与时间索引（TIndex）** 的二维权限模型，提供了更强的安全保证，但其硬件权限由多张表格（IRT、DPOT、TTT 等）与多个寄存器协同决定，**架构极度复杂**。直接编程极易因索引配置错误导致权限泄露或意外的控制流跳转。
*   **目标**：本文旨在提出一套**通用编程模型与安全模型**，抽象 POE2 的时空索引复杂性，使开发者能安全、简便地构建具备典型进程内隔离模式（如对称隔离、非对称沙箱、Enclave）的系统。

### 2. 论文提出的方法论：核心思想、关键技术细节、算法或流程
*   **核心抽象：“Complet”（隔离体）**
    *   将 POE2 中单一的保护域定义为 **Complet**（隔离 applet）。每个 Complet 被分配**唯一且不同**的 `POIndex` 和 `TIndex` 组合，从根本上避免直接的空间或时间索引冲突。
    *   默认安全假设将 Complet 内的代码视为**完全敌对**的，可执行任意指令。
*   **可信中间层：Monitor 与 跳板（Trampoline）**
    *   **Monitor**：进程内特权组件，负责 Complet 生命周期管理（部署二进制、配置 POE2 表格、分配共享内存），并暴露 hostcall 接口。
    *   **安全跳板**：隔离的 Complet 之间不能直接相互跳转。所有域间转移必须通过一段**极小（<100 条汇编指令）、无状态的可信跳板代码**执行。跳板代码所处的内存区域拥有独立的 `POIndex`。
*   **受控转换流程**：
    *   **两步索引变更**：因为 `TIndex`（通过 `TCHANGE` 指令修改）和 `POIndex`（通过分支指令修改）不能原子性地同时切换，转换被拆解为一个经过**中间状态**的序列。例如，先从 Complet A 分支到跳板的 `POIndex`，然后跳板执行 `TCHANGE` 改变 `TIndex` 以获取目标 Complet 的身份，最后再分支到 Complet B。
    *   **身份保持与上下文切换**：跳板在转换时保存和恢复寄存器上下文，并在提升 `TIndex` 到 Monitor 级别时，传递调用来源的身份标识。
*   **表格抽象 API**：将内核态对 IRT、DPOT、TTT 等复杂表格的配置，封装为 `deploy_complet`、`enable_transition`、`install_hook` 等简单函数， Monitor 只需面向 Complet 对象进行编程。

### 3. 实验设计：使用的数据集/场景、对比方法
*   **实验平台**：基于 Arm 公司提供的 **POE2 功能仿真模型（FVP）** 进行原型验证，并非真实硬件。
*   **评测场景（设计模式）**：未采用特定的标准 Benchmark 或数据集，而是针对进程内隔离的三种经典设计模式实现了概念验证：
    1.  **对称隔离**：相互不信任的对等方通过共享内存和 Monitor 通信（类似 Hodor、libmpk）。
    2.  **非对称隔离（JIT 隔离）**：信任域对非信任域具有完全的读写访问能力（类似 ERIM、Shreds）。
    3.  **反向沙箱（Enclave 模式）**：隔离域可完整访问宿主进程内存，但宿主无法访问隔离域（类似 Intel SGX 的数据访问模式）。
*   **对比**：本文主要是**架构与模型设计**，未对现有其他隔离系统（如基于 POE1 或 Intel MPK 的系统）进行性能基准测试对比。

### 4. 资源与算力
*   **算力情况**：本文为系统安全与体系结构类论文，**未涉及任何 GPU 算力、模型训练或大规模云资源消耗**。原型验证在模拟器上进行，未提及特定硬件配置或训练时长。

### 5. 实验数量与充分性
*   **实验数量**：文中仅展示了 1 个关键 API 使用示例（Table 1）和 1 个安全转换流程图（Figure 3），并定性描述了三种设计模式的实现方式。提供了少量微观数据：单向转换约需 75 条指令，内核扩展约 300 行代码。
*   **充分性评价**：
    *   **不充分（从性能评估角度）**：缺乏对隔离域转换开销的量化测量，无法判断其相对于 POE1、系统调用或 MPK 的性能优劣。没有内存开销或吞吐量对比。
    *   **客观性**：作为一篇前沿设计提案，其重点在于**架构抽象的正确性与安全性论证**，而非大规模性能对比。实验部分仅用于证明设计的可行性，符合该类论文的常规做法。但由于缺乏性能数据，其实用性尚未得到充分验证。

### 6. 论文的主要结论与发现
*   POE2 虽然带来了革命性的细粒度隔离能力，但其**多维索引与表格的交互构成了巨大的编程陷阱**。
*   提出的 **Complet 编程模型**能够有效地将 POE2 的硬件复杂性隐藏在高层次抽象之后。
*   通过强制使用**独立的 POIndex/TIndex 以及无状态的受控跳板**，该模型在允许高效进程内通信的同时，从根本上杜绝了隔离域之间的任意控制流跳转和权限篡改，为 POE2 提供了一种安全且通用的编程范式。

### 7. 优点：方法或实验设计上的亮点
*   **安全的抽象设计**：引入“时间索引+空间索引”唯一绑定的 Complet 概念，有效解决了 POE2 多索引组合爆炸和状态非法堆叠的问题。
*   **极小化 TCB（可信计算基）跳板**：通过将域间转换逻辑强制限制在 100 行以下的无状态汇编代码中，极大降低了受攻击面，并可结合 BTI 等硬件特性强制定点着陆。
*   **通用性**：同一套 API 和模型能够自然映射并实现业界主流的三种隔离模式（对称、非对称、Enclave），显示了模型的表达能力。
*   **对齐 POE2 架构特性**：巧妙地利用 POE2 必须分两步变更索引的特性，把转换流程固化为安全必经路径。

### 8. 不足与局限：实验覆盖、偏差风险、应用限制
*   **缺乏性能量化分析**：未对转换延迟、内存占用、缓存（新表格）冲刷等关键性能指标进行评测。其论述中提及性能开销可能取决于微架构实现，但未给出任何估算或比较。
*   **无真实硬件验证**：全部验证依赖于 Arm 的功能仿真模型（FVP），其行为与真实芯片可能由于微架构优化（如缓存、推测执行）存在差异。
*   **安全依赖假设过强**：强安全模型依赖跳板代码绝对无 bug，且 Monitor 不会误配表格。对于现实中的复杂应用，Monitor 自身的复杂性可能引入新的漏洞。
*   **ECO 系统支持缺失**：未讨论动态链接、异常处理、多线程同步、信号处理等复杂场景下，Complet 模型的行为和额外负担。
*   **手动重构负担**：模型假设隔离代码是“隔离无感”的，需要 Monitor 通过 `install_hook` 进行函数替换，这可能需要对现有代码进行烦琐的手动或半自动适配。

（完）
