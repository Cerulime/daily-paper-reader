Title: Disintegration Temporal Logic for Probabilistic Hyperproperties

URL Source: https://arxiv.org/pdf/2607.15223v1

Published Time: Fri, 17 Jul 2026 01:41:35 GMT

Number of Pages: 31

Markdown Content:
# Disintegration Temporal Logic for Probabilistic Hyperproperties 

## Mishel Carelli # 

CISPA Helmholtz Center for Information Security, Germany 

## Bernd Finkbeiner # 

CISPA Helmholtz Center for Information Security, Germany, Technical University of Munich, Germany 

Abstract 

We introduce Disintegration Temporal Logic (DTL), a new probabilistic temporal logic that can express a wide range of probabilistic hyperproperties, including probabilistic non-interference and perfect indistinguishability. DTL is based on the notion of measure disintegration from probability theory, which allows for conditioning probabilities on a finite or infinite sequence of events occurring during a program execution. This naturally supports reasoning about interacting stochastic systems, where complete executions of one component induce conditional probability distributions over another. We illustrate applications of DTL to systems interacting with stochastic environments, distributional properties of Markov decision processes, and probabilistic automata on infinite words, and discuss its relationship to existing probabilistic logics. While model checking Markov chains against full DTL is undecidable, we identify two decidable fragments that capture many hyperproperties of interest. The linear fragment admits a polynomial-time model-checking procedure based on linear-algebraic techniques and captures probabilistic information-flow properties such as perfect indistinguishability and history-based probabilistic non-interference. The qualitative fragment admits an automata-theoretic model-checking procedure that extends the standard algorithm for HyperCTL ∗ with reasoning about bottom strongly connected components. 

2012 ACM Subject Classification Theory of computation → Logic and verification 

Keywords and phrases Verification of Probabilistic Systems, Hyperproperties, Probabilistic Hyper-properties, Disintegration 

Funding Mishel Carelli : This work was supported by the European Research Council (ERC) Grant HYPER (No. 101055412). 

Bernd Finkbeiner : This work was supported by the European Research Council (ERC) Grant HYPER (No. 101055412). 

## 1 Introduction 

Hyperproperties are system properties that relate multiple execution traces [ 19 ]. They arise naturally in areas such as information-flow security, multi-agent systems, and counterfactual reasoning. There is substantial interest in finding expressive specification logics that capture a large set of relevant hyperproperties and yet have verification problems with acceptable computational cost (cf. [25]). For circuits and other non-probabilistic systems, simple variations of the standard temporal logics, such as HyperLTL [ 17 ], the extension of linear-time temporal logic (LTL) with quantification over multiple traces, have proven to be powerful unifying frameworks. Such logics cover a large class of hyperproperties, including notions like non-interference, robustness, and counterfactual causality, that were previously studied in isolation. For probabilistic systems, the quest for general specification logics has turned out to be more difficult. A typical example of a probabilistic temporal logic is HyperPCTL [ 1 ], which combines standard probabilistic reasoning from logics like PCTL [ 8] with quantification over states. This allows arXiv:2607.15223v1 [cs.LO] 16 Jul 2026 2 Disintegration Temporal Logic for Probabilistic Hyperproperties 

for the specification of restricted versions of probabilistic non-interference, such as that an output distribution must not depend on the initial state. However, logics like HyperPCTL cannot refer to conditional probabilities . As a result, such logics cannot express notions like 

probabilistic non-interference in full generality, which is needed for systems that communicate reactively through low-security and high-security channels [29]. Consider a system that, at every step, receives a low-security input and a high-security input in variables l and h and, based on the previous trace and the current inputs, probabil-istically determines the value of a binary output variable o. Probabilistic non-interference requires that for any low-level input trace L of length n, any high-level input trace H of length n, and any low-level output trace O of length n, such that the joint probability 

P(H, L, O ) > 0, the probability of o being true at step n conditioned on (H, L, O ), is the same as the probability conditioned only on (L, O ). Intuitively, this property expresses that the high-level input trace does not provide additional information about the next output, once the low-level input and output history is fixed. To express properties like general probabilistic non-interference in a temporal logic, the logic must support both temporal reasoning and probabilistic reasoning with conditioning on sequences of events . In this paper, we introduce Disintegration Temporal Logic (DTL) , which provides such a combination. DTL expresses general probabilistic non-interference as the following formula: 

P

( (P( o | [h, l, o ]) = P( o | [l, o ]) ))

= 1 . (1) Here, P( o | [h, l, o ]) denotes the probability that o is true at the next step, conditioned on the history of h, l, and o. Analogously, P( o | [l, o ]) conditions only on the history of l and 

o. The globally operator has the usual meaning, that the subformula must hold at every point in time, and P(·) = 1 requires that the property holds for almost every trace (up to a set of measure zero). Combining these components, we obtain that the formula (1) holds for a system M if the system satisfies probabilistic non-interference. In addition to conditioning on finite sequences of events, DTL also allows for conditioning on infinite sequences. This naturally arises in systems consisting of several interacting stochastic components. Consider a system interacting with an external stochastic environment modeled by a Markov chain. DTL can express properties such as With probability at least 

0.1, the environment produces an execution such that, conditioned on this execution, the probability that the system exhibits the undesirable behavior is greater than 0.4. This is expressed by the formula 

M ⊨ P

(

P(ϕbad | AP env 

) > 0.4

)

> 0.1.

Here, AP env denotes the set of actions produced by the environment. Note that we write 

AP env rather than [AP env ], corresponding to conditioning on the full infinite trace rather than on a finite prefix as in formula (1). Beyond these motivating examples, DTL also naturally expresses distributional properties of Markov decision processes and properties of probabilistic automata on infinite words, establishing connections to existing probabilistic verification frameworks. The mathematical foundation of DTL is the notion of measure disintegration . Disin-tegration allows us to define conditional probabilities even when conditioning on events of probability zero. This is essential in our setting, since individual infinite traces typically have probability zero, yet we still want to condition on them. The problem of defining conditional probabilities in such cases dates back to Kolmogorov [ 34 ] and the Borel–Kolmogorov paradox, M. Carelli and B. Finkbeiner 3

which illustrates the subtlety of conditioning on null events. Measure-theoretic disintegration, established by Rokhlin [44], provides a principled resolution of this issue [32, 14]. For full DTL, the model checking problem (determining whether a formula is satisfied by a given Markov chain) is undecidable. However, we identify two decidable fragments that capture many hyperproperties of interest. The linear fragment allows comparing probabilities to 1 as well as checking the equality between probabilities. This fragment can express conditional independence properties, including probabilistic non-interference and perfect indistinguishability. The linear fragment admits polynomial-time model checking based on linear-algebraic reasoning, similar in spirit to algorithms for checking equivalence of labeled Markov chains [48]. The qualitative fragment can be viewed as a probabilistic counterpart of the widely used hyperproperty logic HyperCTL ∗ [ 17 ]. In this fragment, probabilities may only be compared to 0 or 1, yielding “soft” versions of existential and universal quantification. The qualitative fragment can express, among others, properties of systems interacting with stochastic environments that compare probabilities only to 0 and 1. We present a model-checking algorithm for this fragment that combines reasoning about bottom strongly-connected components with the standard HyperCTL ∗ model-checking procedure. The resulting complexity is a tower of exponentials whose height is linear in the alternation depth of the formula. The remainder of the paper is organised as follows: Section 2 presents preliminaries, introduces measure disintegration, and establishes key properties of disintegration on spaces of infinite traces. Section 3 defines the syntax and semantics of DTL and justifies the semantics using measure-theoretic arguments. Section 4 demonstrates how to express different properties in DTL and relates it to existing logics. Sections 5 and 6 introduce the linear and qualitative fragments and provide corresponding model-checking algorithms. Section 7 concludes. 

## 2 Preliminaries 

In this section, we introduce both well-known concepts and new notions that are used throughout the paper. We begin with notations, followed by basic definitions from probability theory on the space of infinite words. We then introduce the fundamental concept of conditioning via measure disintegration, which forms the basis of our logic. Next, we describe our approach to conditioning on the space of infinite words through the notion of a cut , and present several technical results that provide a constructive interpretation of the abstract concept of disintegration in the space of infinite words. Finally, we define Markov chains, Markov decision processes, and various types of automata. 

## 2.1 Notations 

Let N, R, and R+ denote the sets of natural numbers, real numbers, and non-negative real numbers, respectively. Nω , denotes the set N ∪ { ω}, with a linear order, extended from N,with ω > n , for every n ∈ N.Given an element s ∈ X × Y , sX denotes the projection of s on Y .4 Disintegration Temporal Logic for Probabilistic Hyperproperties 

Given a set X and n ∈ N, we write 2X for the set of all subsets of X, Xn for the set of sequences of elements of X of length n, X∗ = ⋃ 

> n∈N

Xn, and Xω for the set of all infinite sequences of elements of X, called traces . Given a subset Y ⊆ X, Y denotes the complement of Y .Given a trace π ∈ Xω ∪ X∗ and n ∈ N, let π(n) denote the n-th element of π if it exists, and let π[n] denote the prefix of π of length min {n, |π|} .Given π ∈ X∗ and ρ ∈ Xω ∪ X∗, their concatenation is denoted by πρ . For Y ⊆ X∗ and 

Z ⊆ Xω ∪ X∗, we define their concatenation element-wise by Y Z = {πρ | π ∈ Y and ρ ∈ Z}.

For π ∈ X∗ and Z ⊆ Xω ∪ X∗, denote πZ = {π}Z.

## 2.2 Probability measure on infinite words 

Given a set X, a σ-algebra on X is a non-empty collection X ⊆ 2X that is closed under complementation and countable unions and intersections. A measurable space is a pair 

(X, X ), where X is a set and X is a σ-algebra on X.Given a countable set X, the Cantor σ-algebra on Xω is the minimal σ-algebra containing the cylinder sets of the form Cyl (π) := {πρ | ρ ∈ Xω }, where π ∈ X∗. We always assume the Cantor σ-algebra on Xω and omit mentioning it. Let (X, X ) and (Y, Y) be measurable spaces. A function f : X → Y is measurable if 

f −1(B) ∈ X for every B ∈ Y . The product σ-algebra on X × Y is the smallest σ-algebra containing all rectangles of the form A × B with A ∈ X and B ∈ Y . We denote it by X ⊗ Y .A finite measure on (X, X ) is a function μ : X → R+ such that μ(∅) = 0 and μ is 

countably additive , i.e. μ(⋃ 

> n∈N

An) = ∑ 

> n∈N

μ(An) for every sequence of pairwise disjoint sets A1, A 2, · · · ∈ X . A measure μ is called a probability measure if μ(X) = 1 .We denote by M(X, X ) and P(X, X ) the sets of finite measures and probability measures on (X, X ), respectively. Any countably additive function from the set of cylinder sets of Xω to [0; 1] , that maps ∅

to 0, extends uniquely to a probability measure on Xω [8]. Given a measure μ on (X × Y, X ⊗ Y ), define a marginal measure ˆμX on (X, X ) for any 

B ∈ X as ˆμX (B) = μ(B × Y ).A property on X (for example, the equivalence of two functions or of two sets) is said to hold almost everywhere with respect to a measure μ (written μ-a.e.), if the set of points where the property fails has μ-measure zero. For the real-valued measurable function f : X → R, measure μ and a measurable subset 

A ⊆ X we denote an integral of f with respect to μ over A as ∫ 

> A

f (x)μ(dx ). For the definition of the integral and a more comprehensive introduction to measure theory, we refer to [ 32 , 47 ]. 

## 2.3 Conditioning and Disintegration 

The standard definition of the probability of an event A conditioned on an event B is given by 

P(A | B) = P(A ∩ B)

P(B) .

This formula is well-defined only when P(B)̸ = 0 . However, in many situations, we would like to condition on events of probability 0.Consider, for example, the uniform distribution on the unit square [0 , 1] ×[0 , 1] . Intuitively, if we condition on the first coordinate being equal to 0.3, the second coordinate should remain uniformly distributed on [0 , 1] . However, the event that the first coordinate is exactly 0.3M. Carelli and B. Finkbeiner 5

has probability 0, so such a conditional distribution is not defined by the standard formula above. To address this issue, probability theory uses the notion of measure disintegration. Quoting [ 32 ]: "Modern probability theory can be said to begin with the notions of conditioning and disintegration." Intuitively, if we have a measure on X × Y , the disintegration of this measure is a well-behaved choice of conditional measures μx on Y , indexed by points x ∈ X, called kernel .Let (X, X ) and (Y, Y) be measurable spaces. Formally, a kernel from X to Y is a function 

ν : X → M (Y, Y) such that for every B ∈ Y , the map x 7 → ν(x)( B) is measurable. Given a measure μ ∈ M (X, X ) and a kernel ν : X → M (Y, Y) their composition measure 

μ ⊗ ν ∈ M (X × Y, X ⊗ Y ) is given by 

(μ ⊗ ν)( A × B) = 

∫

> A

ν(x)( B)μ(dx ).

Intuitively, for each slice corresponding to a fixed value x ∈ X, the measure on Y is given by ν(x), and these slice measures are then averaged according to μ.A measurable space (X, X ) is called standard Borel if there exists a metric on X that makes a complete, separable space, such that X is the associated Borel σ-algebra. We use the fact that Xω with Cantor σ-algebra is standard Borel when X is finite or countable [40], which suffices to apply the following result from [32]. 

▶ Theorem 1 (disintegration) . Given a probability measure μ ∈ P (X × Y, X ⊗ Y ), where 

(Y, Y) is standard Borel, there exists a kernel ν : X → P (Y, Y), such that μ = ˆμX ⊗ ν. Such a kernel is unique up to ˆμX -a.e. equivalence. 

We call such ν a disintegration kernel of μ with respect to X and denote ν(x, ·) as μx.Intuitively, μx represents the conditional distribution of Y given X = x.Returning to the example above, let μ be the uniform distribution on [0 , 1] × [0 , 1] .Disintegrating μ with respect to the first coordinate yields that, for almost every x ∈ [0 , 1] ,the measure μx is the uniform distribution on [0 , 1] . The qualification “almost every” is necessary because the disintegration kernel is unique only up to a set of measure zero. 

## 2.4 Cut and Disintegration in Infinite Words 

Fix a finite set of atomic propositions AP . We work with the space (2 AP )ω of infinite words over 2AP , where at each step every atomic proposition is either true or false. Observe that there is a natural bijection (2 AP )ω ∼= ∏ 

> a∈AP

2ω ,. We will repeatedly use this correspondence. We also use the natural identification 2n × 2ω ∼= 2ω , given by concatenation. In this subsection, we consider disintegration in the space of infinite words. To disintegrate, we need to represent (2 AP )ω as a product of two spaces. For that purpose, we introduce a notion of a cut .Informally, a cut can be viewed as a generalized notion of a timestamp. It specifies which part of a trace is conditioned on. Intuitively, a cut separates a trace into a prefix, representing the past, and a suffix, representing the future; disintegration then yields a distribution over future behavior conditioned on the past. Formally, a cut is a function C : AP → Nω . Given a cut C, we define the sets L(C) and 

U (C) by 

L(C) = ∏

> a∈AP

2C(a), U (C) = ∏  

> a∈AP ;C(a)̸=ω

2ω .

Intuitively, the value C(a) specifies how much of the history of the proposition a is revealed. The factor 2C(a) represents the portion of the trace over a that is conditioned on, 6 Disintegration Temporal Logic for Probabilistic Hyperproperties 

while the corresponding factor 2ω in U (C) represents the remaining unconstrained suffix. If C(a) = ω, then the entire history of a is fixed, and therefore no suffix component for a

appears in U (C).

▶ Note 2. There is a natural bijection L(C) × U (C) ∼= (2 AP )ω .Hence, we can view a measure μ ∈ P ((2 AP )ω ) as a probability measure on L(C) × U (C).For each T ′ ∈ L(C), we may then consider a disintegration measure μT ′ as given by Theorem 1. 

▶ Example 3. Consider the set AP = {a1, a 2} and a cut C such that C(a1) = 3 and 

C(a2) = 2 . Then L(C) = 2 3 × 22, and U (C) = 2 ω × 2ω . An illustration is given in Figure 1.              

> a1
> a2
> 0123456
> conditioned future
> Figure 1 Illustration of a cut Cwith C(a1) = 3 and C(a2) = 2 . The shaded region represents the part of the trace that is conditioned on.

## 2.5 Constructive disintegration 

In this subsection, we give a constructive interpretation of the abstract notion of disintegration on the space of infinite words. This result allows us to compute disintegration measures in certain cases and is later used to develop model-checking algorithms. We first observe that disintegration with respect to a finite prefix coincides with ordinary conditional probability. Since the set of prefixes of a fixed finite length is finite, conditioning on a specific prefix amounts to conditioning on an event of positive probability. In this case, the disintegration measure is given by the classical formula 

P(X | Y ) = P(X ∩ Y )

P(Y ) ,

whenever P(Y ) > 0.

▶ Lemma 4. Let μ ∈ P ((2 AP )ω ) and let C : AP → N be a cut that takes only finite values. Then for every measurable set B ⊆ U (C), for ˆμL(C)-a.e. T ′ ∈ L(C),

μ({T ′} × B)

μ({T ′} × U (C)) = μT ′ (B),

where the fraction is well-defined ˆμL(C)-a.e. 

Proof. Let T ′ ∈ L(C) be such that μ({T ′} × U (C)) > 0. By the definition of disintegration, for every measurable B ⊆ U (C), we have 

μ({T ′} × B) = 

∫  

> T′′ =T′

μT ′′ (B)ˆ μL(C)(dT ′′ ) = = μ({T ′} × U (C)) · μT ′ (B).

Since L(C) is finite, the set of T ′, such that μ({T ′} × U (C)) = 0 has a measure zero. Therefore, the stated equality is well-defined and holds ˆμL(C)-a.e. ◀M. Carelli and B. Finkbeiner 7

Next, we show that disintegration with respect to a cut that assigns the value ω can be obtained as the limit of disintegrations with respect to its finite prefixes. 

▶ Theorem 5. Let μ ∈ P ((2 AP )ω ) and let C : AP → Nω be a cut. For every measurable set 

B ⊆ U (C), for ˆμL(C)-almost every T ′ ∈ L(C),

μ(T ′ 

> n

× B)

μ(T ′ 

> n

× U (C)) −−−−→ 

> n→∞

μT ′ (B),

where 

T ′ 

> n

:= { T ′′ ∈ L(C) | T ′′ [n] = T ′[n] }.

Every fraction in the sequence is well-defined, and the limit exists ˆμL(C)-almost everywhere. 

Proof. Fix a measurable set B ⊆ U (C). For each n ∈ N, the set T ′ 

> n

⊆ L(C) is finite and measurable. By Lemma 4, for all n, for ˆμL(C)-a.e. T ′ ∈ L(C), we have 

μ(T ′ 

> n

× B)

μ(T ′ 

> n

× U (C)) = μT ′[n](B). (2) By the Conditioning Limits Theorem [ 32 , Theorem 9.24], since {T ′} = ⋂ 

> n∈N

T ′

> n

, we obtain 

μT ′[n](B) −−−−→ 

> n→∞

μT ′ (B) (3) for ˆμL(C)-almost every T ′ ∈ L(C). The set where Equation (3) does not hold or Equation (2) does not hold for some n is a countable union of sets of measure zero, and hence has measure zero. ◀

## 2.6 Modeling the system 

We consider labeled Markov Decision Processes .

▶ Definition 6. An MDP is a tuple (S, A, AP, l, R, i ), where S, A, and AP are finite sets of states, actions, and atomic propositions, respectively, l : S → 2AP is a labeling function, 

i ∈ P (S) is an initial distribution, and R : S × A → P (S) is a transition function. 

A Markov chain is an MDP M = ( S, A, AP, l, R, i ) whose action set is a singleton, i.e. 

A = {∗} . Since the single action is irrelevant, we write a Markov chain as (S, AP, l, P, i ),omitting A, where P (s, s ′) := R(s, ∗)( s′).Given a Markov chain M = ( S, AP, l, P, i ), we call the sequence of states π ∈ S∗ ∪ Sω a

path and its corresponding labeling trace l(π) ∈ (2 AP )∗ ∪ (2 AP )ω is given by l(π)( i) = l(π(i)) ,for every i < |π|. M induces a probability measure ν ∈ P (Sω ) on the set of infinite paths. For n ∈ N and a finite path p ∈ Sn, the measure of the corresponding cylinder set is given by 

ν(π ∈ Sω | π[n] = p) = i(p(0)) 

> n−2

∏

> i=0

P (p(i), p (i + 1)) .

Moreover, M induces a probability measure μ ∈ P ((2 AP )ω ) on the set of infinite traces. For n ∈ N and a finite trace t ∈ (2 AP )n, the measure of the cylinder set is given by 

μ(T ∈ (2 AP )ω | T [n] = t) = ν(π ∈ Sω | l(π[n]) = t) = 8 Disintegration Temporal Logic for Probabilistic Hyperproperties 

= ∑

> p∈Sn;l(p)= t

i(p(0)) 

> n−2

∏

> i=0

P (p(i), p (i + 1)) .

We call a Markov chain M = ( S, AP, l, R, i ) precisely labeled , if for every s1, s 2 ∈ S, if 

l(s1) = l(s2), then s1 = s2, i.e., labeling function l : S → 2AP is injective. Throughout the paper, we use a construction in which one component, M1, “blindly” schedules another component, M2. Intuitively, M1 probabilistically generates a blind scheduler for M2. By blind we mean that the scheduler does not depend on the states or transitions of M2, but only on the step index. The probability of generating a particular scheduler is determined solely by M1 and is independent of the behavior of M2. This construction allows us to reason about the behavior of M2 conditioned on the scheduling trace produced by M1.We formalize this intuition using the notion of a cascade product M1 ⊗ M2.

▶ Definition 7. Given two MDPs M1 = ( S1, A, AP 1, l 1, R 1, i 1) and M2 = ( S2, 2AP 1 , AP 2, l 2, R 2, i 2),we define their cascade product as M1 ⊗ M2 = ( S1 × S2, A, AP 1 ∪ AP 2, l, R, i ), where, 

l(s1, s 2) = l1(s1) ∪ l2(s2),R(( s1, s 2), a )( s′

> 1

, s ′

> 2

) = R1(s1, a )( s′

> 1

) · R2(s2, l 1(s′

> 1

))( s′

> 2

),i(s1, s 2) = i1(s1) · i2(s2).

▶ Note 8. If M1 is a Markov chain, then M1 ⊗ M2 is a Markov chain as well. 

## 2.7 Automata 

The algorithm in Section 6 uses several types of automata on infinite words. 

▶ Definition 9 (Alternating Büchi automaton) . An alternating Büchi automaton (ABA) is a tuple A = ( Q, Σ, δ, q 0, F ), where Q is a finite set of states, Σ is a finite alphabet, q0 ∈ B+(Q)

is the initial state formula, F ⊆ Q is the set of accepting states, and the transition function is δ : Q × Σ → B+(Q), where B+(Q) is the set of positive Boolean combinations of states. Let w ∈ Σω . A run of A on w is a (possibly infinitely branching) Q-labeled tree r ⊆ N∗

such that: the set of labels of the children of the root satisfies q0;for every node u of depth i + 1 labeled by q, the set of labels of the children of u satisfies the formula δ(q, w (i)) .A run is accepting if every infinite branch visits F infinitely often. The language L(A) ⊆ Σω

is the set of traces that admit an accepting run. 

We call an ABA existential (universal ) if every formula δ(q, σ ) and the initial formula q0

use only disjunctions ( conjunctions). In this case we may write δ(q, σ ) = Q′ or q0 = Q′ for some Q′ ⊆ Q, meaning ∨ 

> q′∈Q′

q′ ( ∧ 

> q′∈Q′

q′). A reachability (safety ) alternating automaton is defined as ABA above, except that a run is accepting if every infinite branch that visits F at least once (if every infinite branch stays in F forever ). 

▶ Definition 10. A deterministic Rabin automaton (DRA) is a tuple D = ( Q, Σ, δ, q 0, Acc ),where Q is a finite set of states, q0 ∈ Q is the initial state, δ : Q × Σ → Q is a total transition function, and Acc = {(Li, K i) | i ∈ I},, where Li, K i ⊆ Q, is a (finite) Rabin acceptance condition. For w ∈ Σω , the unique run ρ ∈ Qω is given by ρ(n + 1) = δ(ρ(n), w (n)) . It is 

accepting if there exists (Li, K i) ∈ Acc such that ρ visits Li only finitely many times and Ki

infinitely many times. M. Carelli and B. Finkbeiner 9

## 3 DTL 3.1 Syntax 

Let AP be a finite set of atomic propositions. The formulas of Disintegration Temporal Logic are given by the following grammar, where ϕ denotes state formulas and ψ denotes quantitative formulas: 

ϕ := a | ¬ ϕ | ϕ ∧ ϕ | ψ ≤ r | ϕ | ϕ U ϕ, ψ := r · ψ | ψ + ψ | P(ϕ | [A], B ).

Here a ∈ AP and A, B ⊆ AP , and r ∈ R.We use additional temporal operators ϕ = ¬ ¬ϕ; ϕ = true U ϕ; ϕ1 R ϕ2 =

¬(¬ϕ1 U ¬ ϕ2). Moreover, P(ϕ) = P(ϕ | [∅], ∅).The scope of an occurrence of a probabilistic operator P(ϕ | [A], B ) is the subformula ϕ.All occurrences of atomic propositions or temporal operators in the scope of a probabilistic operator are said to be bound . Note that the occurrences of atomic propositions from A ∪ B

in the conditional part [A], B are not bound by this operator. A formula ϕ is called closed if every occurrence of an atomic proposition or a temporal operator is bound by some probabilistic operator. 

## 3.2 Semantics 3.2.1 Informal explanation. 

The main feature of the logic is the conditional probability operator. A formula is evaluated relative to a trace T , a timestamp n, a cut C, and a probability measure μ.The timestamp represents the current position in the trace; temporal operators update the timestamp. The cut specifies which part of the trace is conditioned on, namely the prefix component L(C).The formula P(ϕ | [A], B ) updates the cut from C to C′ by assigning elements of A

the current timestamp n and elements of B the value ω. Intuitively, this corresponds to conditioning on the behavior of atomic propositions in A up to the current step and on the entire behavior of atomic propositions in B. The operator P(ϕ | [A], B ) is interpreted quantitatively and denotes the probability of satisfying ϕ, conditioning on TL(C′).

## 3.2.2 Semantics 

We now define the semantics formally. For every cut C, we fix a disintegration kernel of μ

with respect to L(C). Later, we show that the semantics do not depend on the particular choice of this kernel. Given a trace T ∈ (2 AP )ω , a timestamp n ∈ N, a cut C : AP → Nω , and a measure 

μ ∈ P ((2 AP )ω ), the satisfaction relation T, C, n ⊨μ ϕ is defined inductively as follows: 

T, C, n ⊨μ a iff a ∈ T (n),T, C, n ⊨μ ¬ϕ iff T, C, n ̸ ⊨μ ϕ, T, C, n ⊨μ ϕ1 ∧ ϕ2 iff T, C, n ⊨μ ϕ1 and T, C, n ⊨μ ϕ2,T, C, n ⊨μ ψ ≤ r iff JT, C, n, ψ Kμ ≤ r, T, C, n ⊨μ ϕ iff T, C, n + 1 ⊨μ ϕ, T, C, n ⊨μ ϕ1 U ϕ2 iff ∃i. T, C, n + i ⊨μ ϕ2 and ∀j < i. T, C, n + j ⊨μ ϕ1.10 Disintegration Temporal Logic for Probabilistic Hyperproperties 

The formula ψ is interpreted quantitatively over a trace T , a timestamp n ∈ N, a cut C

and a measure μ, with its semantics taking values in R.

JT, C, n, r · ψKμ = r · JT, C, n, ψ Kμ,

JT, C, n, ψ 1 + ψ2Kμ = JT, C, n, ψ 1Kμ + JT, C, n, ψ 2Kμ,

JT, C, n, P(ϕ | [A], B )Kμ = μTL(C′) (t ∈ U (C′) | (TL(C′), t ), C ′, n ⊨μ ϕ).

In the last line, the cut C′ : AP → Nω is defined for p ∈ AP by 

C′(p) = 



C(p), for p / ∈ A ∪ B, max (C(p), n ), for p ∈ A, ω, for p ∈ B. 

(4) Here, TL(C′) denotes the projection of T ∈ (2 AP )ω ∼= L(C′) × U (C′) onto L(C′), and 

μTL(C′) is the corresponding disintegration measure obtained from the fixed disintegration kernel. 

▶ Note 11. Disintegration measures for different prefixes must be obtained from the same kernel. Otherwise, inconsistent choices may violate the measurability of the semantic interpretation. 

▶ Example 12. Consider the cut C with C(a1) = 3 and C(a2) = 2 from Example 3. Suppose we evaluate the formula JT, C, 5, P(ϕ | [a1], a 2)Kμ. The cut must then be updated according to Equation (4). For the resulting cut C′, we have C′(a1) = 5 , and C′(a2) = ω. Indeed, the formula requires conditioning on the history of a1 up to the current time and on the entire history of a2.                  

> a1
> a2
> 0123456
> future original condition updated
> Figure 2 Illustration of a cut update. The blue region represents the original cut Cwith C(a1) = 3
> and C(a2) = 2 . The red region shows the additional portion required after evaluation of the formula, yielding the updated cut C′with C′(a1) = 5 and C′(a2) = ω.

Given a Markov chain M inducing a measure μ and a closed DTL formula ϕ, we define 

M ⊨ ϕ iff T, C 0, 0 ⊨μ ϕ, 

where C0(p) = 0 for all p ∈ AP and T is an arbitrary trace. Since ϕ is closed, the evaluation does not depend on the choice of T .The DTL model-checking problem is to decide, given a Markov chain M and a closed DTL formula ϕ, whether M ⊨ ϕ holds. 

## 3.2.3 Correctness and Undecidability 

Recall that disintegration kernels are defined uniquely up to a.e. equivalence. The following theorem formalizes the correctness of the semantics and shows that the semantic evaluation is independent of the particular choice of disintegration kernels. M. Carelli and B. Finkbeiner 11 

▶ Theorem 13. Given DTL formulas ϕ and ψ, defined by the grammar above, a cut 

C : AP → Nω , a timestamp n ∈ N, and a probability measure μ, the following statements hold. 

1. The function f : (2 AP )ω → R defined by 

f (T ) = JT, C, n, ψ Kμ

is measurable. Moreover, different choices of disintegration kernels yield μ-a.e. equivalent functions. 

2. The set 

{ T ∈ (2 AP )ω | T, C, n ⊨μ ϕ }

is measurable. Moreover, different choices of disintegration kernels yield μ-a.e. equivalent sets. 

Proof. We prove both statements simultaneously by induction on the structure of the formula. 

Base case. For atomic formulas ϕ = a, measurability is immediate, and the evaluation does not depend on any disintegration kernel. 

Induction step (Boolean and temporal operators). Assume the induction hypo-theses hold for all strict subformulas. Negation and conjunction correspond to complementation and intersection of measurable sets, respectively, and therefore preserve measurability. The a.e.-equivalence is preserved under these operations. The operator ϕ defines a measurable set since it shifts the timestamp and relies on the induction hypothesis. The formula ϕ1 U ϕ2 defines a countable Boolean combination of measurable sets and is therefore measurable. The a.e.-equivalence is again preserved. 

Induction step (Comparison operator). For formulas of the form ψ ≤ r, measurabil-ity follows from the induction hypothesis applied to ψ. Moreover, if two interpretations of ψ

are μ-a.e. equivalent, then the induced satisfaction sets are also μ-a.e. equivalent. 

Induction step (Quantitative operators). By the induction hypothesis, the operators 

r · ψ and ψ1 + ψ2 define linear combinations of measurable functions and are therefore measurable. The a.e.-equivalence is preserved. 

Induction step (Probabilistic operator). It remains to consider ψ = P(ϕ | [A], B ).Let C′ be the updated cut defined in (4). By the definition, JT, C, n, ψ Kμ depends only on the projection TL(C′). Hence, it is enough to prove the measurability and ˆμL(C′)-a.s. equivalence of the function g : L(C′) → R defined by 

g(T ′) = μT ′ (t ∈ U (C′) | (T ′, t ), C ′, n ⊨μ ϕ).

The characteristic function 1ϕ : L(C′)×U (C′) → R of the set {T ∈ (2 AP )ω | T, C ′, n ⊨μ ϕ}

is measurable by the induction hypothesis. By the [ 32 , Lemma 3.2], the function 1ϕ · μT ′ : L(C′) → M (U (C′)) is a kernel. For 

T ′ ∈ L(C′),

g(T ′) = 

∫ 

> U(C′)

1ϕ(T ′, t )μT ′ (dt ) = 1ϕ · μT ′ (U (C′)) .

Thus, g is measurable by the definition of the kernel. We now show ˆμL(C′)-a.e. equivalence in two steps. First, if a different disintegration kernel with respect to L(C′) is chosen, then by Theorem 1 the kernels agree ˆμL(C′)-a.e., and so do the resulting functions g.12 Disintegration Temporal Logic for Probabilistic Hyperproperties 

Next, consider a different choice of all disintegration kernels except for the one with respect to L(C′). Denote the characteristic function for this choice as 1′ 

> ϕ

and the resulting function as g′. By the induction hypothesis 1′ 

> ϕ

is μ-a.e. equivalent to 1ϕ. Denote the set where they differ as ∆. Then 

0 = μ(∆) = 

∫

> L(C′)

∫ 

> U(C′)

1∆(T ′, t )μT ′ (dt )ˆ μL(C′)(dT ′) = =

∫

> L(C′)

∫ 

> U(C′)

|1ϕ(T ′, t ) − 1′

> ϕ

(T ′, t )|μT ′ (dt )ˆ μL(C′)(dT ′) ≥≥

∫

> L(C′)

|

∫ 

> U(C′)

1ϕ(T ′, t )μT ′ (dt ) −

∫ 

> U(C′)

1′

> ϕ

(T ′, t )μT ′ (dt )|ˆμL(C′)(dT ′) = =

∫

> L(C′)

|g(T ′) − g′(T ′)|ˆμL(C′)(dT ′)

The second equality follows from the definition of the disintegration kernel and the fact that all μT ′ are given by the same kernel. The inequality is due to the reverse triangle inequality. Integral of nonnegative function |g′(T ′) − g(T ′)| with respect to ˆμL(C′) is zero, hence the function is zero ˆμL(C′)-a.e. 

◀▶ Theorem 14. The DTL model-checking problem is undecidable. 

Proof. We reduce from strict cutpoint emptiness for probabilistic finite automata (PFA), which is undecidable (already for cutpoint 1/2) [43]. 

PFA emptiness. An instance consists of a finite set of stochastic matrices G ⊆ Rd×d,an initial distribution π ∈ Rd, and an accepting vector f ∈ { 0, 1}d. The question is whether there exist m ∈ N and M0, . . . , M m−1 ∈ G such that 

π⊤M0M1 · · · Mm−1f > 12 .

Encoding letters. Fix a finite set of propositions AP and a surjection η : 2 AP ↠ G.Let af /∈ AP be a fresh proposition and set AP f := AP ∪ { af }.

Markov chain. We define a Markov chain M = ( S, AP f , l, P, i M ) with state space 

S := 2 AP × { 1, . . . , d }. The initial distribution is iM (x, j ) = πj /|2AP | for all x ∈ 2AP and 

j ∈ { 1, . . . , d }. The labeling is 

l(x, j ) = 

{x, if fj = 1 ,x ∪ { af }, otherwise .

Transitions are given by, for all x, y ∈ 2AP and j, k ∈ { 1, . . . , d },

P ((x, j ), (y, k )) = 1

|2AP | · η(y)( j, k ).

Since each η(y) is stochastic, P is stochastic as well. 

DTL formula. Consider the DTL formula 

ϕ := P( (P( af | [AP ]) > 0.5))) > 0.

Correctness. Every finite AP -prefix (equivalently, every t ∈ (2 AP )m) has positive probability in M by construction. Conditioning on the observation Obs = t fixes the letter M. Carelli and B. Finkbeiner 13 

sequence t(0) , . . . , t (m − 1) and thus the distribution on the internal index j ∈ { 1, . . . , d }

evolves as π⊤η(t(0)) · · · η(t(m − 1)) . Hence the conditional probability of seeing af at the corresponding step equals 

π⊤η(t(0)) · · · η(t(m − 1)) f. 

Therefore, M ⊨ ϕ holds iff there exists a finite word of matrices from G whose accept-ance probability exceeds 1/2, i.e. iff the PFA instance is nonempty above cutpoint 1/2.Undecidability follows. ◀

## 4 Expressing properties in DTL 4.1 Security properties and independence 

We show how DTL can express well-known security properties formulated in terms of independence and conditional independence between different components of an execution trace. Typically, such properties require that conditioning on different high-level behaviors does not change the induced distribution over low-level behaviors. These properties fall within the linear fragment of DTL, introduced in Section 5, for which we provide a polynomial-time model-checking algorithm. 

## 4.1.1 Probabilistic Non-interference 

To define probabilistic non-interference, originally formulated by Gray [ 31 , 30 ], consider a probabilistic system that communicates with agents through high- and low-level channels. Informally, the property requires that the low-level trace does not give any information on the distribution of the high-level trace, or equivalently, there is no probabilistic information leakage from high-level data to low-level data. As defined in [ 31 ], probabilistic non-interference can be phrased as a conditional-independence requirement. For every n ∈ N, for any high-level input trace H of length n, any low-level input and output traces L and O of length n,and any low-level output event o at step n+ 1, whenever the joint event (H, L, O ) has positive probability, the probability of observing o at step n+ 1 conditioned on (H, L, O ) equals the probability conditioned on (L, O ).We demonstrate how to express this property in DTL. Let M be a Markov chain that models our probabilistic system, and let the set of atomic propositions be AP = {h, l, o },where h and l represent the high- and low-level inputs and o the low-level output. We say that M satisfies probabilistic non-interference if 

M ⊨ P

( (P( o | [h, l, o ]) = P( o | [l, o ]) ))

= 1 .

The informal quantification “for all input traces of length n” is captured by the outermost quantification with probability 1 and the globally operator . The innermost equality between probabilistic operators requires that o is independent from the history of h, conditioning on the history of l and o.

▶ Remark 15. In [ 31 ], the system may have an arbitrary number of channels and variables. For readability, we use one high-level input, one low-level input, and one output proposition. The formulation extends directly to multiple variables by replacing every single proposition with a tuple of propositions and updating the conditioning sets accordingly. 

Relation to other version of probabilistic non-interference. 

In the literature [ 1, 19 , 46 ], another version of probabilistic non-interference is used. It requires that the distribution of the low-observable trace is the same for every pair of 14 Disintegration Temporal Logic for Probabilistic Hyperproperties 

low-level equivalent initial states. This version is a weakening of the original probabilistic non-interference property: whereas the original requires independence from the high-level history at every step of the execution, this version only requires independence from the high-level information of the initial state, leaving open the possibility that high-level information leaks through the transition dynamics. For finite state systems, model checking of this weaker version can be reduced to a finite number of labeled Markov chain equivalence checks (or probabilistic bisimulation conditions). In systems with ongoing interaction, the difference is significant. A system may conceal the high-level initial state and thus satisfy the weaker property, while still allowing future low-level observations to depend on the evolving high-level state. In such a system, information about high-level behavior may be leaked dynamically during execution even though the distribution of complete low-level traces is independent of the initial secret. The conditional-independence formulation rules out precisely this form of information flow. In that sense, the original notion of probabilistic non-interference is similar to the possibilistic trace-based version [27, 18, 17]. 

## 4.1.2 Perfect Indistinguishability 

Perfect indistinguishability [ 45 , 33 , 18 ], also known as perfect secrecy, is commonly formulated as an indistinguishability requirement for ciphertext distributions, which in the perfect case reduces to (a form of) independence. A probabilistic encryption system consists of two components: a key-generation procedure and an encryption procedure that, given a message and a key, produces a ciphertext (which can later be decrypted using the key). For simplicity, we model messages as finite traces over 2{m} and ciphertexts as finite traces over 2{c}. We assume that M D receives a message trace and produces a ciphertext trace, with key generation happening internally. 

M D satisfies perfect indistinguishability if for any two messages T1, T 2 and any ciphertext trace D, the probability of outputting D on input T1 equals the probability of outputting D

on input T2.As above, we model universal quantification over messages by composing M D with a Markov chain Mmsg that generates message letters uniformly. Let M := Mmsg ⊗ M D. 

To express perfect indistinguishability in DTL, we need to require that the message and ciphertext projections are independent. We can do it as follows: 

M ⊨ P

( (P( (P(c | [m]) = P(c)) | [c]) = 1 ))

= 1 ∧∧P

( (P( (P(m | [c]) = P(m)) | [m]) = 1 ))

= 1 .

Let us demonstrate that the conjunction above is equivalent to the independence of the projections of an execution trace onto (2 {m})ω and (2 {c})ω . Let μ be the probability measure induced by M . Assume the projections are not independent. Then by Lemma 4.6 in [ 32 ]there exist n, k ∈ N and finite traces Tm ∈ (2 {m})n and Tc ∈ (2 {c})k such that 

μ(( Tm(2 {m})ω ) × (2 {c})ω ) · μ((2 {m})ω × (Tc(2 {c})ω )) ̸ ≠

= μ(( Tm(2 {m})ω ) × (Tc(2 {c})ω )) .

Without loss of generality, assume n ≤ k, and assume that for the shorter prefix of Tc, it does not hold; otherwise, we can shorten it. In that case, there is at least one T ′ 

> m

∈ (2 {m})k,such that T ′

> m

[n] = Tm, and 

μ((2 {m})ω × (Tc(2 {c})ω ) | (2 {m})ω × (Tc[k − 1](2 {c})ω )) ̸ =M. Carelli and B. Finkbeiner 15 ̸

= μ((2 {m})ω × (Tc(2 {c})ω ) | (( T ′

> m

(2 {m})ω ) × (Tc[k − 1](2 {c})ω )) ,

which fails the first conjunct. If n ≥ k, the second conjunct fails. 

## 4.2 Stochastic environment 

Many stochastic systems consist of several interacting components, one of which models an external stochastic environment. Temporal specifications concern infinite executions, and each complete environment execution (i.e., an infinite trace) naturally induces a conditional probability distribution over the executions of the remaining system. DTL captures these conditional distributions through conditioning on infinite traces, allowing one to express not only the overall probability of satisfying a temporal specification, but also how this probability varies across different environment executions. This setting naturally occurs in a variety of applications. 

Communication networks. A communication network interacting with a stochastic environment modeling the quality of the communication channel and the resulting burst errors [ 26 , 24 , 28 ]. The Gilbert–Elliott model is widely used to capture burst errors by representing the channel quality as a two-state Markov chain. 

Hardware systems. A hardware component interacting with an external stochastic environment modeling radiation-induced transient faults (soft errors), which occur probabil-istically [38, 13]. 

Embedded systems. The physical environment of an embedded system can be modeled by a finite-state Markov chain, as in Markov jump linear systems, where the Markov chain represents environmental changes [21]. Let the environment be modeled by a Markov chain Menv over the set of atomic proposi-tions AP env . Let the system be modeled by an MDP M D whose actions are from 2AP env .Their cascade product M := Menv ⊗ M D models the joint behavior of the environment and the system. Let μ denote the probability measure on infinite traces induced by M .Let the temporal formula ϕbad specify an undesirable system behavior. DTL can express properties such as With probability at least 0.1, the environment produces an execution such that, conditioned on this execution, the probability that the system exhibits the undesirable behavior is greater than 0.4. This is expressed by the formula 

M ⊨ P

(

P(ϕbad | AP env 

) > 0.4

)

> 0.1.

In other words, although the system may perform reliably under most environment executions, there exists a substantial subset of environment executions under which its reliability is significantly lower. Such concentration of failures cannot be detected by considering only the overall probability of undesirable behavior. For example, the mean (overall) probability of undesirable behavior over all environment executions may be as low as 0.05, even though there exists a non-negligible subset of environment executions for which the conditional probability of failure exceeds 0.4. Consequently, two systems with the same overall probability of failure may exhibit fundamentally different behavior: one may exhibit a relatively uniform probability of failure across all environment executions, whereas the other may exhibit a high probability of failure concentrated on a significant subset of environment executions. Classical probabilistic temporal logics reason only about the overall probability of satisfying a temporal specification and therefore cannot distinguish between these situations. In contrast, by conditioning on complete environment executions (i.e., infinite traces), DTL enables reasoning not only about the overall probability of satisfying a temporal specification, but also about how this probability depends on environment execution, 16 Disintegration Temporal Logic for Probabilistic Hyperproperties 

revealing whether failures are uniformly distributed or concentrated on particular classes of environment executions. In particular, replacing the threshold 0.4 by 0 or 1 yields a property in the qualitative fragment of DTL, for which we present a model-checking algorithm in Section 6. 

## 4.3 Distribution transformers and probabilistic automata 

We briefly discuss two verification frameworks for probabilistic systems whose specifications can be naturally expressed in DTL. The common theme is that, rather than quantifying existentially over schedulers, one can reason about a distribution over schedulers and ask for the probability mass of schedulers satisfying a given specification. This modification changes the original problem and yields a "more probabilistic" counterpart. For safety and reachability objectives, the two formulations coincide. For more general ω-regular objectives, however, they may differ, and the new one may even become decidable in cases where the classical existential formulation is undecidable. Properties that compare probabilities only with 0 and 1 belong to the qualitative fragment of DTL, which we introduce in Section 6 and show to be decidable. 

## 4.3.1 MDP as distribution transformers 

Fix an MDP M D and a blind scheduler, i.e. an infinite sequence of actions. Under such a scheduler, the execution of M D induces a sequence of distributions over states. Properties that refer to this sequence are often called distributional properties . This view received significant attention over the last decade [ 35 , 3, 4], with applications in a number of areas, e.g., multi-agent systems [2]. Distributional properties require some linear (specified by linear inequalities) labeling, i.e., functions P(S) → 2Σ, for the set of states of the MDP S and the set of labels Σ.

Distributional Model Checking Problem asks, given an MDP M D , a regular set of blind schedulers A, a labeling function λ, and a Büchi automaton B over 2Σ, whether there is a scheduler in A, such that it produces the sequence of labels under λ which is in L(B).Distributional properties can be modeled with DTL, replacing the existential quantification "there is a scheduler" by the probabilistic requirement "there is a set of schedulers of positive measure". Formally, instead of a regular set of schedulers A, assume a probability distribution over blind schedulers given by a Markov chain Msched , and consider the cascade product 

M := Msched ⊗ M D . Let AP sched denote the atomic propositions encoding scheduler choices in Msched . Assume that M is precisely labeled. For a state s of M D , let ϕs be a (state) formula describing that the M D -coordinate of M on this state is s. Then the conditional probability of being in state s given the scheduler trace is expressible as P(ϕs | [AP sched ]) .Since the logic admits linear combinations of such probabilities, we can encode any linear labeling λ, and we can express the LTL property corresponding to L(B) (when L(B) is LTL-expressible). Let ϕ denote the resulting formula. We can then require that the set of schedulers satisfying ϕ has positive measure. We define the property 

M ⊨ P(ϕ) > 0

If L(B) is a reachability property, this essentially coincides with pure existential quantification. In general, however, it is stronger, since it requires a set of schedulers of positive probability mass rather than a single scheduler. M. Carelli and B. Finkbeiner 17 

## 4.3.2 Probabilistic Automata over Infinite Words 

Probabilistic automata over infinite words [ 7, 15 ] can be viewed as an MDP in the sense of our modeling framework, together with a Büchi, Streett, or Rabin acceptance condition over 2AP .A central question studied in this setting is whether there exists a blind scheduler such that the probability of producing an infinite trace satisfying a given acceptance condition meets a prescribed linear requirement. Typically, this is phrased by comparing the acceptance probability against thresholds such as 1, 0, or an arbitrary ϵ ∈ [0 , 1] . It is known that certain variants, in particular, even those involving comparison against 0 or 1, lead to undecidability [7]. We can model this setting by assuming a Markov chain Maut with a set of atomic propositions AP aut such that the action set of M D is 2AP aut . Let M := Maut ⊗ M D be the corresponding cascade product. Any acceptance condition over 2AP can be modeled by a DTL formula ϕacc without occurrences of probabilistic operators. Then, for any comparison operator ⋆ ∈ {≤ , ≥, =} and any ϵ ∈ [0 , 1] , we can express that the set of schedulers satisfying the comparison has positive probability mass as 

M ⊨ P

(

P(ϕacc | AP aut 

) ⋆ ϵ 

)

> 0.

Note that this property differs from the original one, since it requires the existence of a set of schedulers (of positive measure), rather than a single scheduler. Moreover, as we demonstrate in the next section, this property is decidable for ϵ ∈ { 0, 1}, unlike the original one. 

## 4.4 Related logics 

The most widely used probabilistic temporal logic, PCTL ∗ [8], combines probabilistic and temporal operators in a branching-time setting. In the case of a precisely labeled system (when the labeling is injective), DTL subsumes PCTL ∗. Let J = [ j1, j 2] be an interval. In a precisely labeled system, the PCTL ∗ probabilistic operator PJ (ϕ) can be expressed in DTL by 

j1 ≤ P(ϕ | [AP ]) ≤ j2,

where conditioning on [AP ] corresponds to probability conditioning on the trace up to the current timestamp. An approach orthogonal to ours to incorporating conditional probabilities into temporal logics is studied in [ 9, 5]. The authors consider conditional probabilities of the form P(ψ | ϕ),where both ψ and ϕ are ω-regular events, typically relying on the property P(ψ | ϕ) = P(ψ∧ϕ) 

> P(ϕ)

.As discussed in Section 1.1 of [ 9, 5], this formalism is also used to express certain security properties involving independence. However, it can only express independence between specific ω-regular outcomes (such as l and h), and does not allow expressing independence from an arbitrary history, as required for probabilistic non-interference and perfect indistinguishability. The PHL logic for probabilistic hyperproperties combines probabilistic operators with explicit quantification over schedulers [ 23 ]. To mitigate undecidability, the corresponding model-checking procedure treats families of schedulers jointly. Another logic for probabilistic hyperproperties, HyperPCTL [ 1], combines probabilistic reasoning with quantification over states. This allows, for example, expressing a restricted variant of probabilistic non-interference, stating that the distribution of observable outputs is independent of the initial secret state. The difference from the version of probabilistic non-interference considered in our paper is discussed in Section 4.1.1. 18 Disintegration Temporal Logic for Probabilistic Hyperproperties 

The extension of HyperPCTL called HyperPCTL ∗ [ 49 ] allows nesting of probabilistic measurements over paths of the system (sequences of system states) and measurements over tuples of paths. The key difference is that DTL conditions on observable traces (or projections of traces onto selected atomic propositions), whereas HyperPCTL ∗ reasons about concrete execution paths. Conditioning on traces naturally captures partial observations of an execution and therefore enables the specification of information-flow properties. For example, the version of probabilistic non-interference considered in [ 49 ] coincides with that of HyperPCTL [1] and requires only independence from the initial secret state. Logics for possibilistic hyperproperties such as HyperLTL and HyperCTL ∗ [17 ] allow nesting and alternation of universal and existential path quantifiers, with temporal operators. Our qualitative fragment can be viewed as a probabilistic counterpart of HyperCTL ∗, where strict quantifiers are replaced by their "softer" probabilistic counterparts. There are several program logics for probabilistic conditioning and independence, such as Lilac [ 37 ], Bluebell [ 10 ], and Probabilistic Separation Logic [ 12 ], as well as probabilistic relational program logics for security properties [ 11 , 6]. The proof system presented in [ 20 ]enables reasoning about possibilistic hyperproperties expressed in HyperLTL. 

## 5 Linear Fragment 

The linear fragment of DT L is given by the following grammar: 

κ ::= a | κ ∧ κ | ¬ κ | κ U κ | κ, θ ::= P(κ | [A1]) = P(κ | [A2]) | θ | θ | P(θ | [B]) = 1 ,χ ::= P(θ) = 1 | χ ∧ χ | ¬ χ. 

Here A1, A 2, B ⊆ AP .The linear fragment of DTL can express independence of the suffix from some part of the prefix, including properties like probabilistic non-interference and perfect indistinguishability. In this chapter, we show that model-checking for the linear fragment is in PTIME , using an algorithm inspired by [48]. But first, we define an auxiliary construction. 

## 5.1 Multidynamic Automata 

We define a new type of automata that we use in this section. Intuitively, we use it to prove the generalized notion of equivalence of labeled Markov chains and prove the equivalence of two different dynamics on a graph with respect to some quadratic form. 

▶ Definition 16. A multidynamic automaton is a tuple D = ( n, Q, Q in , Q fin , δ, z ), where 

n ∈ N, Q is a finite set of states, Qin , Q fin ⊆ Q, δ : Q × Q ⇀ (Rn×n × Rn×n) is a partial function, and z : Qin → Rn × Rn is a set of pairs of initial vectors. For k ≥ 1, a word p ∈ Qk is a path of D if p(0) ∈ Qin and δ(p(i), p (i + 1)) is defined for every i < k − 1.For every path p ∈ Qk, define val (p) ∈ Rn ×Rn recursively: if k = 1 then val (p) = z(p(0)) ,and if p′ = p · q is obtained by appending q ∈ Q to p, then 

val (p′) = (δ(p(k − 1) , q )1 val (p)1, δ (p(k − 1) , q )2 val (p)2

).

Given a matrix W ∈ Rn×n, and a multidynamic automaton D = ( n, Q, Q in , Q f in , δ, z ), we say that D globally satisfies the quadratic form W , if for any path p ∈ Qk with p(k−1) ∈ Qf in ,the equality val (p)T 

> 1

W val (p)2 = 0 holds. M. Carelli and B. Finkbeiner 19 

▶ Lemma 17. Given W ∈ Rn×n and a multidynamic automaton D = ( n, Q, Q in , Q fin , δ, z ),deciding whether D globally satisfies W can be done in PTIME( |D| · n).

Proof. For M ∈ Rn×n, denote as vec (M ) ∈ Rn2

a vector representation of a matrix M . By [41], for vec (AM B ) = ( BT ⊗ A)vec (M ), where ⊗ denotes the Kronecker product. For a path of D, p ∈ Qk, define lval (p) ∈ Rn2

recursively. If k = 1 , define lval (p) = 

z(p(0)) T 

> 2

⊗ z(p(0)) T 

> 1

. If p′ = pq is obtained by appending a state q ∈ Q to a path p, define 

lval (p′) = ( δ(p(k − 1) , q )T 

> 2

⊗ δ(p(k − 1) , q )T 

> 1

)lval (p). Note that lval (p) = val (p)T 

> 2

⊗ val (p)T 

> 1

,since (A ⊗ B)( C ⊗ D) = AC ⊗ BD . Thus, val (p)T 

> 1

W val (p)2 = lval (p)vec (W ) and it is enough to show that lval (p)vec (W ) = 0 for every path p.Define L ⊆ RQ×n2

, such that ⃗v ∈ L if for every q ∈ Qf in , the equality ⃗v q vec (W ) = 0 

holds. Let E := {(q, q ′) ∈ Q × Q | δ(q, q ′) is defined }.For (q, q ′) ∈ E, define a linear function ∆q,q ′ : RQ×n2

→ RQ×n2

, such that ∆q,q ′ (⃗v )q′ =(δ(q, q ′)T 

> 2

⊗ δ(q, q ′)T 

> 1

)⃗v q , and for every q′′ ̸ = q′, ∆q,q ′ (⃗v) q′′ = 0 n2

.For every i ∈ N, define L0 = L, and Li+1 = Li ∩ ⋂ 

> (q,q ′)∈E

∆−1 

> q,q ′

(Li). It is a descending chain of linear subspaces of RQ×n2

, thus it stabilizes in |Q| · n2 steps, since every decrease implies the decrease of a dimension, thus ⋂ 

> i∈N

Li = L|Q|· n2 .Clearly, ⋂ 

> i∈N

Li is a set of vectors from L, for which an application of any finite combin-ation of ∆q,q ′ leaves it in L. Note that ∆q,q ′ models a one-step transition, and L models the set of vector, satisfying W , thus D globally satisfies the quadratic form W iff ⃗v z ∈ L|Q|· n2 ,where ⃗v z ∈ RQ×n2

, with ⃗v zq =

{z(q)T 

> 2

⊗ z(q)T 

> 1

, if q ∈ Qin ,

0n2

, else .

The last condition can be checked in PTIME( |Q| · n). ◀

## 5.2 Algorithm 

▶ Theorem 18. The model checking problem for a linear DTL formula χ, and a Markov chain M , is decidable in PTIME( |M |) and in EXPTIME( |χ|).

Proof. By the definition of the semantics, satisfaction of a formula κ, given by a grammar above, does not depend on a cut or a measure. Let Enκ ⊆ (2 AP )ω be the set of traces, satisfying κ from step n (for any measure and any cut). By Lemma 4, for a cut C : AP → N, taking only finite values, n ∈ N, for μ-a.e. T ,

JT, C, n, P(κ | [A]) Kμ = μ(T1 ∈ (2 AP )ω | T1 ∈ Enκ and T1L(CA) = TL(CA))

μ(T1 ∈ (2 AP )ω | T1L(CA) = TL(CA)) ,

where CA is a cut C updated with [A] according to Equation (4). Let μs ∈ P ((2 AP )ω ) be a measure, induced by a Markov chain Ms = ( S, AP, l, R, i s),with is(s) = 1 .As described in [ 22 ], for a given κ, for every s, μs(E0

> κ

) can be computed in PTIME (|M |)

and in EXPTIME( |κ|).Denote M = ( S, AP, l, R, i ). Let ν ∈ P (Sω ) be a measure induced on the set of paths Sω

by M . μ is an image of ν under l. Hence, 

μ(T1 ∈ (2 AP )ω | T1 ∈ Enκ and T1L(CA) = TL(CA)) = 20 Disintegration Temporal Logic for Probabilistic Hyperproperties 

= ν(π ∈ Sω | l(π) ∈ Enκ and l(π)L(CA) = TL(CA)) = = ∑

> s∈S

μs(E0

> κ

) · ν(π ∈ Sω | π(n) = s and l(π)L(CA) = TL(CA)),

Thus, 

μ(T1 ∈ (2 AP )ω | T1L(CA) = TL(CA)) = ν(π ∈ Sω | l(π)L(CA) = TL(CA)).

Hence, for μ-a.e. T , the equality T, C, n ⊨ (P(κ1 | [A1]) = P(κ2 | [A2])) holds iff, 

∑

> s∈S

μs(E0 

> κ1

) · ν(π ∈ Sω | π(n) = s and l(π)L(CA1 ) = TL(CA1 ))) 

ν(π ∈ Sω | l(π)L(CA1 ) = TL(CA1 )) == ∑

> s∈S

μs(E0 

> κ2

) · ν(π ∈ Sω | π(n) = s and l(π)L(CA2 ) = TL(CA2 ))) 

ν(π ∈ Sω | l(π)L(CA2 ) = TL(CA2 )) ,

where C[A1] and C[A2] are version of cut C updated according to Equation (4) with [A1] and 

[A2], respectively. Suppose χ = ( P(θ) = 1) (if χ is a conjunction of such properties and their negations, each conjunct can be handled separately). By the definition θ is obtained by repeated application of k temporal operators , and some number of probabilistic universal operator 

P(· · · | [B]) = 1 to (P(κ1 | [A1]) = P(κ2 | [A2])) . Let us enumerate temporal operators from outermost to innermost with ⋆0 being the outermost probabilistic operator, and ⋆k−1 being the innermost one. Moreover, for an operator ⋆i, denote a subformula to which it is applied as θi, hence θ has a subformula ⋆iθi. Define Bi ⊆ AP as a set of atomic propositions that appear in universal probabilistic operator inside of θi. Formally, for i < k define 

Bi = ⋃(B | θi has a subformula of the form (P(θ′ | [B]) = 1)) .

Additionally define Bk = ∅.Define a multidynamic automaton D = ( |S|, Q, Q in , Q f in , δ, z ), where Q = S × { 0, . . . , k },

Qf in = S × { k}, Qin = {(s, 0) | i(s) > 0}.For (s, 0) ∈ Qin , y ∈ { 1, 2}, and j < |S|, define 

(z(s, 0) y )j =

{i(sj ), if l(s)Ay ∪B0 = l(sj )Ay ∪B0 ,

0, otherwise. For every u < k , and every s, s ′ < |S|, such that P (s, s ′) > 0 define δ(( s, u ), (s′, u + 1)) ∈

R|S|×| S| × R|S|×| S| for every j, j ′ < |S| and y ∈ { 1, 2} as follows 

δ(( s, u ), (s′, u + 1)) y (j, j ′) = =

{P (sj , s j′ ) if l(sj′ )Ay ∪Bu = l(s′)Ay ∪Bu ,

0, otherwise. Additinally, if ⋆u = , define δ(( s, u ), (s′, u )) ∈ R|S|×| S| × R|S|×| S| as follows 

δ(( s, u ), (s′, u ))( j, j ′) = δ(( s, u ), (s′, u + 1))( j, j ′).

Define, W ′ ∈ R|S|× 2, W ′′ ∈ R2×| S|, where 

W ′(j, 1) = μsj (E0 

> κ1

), W ′(j, 2) = 1 ,M. Carelli and B. Finkbeiner 21 

W ′′ (j, 1) = −1, W ′′ (j, 2) = μsj (E0 

> κ2

).

Define W ∈ R|S|×| S| = W ′W ′′ . Note that for every v, w ∈ R|S|,

( ∑

> j< |S|

μsj (E0 

> κ1

)vj )( ∑

> j< |S|

wj ) = ( ∑

> j< |S|

μsj (E0 

> κ2

)wj )( ∑

> j< |S|

vj )

iff (v)T W w = 0 .Consider a path p ∈ Qn of D. It models a path of M (with the first coordinate), with a non-decreasing sequence of numbers (with the second coordinate), which models the temporal operators’ choice of the number of step, during the semantical evaluation of χ. Moreover, for 

j < |S| and y ∈ { 1, 2}, we obtain 

(val (p)y )j = ν(π ∈ Sω | π(n − 1) = sj and ∀i < n.l (π(i)) Ay ∪Bp(i)2 = l(p(i)1)Ay ∪Bp(i)2 ).

Note that for p ∈ Qn, such that p(n − 1) 2 = k,

(val (p)y )j = ν(π ∈ Sω | π(n − 1) = sj and l(π)L(Cy ) = l(p)L(Cy )),

where Cy is a cut that is defined for a ∈ AP by 

Cy (a) = 

{n − 1, if a ∈ Ay ,max (j < n | a ∈ Bp(j)2 or j = 0) , otherwise. Moreover, define a cut C, such that C(a) = max (j < n | a ∈ Bp(j)2 ), hence for μ-a.e. T , such that TL(C) = l(p1)L(C), we obtain T, C, n ⊨ (P(κ1 | [A1]) = P(κ2 | [A2])) iff 

val (p)T 

> 1

W val (p)2 = 0 .Since every possible sequence of choices of temporal operators is considered, M ⊨ χ iff D

globally satisfies the quadratic form W . Thus, the Theorem follows from Lemma 17. 

◀

## 6 Qualitative Fragment 

The qualitative fragment restricts the nesting of probabilistic operators such that every operator except the outermost may compare probabilities only to 0 or 1.We define the qualitative fragment of DTL by the following grammar. 

α ::= ψ ≤ r | α ∧ α | ¬ α, ψ ::= P(γ) | ψ + ψ | r · ψ, γ ::= a | ¬ a | γ ∧ γ | γ ∨ γ | γ | γ U γ | γ R γ | P(γ | [A], B ) = 1 | P(γ | [A], B ) > 0.

The qualitative fragment captures some of the properties discussed in section Sections 4.2 and 4.3. In particular, it can express stochastic environment properties in which inner conditional probabilities are compared only to 0 or 1, distributional properties in which the labeling function checks only whether entries of the distribution vector are 0 or 1, as well as properties of probabilistic automata in which the acceptance probability is required to be 0 or 1. As mentioned earlier, the latter is undecidable in the original setting with strict existential quantification over schedulers; we show below that it becomes decidable when moving to our soft (probabilistic) quantification. We call P(. . . ) = 1 and P(. . . ) > 0 universal and existential probabilistic operators, respectively. 22 Disintegration Temporal Logic for Probabilistic Hyperproperties 

▶ Definition 19. The alternation depth of a qualitative DTL formula is the maximum number of alternations between existential and universal probabilistic operators along any syntactic nesting path. Additionally, each occurrence of U and R is counted as two further alternations. 

For n, m, c ∈ N define gc(n+1 , m ) = cgc(n,m ), and gc(0 , m ) = m. We define N SP ACE (g(n, m )) 

to be the class of languages that are accepted by a nondeterministic Turing machine that runs in space O(gc(n, m )) for some c > 1.Below, we present the main result of this section. It relies on the notion of cascade compatibility, which generalizes properties of models constructed as cascade products of multiple components. A formal definition of this notion is given later in this section. 

▶ Theorem 20. The model-checking problem for a qualitative DTL formula α of alternation depth k, cascade-compatible with a precisely labeled Markov chain M , is in N SP ACE (g(k +1, |α| + |M |)) .

We prove this Theorem by presenting an algorithm that, for every subformula γ, constructs an alternating Büchi automaton Bγ that models the semantic evaluation of γ on an input trace, cut, and timestamp, each encoded by infinite traces. This construction is inspired by the construction from the HyperCTL ∗ algorithm [ 16 ], combined with a reasoning about strongly connected components for the verification with respect to qualitative probabilistic formulas [8]. The automata construction is built recursively on the structure of formula γ, and uses the Miyano-Hyashi construction for every alternation between universal, existential quantifiers, or the temporal operators U and R.

## 6.1 Cascade property 

We now generalize the property satisfied by the cascade product M1 ⊗ M2, which will be used to derive a corollary of Theorem 5. Intuitively, the cascade property expresses a conditional independence assumption. Fix a prefix length n and a prefix T ′ ∈ (2 AP )n. The property states that, once the projection of 

T ′ onto AP ′ is fixed, the future behavior of the atomic propositions in AP ′ is independent of the remaining part of the prefix over AP ′.

▶ Definition 21 (Cascade property) . A measure μ ∈ P ((2 AP )ω ) satisfies the cascade property 

with respect to a set AP ′ ⊆ AP if for every n ∈ N, every prefix T ′ ∈ (2 AP )n, and every measurable set P ⊆ (2 AP ′

)ω , the following equality holds: 

μ(( T ′  

> AP ′

× (2 AP ′

)n)( P × (2 AP ′

)ω )) · μ(T ′(2 AP )ω ) = = μ(T ′(P × (2 AP ′

)ω )) · μ(( T ′  

> AP ′

× (2 AP ′

)n)(2 AP )ω ).

Equivalently, conditioning on the full prefix T ′ yields the same distribution over future 

AP ′-traces as conditioning only on the projected prefix T ′  

> AP ′

.

▶ Note 22. Let M = ( S1, AP 1, l 1, P, i 1) be a Markov chain and let M D = ( S2, 2AP 1 , AP 2, l 2, R, i 2)

be a Markov decision process. Then the measure induced by the cascade product M ⊗ M D 

satisfies the cascade property with respect to AP 1.Given a cut C : AP → Nω and a number n ∈ N, we say that a property B ⊆ U (C)

depends on the first n steps if for all traces T 1, T 2 ∈ (2 AP )ω ,

T 1[n] = T 2[n] ⇒ T 1  

> U(C)

∈ B iff T 2  

> U(C)

∈ B. M. Carelli and B. Finkbeiner 23 

The next result is a corollary of Theorem 5, which uses the cascade property to prove that, in certain cases, the limit is reached in finitely many steps. 

▶ Corollary 23. Let μ ∈ P ((2 AP )ω ) be a measure satisfying the cascade property with respect to AP ′ ⊆ AP . Let C : AP → Nω be a cut and let N ∈ N be such that C(a) = ω for all 

a ∈ AP ′ and C(a) ≤ N for all a ∈ AP ′.Then for ˆμL(C)-almost every T ′ ∈ L(C), for every n ≥ N and every property B ⊆ U (C)

that depends on the first n steps, 

μ({T ′′ ∈ L(C) | T ′′ [n] = T ′[n]} × B)

μ({T ′′ ∈ L(C) | T ′′ [n] = T ′[n]} × U (C)) = μT ′ (B).

Proof. For n′ ∈ N, define 

T ′ 

> n′

= {T ′′ ∈ L(C) | T ′′ [n′] = T ′[n′]}.

By Theorem 5, for fixed n and B, for ˆμL(C)-almost every T ′ ∈ L(C),

μ(T ′ 

> n′

× B)

μ(T ′ 

> n′

× U (C)) −−−−→ 

> n′→∞

μT ′ (B).

By the cascade property, for every n′ ≥ n,

μ(T ′ 

> n′

× B)

μ(T ′ 

> n′

× U (C)) = μ(T ′ 

> n

× B)

μ(T ′ 

> n

× U (C)) .

Hence, the sequence stabilizes from n onward, and we obtain 

μ(T ′ 

> n

× B)

μ(T ′ 

> n

× U (C)) = μT ′ (B).

For fixed n and B, this equality holds on a set of T ′ ∈ L(C) of ˆμL(C)-measure one. Taking a countable intersection over all such n and B preserves measure one, which concludes the proof. ◀

We say that a subformula is in the scope of a total condition on b if it occurs within a probabilistic operator of the form P(ψ | [A], B ) with b ∈ B.

▶ Definition 24. A DTL formula ϕ is cascade-compatible with a measure μ ∈ P ((2 AP )ω ) if for every subformula ϕ′ of ϕ, μ satisfies the cascade property with respect to the set 

{b ∈ AP | ϕ′ is in the scope of a total condition on b}.

A DTL formula is cascade-compatible with a Markov chain if it is cascade-compatible with a measure induced by that Markov chain. 

Since the cascade property is a conditional-independence requirement, it can be expressed in a linear fragment of DTL similarly to probabilistic non-interference. Let M be a Markov chain over AP and let AP ′ ⊆ AP . Then M satisfies the cascade property with respect to 

AP ′ iff, for every formula x ∈ 2AP ′

, we have 

M ⊨ P

( (P( x | [AP ]) = P( x | [AP ′]) ))

= 1 .

Thus, the cascade property can be checked in polynomial time. It follows that the cascade compatibility of a given Markov chain and formula can also be checked in polynomial time. Hence, Theorem 20 applies directly to systems constructed as cascade products. Moreover, it extends to the more general class of systems that are cascade-compatible with a given formula, and this property can be checked in polynomial time by Theorem 18. 24 Disintegration Temporal Logic for Probabilistic Hyperproperties 

## 6.2 Automata construction 

Let M = ( S, AP, l, P, i ) be a precisely labeled Markov chain, and γ be a qualitative DTL formula defined by the grammar above. We define an alternating Büchi automaton Bγ,M 

recursively on the structure of γ.The automaton is over the alphabet Σ = 2 AP × { 0, ⊥, ⊤} AP × { 0, ⊥} . Intuitively, we interpret an infinite trace over {0, ⊥, ⊤} as an element of Nω . For n ∈ N, a trace that starts with 0n⊥ represents n and a trace that starts 0n⊤ represents ω. In the same way, an infinite trace over {0, ⊥} encodes an element of N.This way, an infinite trace over {0, ⊥, ⊤} AP represents a cut, and an infinite trace over 

{0, ⊥} represents a timestamp. Intuitively, Bγ,M models the evaluation of γ for a given trace, a cut, and a timestamp. Everywhere in this construction σ ∈ Σ, t ∈ 2AP ,⃗v ∈ { 0, ⊥, ⊤} AP , and b ∈ { 0, ⊥} .For a ∈ AP , define Ba,M = ( {q0, q acc , q rej }, Σ, δ a,M , q 0, q acc ), where 

δa,M (qrej , σ ) = qrej , δa,M (qacc , σ ) = qacc ,δa,M (q0, (t,⃗ v, 0)) = q 0,δa,M (q0, (t,⃗ v, ⊥)) = 

{qacc , if a ∈ t, qrej . else. The automaton B¬a,M is defined in the same way, with the only difference that 

δ¬a,M (q0, (t,⃗ v, ⊥)) = 

{qacc , if a / ∈ t, qrej , else. The automata Bγ1∧γ2,M and Bγ1∨γ2,M are constructed as an intersection and union of 

Bγ1,M and Bγ2,M .The automaton B γ,M is defined using the automaton Bγ,M = ( Q, Σ, δ γ , q 0γ , F ) as 

B γ,M = ( Q × { q0, q ⊥}, Σ, δ, q 0γ × { q0}, F × { q0, q ⊥}), where for q ∈ Q, i ∈ { 0, ⊥} ,

δ(( q, q i), (t,⃗ v, b )) = δ γ (q, (t,⃗ v, i )) × { q b}.

The automaton Bγ1 U γ2,M is defined using the automata Bγ1,M = ( Q1, Σ, δ 1, q 01 , F 1) and 

Bγ2,M = ( Q2, Σ, δ 2, q 02 , F 2). We assume that Bγ1,M and Bγ2,M are existential. Otherwise, we turn them into existential using Miyano-Hayashi construction [39]. 

Bγ1 U γ2,M = ((2 Q1 × Q2 × { q0, q U }) ⊔ Q1 ⊔ Q2, Σ, δ, {q01 } × q02 × { q0}, F 1 ⊔ F2). Here, for Q′ 

> 1

⊆ Q1, q1 ∈ Q1 and q2 ∈ Q2 we define 

δ(( Q′

> 1

, q 2, q 0), (t,⃗ v, 0)) = ∨

> q′
> 2∈δ2(q2,(t,⃗ v, 0))

( ⋃

> q1∈Q′
> 1

δ1(q1, (t,⃗ v, 0)) , q ′

> 2

, q 0),δ(( Q′

> 1

, q 2, q 0), (t,⃗ v, ⊥)) = (( ∨ 

> q1∈Q′
> 1;q′
> 1∈δ1(q1,(t,⃗ v, ⊥))

q′

> 1

)∧∧( ∨

> q′
> 2∈δ2(q2,(t,⃗ v, 0))

( ⋃

> q1∈Q′
> 1

δ1(q1, (t,⃗ v, 0)) , q ′

> 2

, q U ))) ∨ ( ∨

> q′
> 2∈δ2(q2,(t,⃗ v, ⊥))

q′

> 2

),δ(( Q′

> 1

, q 2, q U ), (t,⃗ v, b )) = δ(( Q ′

> 1

, q 2, q 0), (t,⃗ v, ⊥)) ,δ(q1, (t,⃗ v, b )) = δ 1(q1, (t,⃗ v, b )) ,δ(q2, (t,⃗ v, b )) = δ 2(q2, (t,⃗ v, b )) .M. Carelli and B. Finkbeiner 25 

The automaton Bγ1 R γ2,M is defined as the complement of B(¬γ1) U(¬γ2),M , since γ1 R γ2 =

¬(( ¬γ1) U(¬γ2)) .We define automata BP(γ|A,B )=1 ,M and BP(γ|A,B )>0,M , using the automaton Bγ,M . First, we convert Bγ,M into a deterministic Rabin automaton Dγ,M . Next, intuitively, we take a product of M = ( S, AP, l, P, i ) and Dγ,M = ( Qγ , Σ, δ γ , q 0γ , Acc γ ), interpreting a cut C

and a timestamp n as an input. Formally, define an MDP M D = ( S × Qγ , {0, ⊥, ⊤} AP ×{0, ⊥} , AP, l M D , R, i M D ), where lM D (s, q ) = l(s), and 

R(( s, q ), (⃗v, b ), (s ′, q ′)) = 

{P (s, s ′), if δγ (q, (l(s),⃗ v, b )) = q ′,

0, otherwise .iM D (s, q ) = 

{i(s), if q = q0γ ,

0, otherwise. We call a set O of states of M D 0-strongly connected if for any o, o ′ ∈ O, there exists a sequence o1, . . . , o k, such that o1 = o, ok = o′, and for any 1 ≤ j < k , oj ∈ O and 

R(oj , 0|AP |+1 , o j+1 ) > 0. A set O is called 0-Bottom strongly connected component (0-BSCC) if it is a maximal 0-strongly connected set, and for any o ∈ O and e / ∈ O, R(o, 0|AP |+1 , e ) = 0 .We call 0-BSCC O of M D accepting , if there exists (Li, K i) ∈ Acc γ , such that O ∩ (S ×

Li) = ∅ and O ∩ (S × Ki)̸ = ∅. Otherwise, we call BSCC O rejecting . Denote the union of all the accepting BSCC as ACC and the union of all the rejecting BSCC as REJ .Finally we define BP(γ|[A],B )=1 ,M = (( S×Qγ ×{ 0, 1, ⊥, ⊤} AP ×{ 0, ⊥} )∪{ qsink }, Σ, δ, q 0, F =1 )

as a universal safety automaton, where 

q0 = {(s, q, 0AP , 0) | iM D (s, q ) > 0}.

To define δ, first we need to define the following. Given ⃗v,⃗ v′ ∈ { 0, 1, ⊥, ⊤} AP and b, b ′ ∈{0, ⊥} define b′′ = 0 , if b = b′ = 0 , otherwise b′′ = ⊥. Moreover, define ⃗ v′′ ∈ { 0, 1, ⊥, ⊤} AP ,such that for every a ∈ AP ,⃗v′′ a =



⊤, if (a ∈ B) ∨ (⃗v a = ⊤) ∨ (( ⃗ v′a = ⊤) ∧ (⃗v a = 0)) , else 

0, if (⃗v a =⃗ v′a = 0) , else 

1, if (( ⃗v a = 1) ∨ (⃗ v′a = ⊥)) ∧ (a ∈ A) ∧ (b′′ = 0) , else 

⊥.

Define ⃗ v′′  

> 0

∈ { 0, ⊥, ⊤} AP as ⃗ v′′ with 1 replaced by 0.For s, s ′ ∈ S, q, q ′ ∈ Q and t ∈ 2AP , such that for every a, the implication ⃗ v′′ a ∈ { 0, ⊤} ⇒ 

l(s)a = ta holds, define 

δ(( s, q,⃗ v, b ), (t,⃗ v′, b ′)) = {(s′, q ′,⃗ v ′′ , b ′′ ) | R(( s, q ), (⃗ v′′  

> 0

, b ′′ ), (s′, q ′)) > 0},

Otherwise, define δ(( s, q,⃗ v, b ), (t,⃗ v′, b ′)) = qsink . Moreover, δ(qsink ) = {qsink }.

F =1 = REJ × {⊥ , ⊤} AP × {⊥} 

Now let us define BP(γ|[A],B )>0,M = (( S ×Qγ ×{ 0, 1, ⊥, ⊤} AP ×{ 0, ⊥} )∪{ qsink }, Σ, δ, q 0, F >0)

as an existential reachability automaton, with q0 and δ defined in the same way as in 

BP(γ|[A],B )=1 ,M , but 

F >0 = ACC × {⊥ , ⊤} AP × {⊥} .26 Disintegration Temporal Logic for Probabilistic Hyperproperties 

## 6.3 Correctness and the algorithm 

▶ Lemma 25. Let n ∈ N be a timestemp, C : AP → Nω be a cut. Let πn ∈ 0n⊥{ 0, ⊥} ω ,and πC ∈ (Π a∈AP ;C(a)<ω 0C(a)⊥{ 0, ⊥, ⊤} ω ) × (Π a∈AP ;C(a)= ω 0∗⊤{ 0, ⊥, ⊤} ω ) be traces. Then for μ-a.e. trace T ∈ (2 AP )ω ,

T, C, n ⊨μ γ iff (T, π C , π n) ∈ L (Bγ,M ).

Proof. We prove the statement by structural induction on γ.

Base case. For γ = a, or γ = ¬a with a ∈ AP , the automaton Bγ,M waits until the first step where the last component of the input equals ⊥ (which occurs at position n by the choice of πn) and then accepts iff a ∈ T (n) (respectively, iff a / ∈ T (n)). Hence, the claim holds (for every T ∈ (2 AP )ω ) by the definition of the semantics. 

Induction step (Boolean operators and ). For γ1 ∧ γ2 or γ1 ∨ γ2 immediately from the induction hypotheses. For γ, by construction of B γ,M , for μ-a.e. T ,

(T, π C , π n) ∈ L (B γ,M ) iff (T, π C , 0πn) ∈ L (Bγ,M ).

Thus, the claim follows from the induction hypothesis for γ.

Induction step ( U and R). Consider γ1 U γ2 and assume that Bγ1,M and Bγ2,M are existential automata. For i ∈ N, denote as πn+i, the trace πn with prefix of length n + i replaced by 0n+i−1⊥.By the induction hypothesis and the definition of the semantics, for μ-a.e. T , T, C, n ⊨μ

γ1 U γ2 iff there exists i such that there exists an accepting run of Bγ2,M on (T, π C , π n+i)

and, for every j < i , there exists an accepting run of Bγ1,M on (T, π C , π n+j ). Note that the induction hypothesis works μ-a.e. for individual i and j, but since the number of possible values of i and j is countable, we can conclude that it works μ-a.e. for all i and j.On the other hand, (T, π C , π n) ∈ L (Bγ1 U γ2,M ) iff there exists an accepting run of the constructed automaton on (T, π C , π n). By construction, the run nondeterministically guesses the witness position i at which γ2 should hold, while ensuring that γ1 holds at all earlier positions. This is equivalent to the existence of i satisfying the two conditions above. Hence, the claim follows. The case of R follows from the duality γ1 R γ2 = ¬(( ¬γ1) U(¬γ2)) .

Induction step (Probabilistic operators). Finally, let us prove the induction step for P(γ | [A], B ) = 1 . Note that since Dγ,M is a determinized Bγ,M the induction hypothesis holds for Dγ,M .Recall the definition of C′ in Equation (4). Let πC′ ∈ (Π a∈AP ;C′(a)<ω 0C′(a)⊥{ 0, ⊥, ⊤} ∗0ω )×

(Π a∈AP ;C′(a)= ω 0∗⊤{ 0, ⊥, ⊤} ∗0ω ) and let π′ 

> n

∈ 0n⊥{ 0, ⊥} ∗0ω .Note that (πC′ , π ′

> n

) can be modeled deterministically with a finite set of states, thus 

M D (πC′ , π ′

> n

) can be viewed as a finite state Markov chain. Clearly, M D (πC′ , π ′

> n

) induces the same measure μ on (2 AP )ω as M .Any finite path of M D (πC′ , π ′

> n

), that has a positive probability is uniquely determined by its S coordinate corresponding to a state of M , since Dγ is deterministic and (πC′ , π ′

> n

) is modeled deterministically. Since M is precisely labeled, the labeling is injective on the set of finite paths of M D (πC′ , π ′

> n

) with positive probability. By the Limit Behaviour of Markov Chains [ 8, Theorem 10.27], almost every path of 

M D (πC′ , π ′

> n

) visits every state of some BSCC infinitely often. Note that by the construction of (πC′ , π ′

> n

), BSCCs of M D (πC′ , π ′

> n

) are 0-BSCCs of M D .M. Carelli and B. Finkbeiner 27 

Let E ⊆ (2 AP )ω be the set of traces T1 such that either T1 is not the image, under the labeling function, of a path of M D (πC′ , π ′

> n

) that takes only positive-probability transitions and visits every state of some BSCC infinitely often, or the equivalence 

(T1, C ′, n ⊨μ γ ←→ (T1, π C′ , π ′

> n

) ∈ L (Dγ,M )) 

fails. By the [ 8, Theorem 10.27] and the induction hypothesis, E s the union of two measure-zero sets and therefore itself has measure zero. Thus, ∫

> L(C′)

∫ 

> U(C′)

1E (T ′, t )μT ′ (dt )ˆ μ′

> L(C′)

(dT ′) = μ(E) = 0 .

Denote as H ⊆ (2 AP )ω the set of traces T1, such that 

T1, C ′, n ⊨μ γ

Denote as H′ ⊆ (2 AP )ω the set of traces T1, such that T1 is an image, under the labeling function, of a path that takes only positive-probability transitions and never visits REJ .Hence, 

∫

> L(C′)

| (

∫ 

> U(C′)

1H (T ′, t )μT ′ (dt )) − (

∫ 

> U(C′)

1H′ (T ′, t )μT ′ (dt )) | ˆμL(C′)(dT ′) ≤≤

∫

> L(C′)

∫ 

> U(C′)

| 1H (T ′, t ) − 1H′ (T ′, t ) | μT ′ (dt )ˆ μL(C′)(dT ′) ≤≤

∫

> L(C′)

∫ 

> U(C′)

1E (T ′, t )μT ′ (dt )ˆ μL(C′)(dT ′) = 0 ,

where the first inequality is the triangle inequality and the second inequality follows from the definitions E, H, and H′. Hence, for ˆμL(C′)-a.e. T ′ ∈ L(C′),

∫ 

> U(C′)

1H (T ′, t )μT ′ (dt ) = 

∫ 

> U(C′)

1H′ (T ′, t )μT ′ (dt ).μT ′ (HT ′ ) = μT ′ (H′  

> T′

).

Thus, for μ-a.e. T ∈ (2 AP )ω ,

T, C ′, n ⊨μ P(γ | [A], B ) = 1 iff 

μTL(C′) (t ∈ U (C′) | (TL(C′), t ), C ′, n ⊨μ γ) = 1 iff 

1 = μTL(C′) (HTL(C′) ) = μTL(C′) (H′ 

> TL(C′)

)

where the last equality holds μ-a.e. For N ∈ N, let KN denote a set of traces T1, such that T1 is an image under the labeling function, of a path that takes only positive-probability transitions and visits REJ within the first N steps. Clearly, μTL(C′) (H′(TL(C′))) = 1 iff for every N ∈ N, μTL(C′) (KN (TL(C′))) = 0 .Note that for every N the property KN depends on the first N steps, since the labeling is injective on finite paths with positive probability. Since μ satisfies the cascade property, Corollary 23 implies that for μ-a.e. T ,

μTL(C′) (KNTL(C′) ) = μ(T 1 ∈ KN | T 1

> L(C′)

[N ] = TL(C′)[N ]) 

μ(T 1 ∈ (2 AP )ω | T 1

> L(C′)

[N ] = TL(C′)[N ]) .28 Disintegration Temporal Logic for Probabilistic Hyperproperties 

Thus, μTL(C′) (H′) = 1 iff for any prefix TL(C′)[N ] there exists no finite trace p ∈ (2 AP )N ,such that pL(C′) = TL(C′)[N ], p(2 AP )ω ⊆ KN , and μ(p(2 AP )ω ) > 0.By the construction BP(γ|[A],B )=1 ,M , receives πC and πn and universally checks for the absence of such p, hence the statement follows. The proof for BP(γ|[A],B )>0,M is analagous. ◀

proof of Theorem 20. Let M be a Markov chain and α a qualitative DTL formula. By definition, α is a Boolean combination of properties of the form P(γ) ≤ r. Using the construction described in Section 6.2, we obtain a Büchi automaton Bγ,M and then translate it into a deterministic Rabin automaton Dγ,M for each such γ. We now analyze the complexity of this construction. Translating an alternating Büchi automaton to a deterministic Rabin automaton incurs a doubly-exponential blow-up [ 42 ]. In our construction, this translation is triggered at every alternation between probabilistic operators and temporal operators. Intuitively, each such alternation introduces an additional exponent. The complementation of alternating Büchi automata (used to express R) causes a quadratic blow-up [36]. We next argue that determinization can be avoided when passing from one existential probabilistic operator to another existential probabilistic operator. Let Bγ be an existential reachability automaton. The formula P(γ | [A], B ) > 0 requires that the property expressed by Bγ holds with positive probability. Equivalently, there exists a finite path of nonzero probability along which Bγ has an accepting run reaching an accepting state. Hence, the existential choices can be merged, and no additional determinization is needed. The argument for the universal-to-universal case is symmetric. Consequently, the size of Dγ is bounded by g(k+1, |α| + |M |) for an appropriate tower function g, where the additional exponent accounts for the required determinism. Given Dγ , the probability μ(L(Dγ )) can be computed in time polynomial in |Dγ | and 

|M | [ 8]. It then remains to evaluate a Boolean combination of linear inequalities, which can be done in time linear in the size of the Boolean formula. 

◀

## 7 Conclusion 

We have introduced Disintegration Temporal Logic (DTL) for probabilistic hyperproperties. DTL reasons about probabilities conditioned on (possibly infinite) sequences of events using the notion of measure disintegration from probability theory. This allows the logic to express conditional independence and, consequently, hyperproperties like probabilistic non-interference and perfect indistinguishability. These hyperproperties belong to the linear fragment of DTL, which is decidable in polynomial time. Moreover, we demonstrated applications of DTL to several classes of probabilistic systems. These include stochastic environments interacting with probabilistic systems, distributional properties of Markov decision processes, and probabilistic automata on infinite words. In each case, DTL reasons about conditional probability distributions induced by finite or infinite observations. For distributional properties and probabilistic automata, DTL replaces strict quantification over blind schedulers with “soft” probabilistic reasoning, yielding decidability in certain settings. Some of these properties belong to the qualitative fragment , for which we presented a model-checking algorithm. M. Carelli and B. Finkbeiner 29 

References 

1 Erika Abraham and Borzoo Bonakdarpour. HyperPCTL: A temporal logic for probabilistic hyperproperties, 2018. URL: https://arxiv.org/abs/1804.01853 , arXiv:1804.01853 .

2 S. Akshay, Krishnendu Chatterjee, Tobias Meggendorfer, and Ðorđe Žikelić. Certified policy verification and synthesis for mdps under distributional reach-avoidance properties, 2024. URL: 

https://arxiv.org/abs/2405.04015 , arXiv:2405.04015 .

3 S. Akshay, Krishnendu Chatterjee, Tobias Meggendorfer, and Ðorđe Žikelić. MDPs as distribution transformers: Affine invariant synthesis for safety objectives. In Computer Aided Verification: 35th International Conference, CAV 2023, Paris, France, July 17–22, 2023, Proceedings, Part III , page 86–112, Berlin, Heidelberg, 2023. Springer-Verlag. doi: 10.1007/978-3-031-37709-9_5 .

4 S. Akshay, Blaise Genest, and Nikhil Vyas. Distribution-based objectives for markov decision processes. Proceedings of the 33rd Annual ACM/IEEE Symposium on Logic in Computer Science , 2018. URL: https://api.semanticscholar.org/CorpusID:13743458 .

5 Miguel E. Andrés and Peter van Rossum. Conditional probabilities over probabilistic and nondeterministic systems. In C. R. Ramakrishnan and Jakob Rehof, editors, Tools and Algorithms for the Construction and Analysis of Systems, 14th International Conference, TACAS 2008, Held as Part of the Joint European Conferences on Theory and Practice of Software, ETAPS 2008, Budapest, Hungary, March 29-April 6, 2008. Proceedings , volume 4963 of Lecture Notes in Computer Science , pages 157–172. Springer, 2008. doi:10.1007/ 978-3-540-78800-3\_12 .

6 Martin Avanzini, Gilles Barthe, Davide Davoli, and Benjamin Grégoire. A quantitative probabilistic relational hoare logic, 2025. URL: https://arxiv.org/abs/2407.17127 , arXiv: 2407.17127 .

7 Christel Baier, Nathalie Bertrand, and Marcus Größer. Probabilistic automata over infinite words: Expressiveness, efficiency, and decidability. volume 3, pages 3–16, 07 2009. doi: 10.4204/EPTCS.3.1 .

8 Christel Baier and Joost-Pieter Katoen. Principles of model checking . MIT Press, 2008. 

9 Christel Baier, Joachim Klein, Sascha Klüppelholz, and Steffen Märcker. Computing conditional probabilities in markovian models efficiently. In Erika Ábrahám and Klaus Havelund, editors, 

Tools and Algorithms for the Construction and Analysis of Systems - 20th International Conference, TACAS 2014, Held as Part of the European Joint Conferences on Theory and Practice of Software, ETAPS 2014, Grenoble, France, April 5-13, 2014. Proceedings , volume 8413 of Lecture Notes in Computer Science , pages 515–530. Springer, 2014. doi:10.1007/ 978-3-642-54862-8\_43 .

10 Jialu Bao, Emanuele D’Osualdo, and Azadeh Farzan. Bluebell: An alliance of relational lifting and independence for probabilistic reasoning. Proceedings of the ACM on Programming Languages , 9(POPL):1719–1749, January 2025. URL: http://dx.doi.org/10.1145/3704894 ,

doi:10.1145/3704894 .

11 Gilles Barthe, Benjamin Grégoire, and Santiago Zanella Béguelin. Formal certification of code-based cryptographic proofs. SIGPLAN Not. , 44(1):90–101, January 2009. doi: 10.1145/1594834.1480894 .

12 Gilles Barthe, Justin Hsu, and Kevin Liao. A probabilistic separation logic. Proc. ACM Program. Lang. , 4(POPL), December 2019. doi:10.1145/3371123 .

13 R. Baumann. Radiation-induced soft errors in advanced semiconductor technologies. IEEE Transactions on Device and Materials Reliability , 5:305–316, 10 2005. doi:10.1109/TDMR. 2005.853449 .

14 Joseph T. Chang and David Pollard. Conditioning as disintegration. Statistica Neerlandica ,51, 1997. URL: https://api.semanticscholar.org/CorpusID:16749932 .

15 Krishnendu Chatterjee and Mathieu Tracol. Decidable problems for probabilistic automata on infinite words. 2012 27th Annual IEEE Symposium on Logic in Computer Science , pages 185–194, 2011. URL: https://api.semanticscholar.org/CorpusID:14146404 .30 Disintegration Temporal Logic for Probabilistic Hyperproperties 

16 Michael R. Clarkson, Bernd Finkbeiner, Masoud Koleini, Kristopher K. Micinski, Markus N. Rabe, and César Sánchez. Temporal logics for hyperproperties. In Martín Abadi and Steve Kremer, editors, Principles of Security and Trust - Third International Conference, POST 2014, Grenoble, France, April 5-13, 2014, Proceedings , volume 8414 of Lecture Notes in Computer Science , pages 265–284. Springer, 2014. URL: https://doi.org/10.1007/ 978-3-642-54792-8_15 .

17 Michael R. Clarkson, Bernd Finkbeiner, Masoud Koleini, Kristopher K. Micinski, Markus N. Rabe, and César Sánchez. Temporal logics for hyperproperties, 2014. URL: https://arxiv. org/abs/1401.4492 , arXiv:1401.4492 .

18 Michael R. Clarkson and Fred B. Schneider. Hyperproperties. In 2008 21st IEEE Computer Security Foundations Symposium , pages 51–65, 2008. doi:10.1109/CSF.2008.7 .

19 Michael R. Clarkson and Fred B. Schneider. Hyperproperties. J. Comput. Secur. , 18(6):1157– 1210, 2010. URL: https://doi.org/10.3233/JCS-2009-0393 .

20 Arthur Correnson and Bernd Finkbeiner. Coinductive proofs for temporal hyperliveness. Proc. ACM Program. Lang. , 9(POPL), January 2025. doi:10.1145/3704889 .

21 Oswaldo Costa, Marcelo Fragoso, and Ricardo Marques. Discrete-time markov jump linear systems. 01 2005. doi:10.1007/b138575 .

22 Costas Courcoubetis and Mihalis Yannakakis. The complexity of probabilistic verification. J. ACM , 42(4):857–907, July 1995. doi:10.1145/210332.210339 .

23 Rayna Dimitrova, Bernd Finkbeiner, and Hazem Torfah. Probabilistic hyperproperties of markov decision processes, 2020. URL: https://arxiv.org/abs/2005.03362 , arXiv:2005. 03362 .

24 E. O. Elliott. Estimates of error rates for codes on burst-noise channels. The Bell System Technical Journal , 42(5):1977–1997, 1963. doi:10.1002/j.1538-7305.1963.tb00955.x .

25 Bernd Finkbeiner. Logics and algorithms for hyperproperties. ACM SIGLOG News , 10(2):4–23, 2023. doi:10.1145/3610392.3610394 .

26 E. N. Gilbert. Capacity of a burst-noise channel. The Bell System Technical Journal ,39(5):1253–1265, 1960. doi:10.1002/j.1538-7305.1960.tb03959.x .

27 J. A. Goguen and J. Meseguer. Security policies and security models. In 1982 IEEE Symposium on Security and Privacy , pages 11–11, 1982. doi:10.1109/SP.1982.10014 .

28 Carlos Gouvea and Carlos Pedroso. Mac-layer packet loss models for wi-fi networks: A survey. 

IEEE Access , 7:180512–180531, 12 2019. doi:10.1109/ACCESS.2019.2958260 .

29 James W. Gray. Toward a mathematical foundation for information flow security. J. Comput. Secur. , 1(3–4):255–294, May 1992. 

30 J.W. Gray. Probabilistic interference. In Proceedings. 1990 IEEE Computer Society Symposium on Research in Security and Privacy , pages 170–179, 1990. doi:10.1109/RISP.1990.63848 .

31 J.W. Gray. Toward a mathematical foundation for information flow security. In Proceedings. 1991 IEEE Computer Society Symposium on Research in Security and Privacy , pages 21–34, 1991. doi:10.1109/RISP.1991.130769 .

32 Olav Kallenberg. Foundations of Modern Probability . 01 2021. doi:10.1007/ 978-3-030-61871-1 .

33 Jonathan Katz and Yehuda Lindell. Introduction to modern cryptography. 2020. URL: 

https://api.semanticscholar.org/CorpusID:9506320 .

34 A. Kolomogoroff and Z. Mathematiker. Grundbegriffe der Wahrscheinlichkeitsrechnung . Ergeb-nisse der Mathematik und Ihrer Grenzgebiete. 1. Folge. Springer Berlin Heidelberg, 2013. URL: https://books.google.de/books?id=KeObBgAAQBAJ .

35 Vijay Anand Korthikanti, Mahesh Viswanathan, Gul Agha, and YoungMin Kwon. Reasoning about mdps as transformers of probability distributions. In 2010 Seventh International Conference on the Quantitative Evaluation of Systems , pages 199–208, 2010. doi:10.1109/ QEST.2010.35 .

36 Orna Kupferman and Moshe Y. Vardi. Weak alternating automata are not that weak. ACM Trans. Comput. Logic , 2(3):408–429, July 2001. doi:10.1145/377978.377993 .M. Carelli and B. Finkbeiner 31 

37 John M. Li, Amal Ahmed, and Steven Holtzen. Lilac: A modal separation logic for conditional probability. Proc. ACM Program. Lang. , 7(PLDI), June 2023. doi:10.1145/3591226 .

38 Natasa Miskov-Zivanov and Diana Marculescu. Modeling and optimization for soft-error reliability of sequential circuits. IEEE Transactions on Computer-Aided Design of Integrated Circuits and Systems , 27(5):803–816, 2008. doi:10.1109/TCAD.2008.917591 .

39 Satoru Miyano and Takeshi Hayashi. Alternating finite automata on omega-words. Theor. Com-put. Sci. , 32:321–330, 1984. URL: https://api.semanticscholar.org/CorpusID:6926486 .

40 Dominique Perrin and Jean-Éric Pin. Infinite words - automata, semigroups, logic and games. In Pure and applied mathematics series , 2004. URL: https://api.semanticscholar.org/ CorpusID:117937381 .

41 K. B. Petersen and M. S. Pedersen. The matrix cookbook, nov 2012. Version 20121115. URL: 

http://www2.compute.dtu.dk/pubdb/pubs/3274-full.html .

42 Nir Piterman. From nondeterministic büchi and streett automata to deterministic parity automata. Logical Methods in Computer Science , Volume 3, Issue 3, August 2007. URL: 

http://dx.doi.org/10.2168/LMCS-3(3:5)2007 , doi:10.2168/lmcs-3(3:5)2007 .

43 Michael O. Rabin. Probabilistic automata. Information and Control , 6(3):230–245, 1963. URL: https://www.sciencedirect.com/science/article/pii/S0019995863902900 ,

doi:10.1016/S0019-9958(63)90290-0 .

44 V. A. Rokhlin. On the fundamental ideas of measure theory. Matematicheskii Sbornik ,25(1):107–150, 1949. New Series, vol. 67. URL: http://mi.mathnet.ru/eng/sm5995 .

45 C. E. Shannon. Communication theory of secrecy systems. The Bell System Technical Journal ,28(4):656–715, 1949. doi:10.1002/j.1538-7305.1949.tb00928.x .

46 G. Smith. Probabilistic noninterference through weak probabilistic bisimulation. In 16th IEEE Computer Security Foundations Workshop, 2003. Proceedings. , pages 3–13, 2003. doi: 10.1109/CSFW.2003.1212701 .

47 Terrence Tao. An introduction to measure theory. 2011. URL: https://api.semanticscholar. org/CorpusID:117492913 .

48 Wen-Guey Tzeng. A polynomial-time algorithm for the equivalence of probabilistic auto-mata. SIAM Journal on Computing , 21(2):216–227, 1992. arXiv:https://doi.org/10.1137/ 0221017 , doi:10.1137/0221017 .

49 Yu Wang, Siddhartha Nalluri, Borzoo Bonakdarpour, and Miroslav Pajic. Statistical model checking for hyperproperties, 2020. URL: https://arxiv.org/abs/1902.04111 , arXiv:1902. 04111 .