---
title: "Hamm-Grams: An Algorithm for Mining Regular Expressions of Bytes"
title_zh: Hamm-Grams：一种挖掘字节正则表达式的算法
authors: "Derek Everett, Edward Raff, James Holt"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.01445v1"
tags: ["query:sys-security"]
score: 9.0
evidence: 基于正则表达式的恶意软件静态特征提取
tldr: 恶意软件检测是网络安全的关键挑战，传统基于字节n-gram的静态特征虽有效但脆弱，攻击者通过微调字节即可轻易规避。本文提出hamm-grams，一种固定长度且包含单字符通配符的正则表达式特征，更鲁棒地捕获字节模式。我们设计了一种新的局部敏感哈希函数，使汉明距离小的字节对碰撞，并在哈希桶内聚类以确定通配符位置，从而高效挖掘常见hamm-grams。在恶意软件分类和检测任务上的实验表明，hamm-grams特征显著优于传统n-gram，提升了模型的准确率与抗规避能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01445v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 826, \"height\": 720, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01445v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1477, \"height\": 454, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01445v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1505, \"height\": 478, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01445v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1130, \"height\": 733, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.01445v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1566, \"height\": 1370, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01445v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 767, \"height\": 263, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01445v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 768, \"height\": 260, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01445v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 767, \"height\": 264, \"label\": \"Table\"}]"
motivation: 传统n-gram特征在恶意软件静态分析中脆弱，易被字节微调绕过，亟需更鲁棒的字节级特征。
method: 提出hamm-grams（固定长度带单字符通配符的正则表达式），并设计新型局部敏感哈希结合桶内聚类的高效挖掘算法。
result: 在恶意软件分类与检测任务中，hamm-grams特征相比传统n-gram展现了更优的准确性和鲁棒性。
conclusion: hamm-grams为字节级恶意软件分析提供了一种有效且鲁棒的特征挖掘方法，有助于增强检测系统的抗规避能力。
---

## 摘要
恶意软件构成了一个关键且不断演变的威胁，因此鲁棒有效的恶意软件检测与分类系统至关重要。$n$-gram特征是用于恶意软件有效机器学习系统的常见静态特征之一，但这些特征本质上是脆弱的。我们提出了一种用于构建更鲁棒特征的算法，即hamm-gram，这是一类具有固定长度和单字符通配符的特殊正则表达式。我们设计了一种高效算法来发现常见的hamm-gram，该方法使用一种新的局部敏感哈希，旨在在小汉明距离对之间产生碰撞，并在哈希桶内进行聚类以放置通配符。随后，我们展示了这些特征在恶意软件分类与检测任务中的优势。

## Abstract
Malware poses a critical and ever-evolving threat, and robust and effective systems for detecting and classifying malware are of essential importance. $n$-grams features are among the common static features used in effective machine learning systems for malware, but these features are inherently brittle. We propose an algorithm for constructing more robust features, hamm-grams, which are a special class of regular expressions having a fixed length and single-character wildcards. We devise an efficient algorithm for finding common hamm-grams using a new locality-sensitive hash designed to produce collisions among pairs of small Hamming distance and a clustering within hash buckets to place wildcards. We then demonstrate the advantages of these features in malware classification and detection tasks.

---

## 论文详细总结（自动生成）

好的，请看以下对论文《Hamm-Grams: An Algorithm for Mining Regular Expressions of Bytes》的结构化总结。

### 1. 论文的核心问题与整体含义

*   **核心问题**：在恶意软件静态检测中，`n`-gram（字节序列）是一种常用且高效的机器学习特征。然而，`n`-gram特征极其脆弱，单个字节的微小改变（如汇编指令的等价替换）就会导致该特征完全失效，这严重限制了模型的鲁棒性，尤其是在需要更大`n`值以捕获更长上下文时。
*   **整体含义**：本文旨在解决传统`n`-gram特征脆弱性。其核心思想是提出一种名为“hamm-grams”的新型特征，它本质上是固定长度、允许单字符通配符（`?`）的正则表达式。例如，`55 ? D2 89 E5`能同时匹配 `55 29 D2 89 E5` 和 `55 31 D2 89 E5`，从而提供对抗字节级微调的鲁棒性。文章的工作重点在于设计一个计算上可行的算法，从海量数据中高效挖掘出此类有意义的hamm-grams。

### 2. 论文提出的方法论

*   **核心思想**：将问题转化为如何快速找到海量`n`-gram中相互汉明距离小的组，然后在这些组内通过聚类生成带通配符的公共模式（即hamm-grams）。
*   **关键技术细节**：
    *   **复杂局部敏感哈希（Complex LSH）**：为高效找到汉明距离相近的`n`-gram，论文设计了一种新颖的哈希函数。
        *   **令牌化**：将字节序列中的每个字节映射到复平面单位圆上的一个唯一点（模长为1的复数）。
        *   **哈希计算**：对一个`n`-gram，将其各位置令牌的复数表示乘以一个位置相关的权重向量`w`（元素为`e^(i*r*φ)`，`φ`从`[0, 2π]`均匀随机采样），然后求和得到一个复数值。
        *   **二值化与滚动更新**：取该复数值实部的符号位作为1比特哈希码。该哈希具有滚动特性，即相邻窗口的哈希值可通过`O(1)`时间计算（递归公式：`h_u = a^-1 * h_p - t_j + a^n * t_{j+n}`），这保证了高效性。
        *   **核心性质**：理论分析和实验证明，两个`n`-gram的哈希码碰撞概率是其汉明距离的函数。汉明距离越小，碰撞概率越高，这使得相似序列能被聚集到同一哈希桶中。
*   **算法流程**：
    1.  **第一遍扫描（Top-k哈希）**：对整个语料库中的所有`n`-gram应用上述滚动LSH，统计每个哈希桶（由多比特哈希码构成）的出现频率，使用快速选择选出前`k`个高频哈希桶。
    2.  **第二遍扫描（收集候选项）**：再次扫描语料库，将所有落入前`k`个高频桶中的`n`-gram全部收集到对应的桶中。
    3.  **桶内聚类（生成Hamm-grams）**：对每个桶内的`n`-gram集合，采用自底向上的凝聚聚类。合并准则是：两个表达式（可以是`n`-gram或临时hamm-gram）能通过加入不超过预算数量的通配符而变得一致。每次选择汉明距离最小的两个表达式合并，直至无法合并为止，最终生成一系列hamm-grams。
    4.  **第三遍扫描（最终排序）**：构建一个可高效匹配的前缀树（Trie，扩展支持通配符），再次扫描语料库，统计每个生成的hamm-gram在原始数据中的真实匹配次数，最后按匹配次数排序，保留前`k`个hamm-grams作为最终特征。

### 3. 实验设计

*   **数据集**：实验在三个不同类型的恶意软件数据集上进行，以验证方法的通用性。
    *   **Drebin**：Android恶意软件家族分类数据集（20类，4661个样本）。
    *   **EMBER 2018**：Windows PE恶意软件检测（良性/恶意）数据集（60万训练样本，20万测试样本，类别平衡）。
    *   **PDF数据集**：从VirusTotal抓取的恶意PDF检测数据集（10万训练样本，1万测试样本）。
*   **对比方法与基准**：
    *   **主要对比**：与使用传统`n`-grams作为特征、搭配相同线性分类器（`L1`-正则逻辑回归）的模型进行对比。
    *   **特征数量**：对比了在保留前`k=10^4`和`k=10^5`（Emeber）或`k=500`和`k=5000`（Drebin）个特征时的性能。
    *   **相似性哈希方法对比（Drebin任务）**：与ssdeep、sdhash和LZJD等基于相似性哈希的故障信息相关方法进行了比较。
*   **评估指标**：主要使用平衡准确率（Drebin）和标准化部分AUC（pAUC，专注于低误报率区域，如FPR<0.001-0.01，用于EMBER与PDF），以准确评估在可接受的低误报条件下的检测能力。

### 4. 资源与算力

*   **本文未明确提及**任何实验所使用的硬件配置、GPU型号/数量或具体的训练时长。论文聚焦于算法设计与有效性验证，其训练和推理成本声称很低（模型为线性分类器），适用于资源受限环境，但未给出实验部分的算力细节。

### 5. 实验数量与充分性

*   **实验组数**：
    *   **主实验**：在3个不同平台的数据集上进行了分类/检测性能比较，每组实验内又对比了不同`n`值和不同特征数量`k`的组合。
    *   **消融/辅助实验**：
        *   LSH性质实验：通过模拟实验验证了哈希碰撞概率与汉明距离的关系。
        *   Drebin任务上还增加了与三种相似性哈希方法和k-NN分类的对比。
        *   对Drebin任务进行了特征分析的定性实验，展示了hamm-grams汇聚冗余特征与发现高级模式的能力。
*   **充分性与公平性**：
    *   **充分性**：实验设计较为全面，覆盖了安卓、Windows PE、PDF三类主流恶意软件，并同时考虑了多分类家族识别和良恶性检测两种任务，结论有较好的泛化支撑。
    *   **客观公平性**：对比基准明确（传统`n`-gram），所用数据集均为公开标准数据集，模型和评估指标（特别是低误报下的pAUC）也是安全领域公认的。每种实验结果都通过多次随机实验（如Drebin任务10次）给出了标准差，增加了可信度。

### 6. 论文的主要结论与发现

*   **性能优势显著**：
    *   **特征数受限时优势巨大**：在仅保留少量特征（如Drebin任务中`k=500`）时，hamm-grams的准确率远超`n`-grams（例如`n=8`时，hamm-grams准确率>80%，而`n`-grams接近随机猜测）。
    *   **高特征数时仍具竞争力**：在保留更多特征（如`k=10^5`）的EMBER和PDF大型检测任务上，长度为8的hamm-grams在所有模型中取得了最好的检测性能（pAUC最高），且长度16的hamm-grams性能也基本平或优于最好的`n`-gram模型。
*   **特征质量更高**：
    *   **解决冗余**：Hamm-grams能自动将因单一字节变化而产生的多个语义等价的`n`-gram合并为一个特征，释放特征容量，让模型有机会学习到更多样、更重要的其他模式。
    *   **发现新模式**：Hamm-grams凭借通配符能发现传统`n`-grams无法捕获的有意义模式（如检测到恶意软件利用特定命名规律的字体文件作为攻击载体）。

### 7. 优点

*   **方法创新性强**：设计了一种新颖的、在汉明距离上具有局部敏感性的滚动哈希函数，理论分析与实验验证完备，为解决大规模序列的近似匹配问题提供了新工具。
*   **算法效率高**：哈希计算支持`O(1)`时间滚动，结合近似分桶和凝聚聚类，能处理包含千万级`n`-grams的大规模语料，在计算上具有可行性。
*   **解决问题扎实**：精准针对`n`-gram对微小扰动敏感的已知痛点，提出的hamm-grams特征在保持线性模型高效、可解释等优点的同时，显著提升了鲁棒性和准确性。
*   **评估全面且贴近实战**：实验涵盖多平台、多任务（检测与分类），使用关注低误报率的实战评估指标，并进行了与工业界相似性哈希工具的对比，论证充分。
*   **强解释性**：Hamm-grams作为带通配符的字节串，本身具有极强的可解释性，安全分析师可以直接阅读这些特征来理解恶意行为。

### 8. 不足与局限

*   **算力开销未量化**：尽管算法设计高效，但论文完全没有提及在三遍扫描和处理海量数据时所需的实际内存和计算时间，这使得在实际环境中估算部署成本变得困难。
*   **通配符预算的敏感性**：Hamm-grams的质量依赖于通配符预算（最大允许`K`个通配符）的设置。论文未深入探讨如何最优选择`K`值，以及模型性能对不同`K`值的敏感度。
*   **对反分析的脆弱性未讨论**：虽然hamm-grams能抵抗字节的微调，但它可能仍能被针对性更强的攻击绕过（例如，同时改变多个字节使其超出通配符预算），论文未对此类高级对抗鲁棒性进行分析。
*   **与深度学习特征缺乏比较**：实验仅对比了作为线性模型输入的`n`-grams，未与基于深度学习的端到端模型或结合`n`-grams的深度学习方法进行比较，不足以说明hamm-grams在所有特征体系中的定位。
*   **聚类阶段效率的确定性**：桶内自底向上的凝聚聚类最坏情况复杂度较高，尽管论文提到通过立即合并距离为零的对等优化手段在实践中有更好的表现，但其在最坏情况下的可扩展性依然是一个潜在风险。

（完）
