---
title: "Buzz to Boom: Detecting Message Progression Vulnerabilities in Electron Applications via Segmented Directed Fuzzing"
title_zh: 从蜂鸣到爆裂：通过分段定向模糊测试检测 Electron 应用中的消息递进漏洞
authors: "Jianjia Yu, Zhengyu Liu, Ziyang Li, Yu Sun, Yinzhi Cao"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.20698v1"
tags: ["query:sys-security"]
score: 9.0
evidence: 通过分段定向模糊测试检测Electron应用中的多步消息传递漏洞，实现利用防范
tldr: Electron应用跨进程消息传递可被利用构造多步攻击链（MPV），但传统方法无法检测。Proton框架将端到端模糊测试分解为按消息边界的分段定向测试，各段分别引导抵达危险函数或传播攻击载荷至下一进程，之后合成崩溃输入验证端到端利用。在589款真实应用测试中，发现23个零日MPV，其中22个导致系统命令执行，包括星标超5万的热门项目，已获11个CVE编号及漏洞赏金。该方法显著提升了跨进程漏洞的自动化发掘能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.20698v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1844, \"height\": 439, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20698v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1835, \"height\": 1183, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20698v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 873, \"height\": 484, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20698v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 885, \"height\": 765, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.20698v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1816, \"height\": 870, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.20698v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 924, \"height\": 455, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.20698v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1853, \"height\": 667, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.20698v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 889, \"height\": 303, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.20698v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 867, \"height\": 931, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.20698v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 840, \"height\": 257, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.20698v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 876, \"height\": 551, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.20698v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1632, \"height\": 431, \"label\": \"Table\"}]"
motivation: 现有Electron应用安全研究多关注配置缺陷与恶意DOM，无法检测需要跨进程消息传递的多步利用漏洞（MPV）。
method: Proton将端到端模糊测试分解为按消息边界的进程分段，每段目标为抵达sink或传播载荷至下一进程，并合成崩溃输入验证利用链。
result: 在589款真实Electron应用中检出23个零日MPV，其中22个可导致OS命令执行，已获11个CVE和漏洞赏金。
conclusion: Proton显著提升了对跨进程消息传递漏洞的自动化检测能力，并在真实应用中验证了较高的漏洞危害与影响。
---

## 摘要
Electron 是一个使用 Web 技术构建跨平台桌面应用的流行框架。这类应用由多个具有不同特权级别的进程组成，并通过消息传递进行通信。当进程间消息携带攻击者控制的输入时，它们可以跨进程传播并到达特权 API，例如命令执行。这种消息传播行为被描述为消息递进漏洞 (MPV)。MPV 的利用具有挑战性，因为它通常需要多个步骤，例如，首先通过消息传递在一个进程中实现任意代码执行，然后在另一个进程中使用在第一个进程中构造的另一消息进行命令注入。据我们所知，现有的 Electron 安全工作仅研究不安全配置和恶意文档对象模型 (DOM) 内容，即它们无法检测或利用这些需要通过消息传递进行的复杂跨进程利用来触发的漏洞。我们提出 Proton，一个用于检测 MPV 的分段定向模糊测试框架。我们的关键见解是将端到端的模糊测试沿着消息传递边界分解为每个进程的片段，其中每个片段的模糊测试目标要么是：(i) 到达当前进程中的汇点，要么是 (ii) 将有效载荷传播到下一个进程，以实现对另一个进程的探索。在第二种情况下，消息将作为下一个片段语料库的种子。最后，Proton 从每个进程合成崩溃输入以验证端到端利用。我们针对 589 个真实世界的 Electron 应用评估了 Proton，发现了 23 个零日 MPV。其中 22 个导致操作系统命令执行，涉及 GitHub 星标超过 5 万的项目。我们负责任地披露了所有发现。迄今为止，我们已收到 13 个致谢、11 个修复和 11 个 CVE，包括来自 Vercel 的漏洞赏金。

## Abstract
Electron is a popular framework for building cross-platform desktop applications using web technologies. Such applications consist of multiple processes with different privilege levels that communicate via message passing. When inter-process messages carry attacker-controlled inputs, they can propagate across processes and reach privileged APIs, e.g., command execution. Such a message propagation behavior is characterized as Message Progression Vulnerabilities (MPVs). The exploitation of MPVs is challenging because it often requires multiple steps, e.g., first arbitrary code execution in one process via message passing, and then command injection in another process using another message crafted in the first process. To our knowledge, existing works on Electron security only study unsafe configurations and malicious Document Object Model (DOM) content, i.e., they cannot detect or exploit these vulnerabilities that need to be triggered by complex cross-process exploits via message passing. We present Proton, a segmented directed fuzzing framework for detecting MPVs. Our key insight is to decompose end-to-end fuzzing into per-process segments along message-passing boundaries, where the goals of fuzzing each segment are either: (i) reaching a sink in the current process or (ii) propagating the payload to the next process, to enable the exploration of another process. In the second case, the messages seed the corpus of the next segment. Finally, Proton synthesizes crash inputs from each process to validate end-to-end exploits. We evaluate Proton against 589 real-world Electron applications, resulting in 23 zero-day MPVs. Among them, 22 lead to OS command execution, including projects with over 50k GitHub stars. We responsibly disclosed all findings. To date, we have received 13 acknowledgments, 11 fixes, and 11 CVEs, including a bug bounty from Vercel.

---

## 论文详细总结（自动生成）

好的，以下是对该论文的结构化总结。

### 1. 论文的核心问题与整体含义

-   **研究背景**：Electron 是一个广泛使用的跨平台桌面应用开发框架（如 VS Code、Slack、Discord 均基于此）。其架构包含拥有不同权限的多个进程（主进程、渲染进程、工具进程），进程间通过消息传递（IPC）进行通信。
-   **核心问题**：论文定义了一种新型安全漏洞——**消息递进漏洞 (MPV)**。此类漏洞的特点是，攻击者控制的输入可利用多步消息传递链，在不同进程间“递进”，最终到达高权限的危险API（如命令执行）。
-   **研究动机**：现有的 Electron 安全研究仅关注单一进程内的不安全配置或恶意 DOM 内容，无法检测或利用这种需要通过复杂跨进程消息传递才能触发的多步利用链（MPV）。
-   **整体含义**：论文提出并验证了一种全新的自动化方法（Proton），能够系统性地检测真实 Electron 应用中此前未被发现的、高危害的 MPV 漏洞。

### 2. 论文提出的方法论

-   **核心思想**：**分段定向模糊测试**。将跨越多个进程的端到端利用路径，在消息传递边界（即“递进API”）处分解为独立的片段，然后对每个片段进行独立的、目标导向的模糊测试。

-   **关键技术细节与流程**：
    1.  **段间分解与定义**：
        -   **递进API**：能接收攻击者控制数据，并将其转发至新执行上下文的 API（如 `ipcRenderer.send`、`fetch`、`win.loadURL` 等）。
        -   **片段分割**：一条完整的利用链 `π` 被分解为一系列片段 `S1, S2, ..., Sk`。每个片段的目标是：**a)** 抵达当前进程内的终端汇点（如命令执行），或 **b)** 抵达一个递进API，并将生成的有效载荷传递给下一片段，作为其模糊测试的种子。

    2.  **两阶段检测框架**：
        -   **阶段一：基于LLM的静态递进分析**。利用大型语言模型（Claude Opus 4.7）智能体，对应用源代码进行污点风格的分析，识别潜在的漏洞路径（从外部入口到危险汇点），并自动生成：
            -   路径的段分解信息。
            -   每个片段的模糊测试**驱动程序（Harness）**（可直接注入源码的补丁文件）。
            -   高质量的初始**种子语料库**。
        -   **阶段二：分段模糊测试**。利用覆盖率引导的模糊测试引擎（基于 `jazzer.js` 及 `libfuzzer` 扩展）对每个片段独立进行模糊测试，同时引入了两种高效的反馈机制：
            -   **输入重构器**：对于可逆的解析操作（如 `JSON.parse`），模糊测试器可直接测试解析后的结构化数据，再由重构器逆向生成符合格式的原始输入，极大提升了模糊测试效率。
            -   **语法与金丝雀预言机**：通过检测API的解析错误（语法预言机）或在输入中嵌入特殊标记并监控其流向（金丝雀预言机）来判断是否成功触发目标汇点。

    3.  **算法流程简析**：
        **算法1：分段模糊测试工作流**
        1.  遍历所有片段。
        2.  若片段是完全可逆的，则跳过模糊测试，后续通过输入重构器生成输入。
        3.  若不可逆，则植入该片段专用的驱动和预言机，并将其前序片段的输出作为输入。
        4.  对该片段执行模糊测试，寻找能触发其目标汇点（终端或递进API）的输入。
        5.  若未找到有效输入，则终止整个利用链的探索。
        6.  若找到，且前一跳片段是可逆的，则重构其输入。
        7.  将所有已验证的片段及输入组合成完整利用链。
        8.  进行端到端验证，合成最终攻击载荷并在完整应用中测试其有效性。

### 3. 实验设计

-   **数据集**：从 GitHub 收集了 **589个** 真实世界的开源 Electron 应用。筛选条件为：`package.json` 中包含 `electron` 依赖、星数超过100、且代码库中包含自定义 URI 处理相关关键词 ( `open-url` 或 `second-instance` )。
-   **基准对比 (Baseline)**：由于无现有工具可比，作者基于 `jazzer.js` 实现了一个**端到端（End-to-End）模糊测试器**作为对比基线。
-   **对比方法与消融实验**：
    -   **RQ5 (与基准对比)**：将 Proton 的分段模糊测试与端到端基准模糊测试在零日漏洞发现数量和边缘覆盖率上进行对比。
    -   **RQ4 (LLM组件消融)**：分析 LLM 生成的驱动和种子对 Proton 性能的贡献，通过将其与**基于模板的驱动**和**随机种子**进行替换对比。

### 4. 资源与算力

-   **计算平台**：所有实验在一台配备 **Apple M4 Pro 处理器** 和 **24 GB 内存** 的 Mac 工作站上完成，运行 macOS 15.7.2。
-   **未使用** GPU 进行大规模训练或推理，实验以在 CPU 上进行模糊测试和 LLM API 调用为主。
-   **LLM调用**：静态分析阶段使用了 `claude-opus-4-7` 模型。平均每个应用的分析时间为 2.42 分钟，消耗 12.6 万 tokens。模糊测试阶段，每个应用设置了 **4小时** 的时间预算。

### 5. 实验数量与充分性

论文设计了相当全面和多维度的实验来验证其方法。

-   **实验分组**：至少包含以下5组关键实验来回答5个研究问题（RQs）。
    1.  **主实验 (RQ1)**：在全部589个应用上运行 Proton 以发现零日漏洞。
    2.  **准确性评估 (RQ2)**：分析检测流水线两个阶段的误报率与漏报率。
    3.  **性能评估 (RQ3)**：报告静态分析和分段模糊测试的时间、Token消耗及暴露时间（TTE）。
    4.  **LLM组件消融 (RQ4)**：驱动（LLM vs 模板）和种子（LLM vs 随机）的交叉对比实验。
    5.  **核心方法消融 (RQ5)**：Proton（分段测试） vs. 基线（端到端测试）在漏洞发现和代码覆盖率上的对比。
-   **充分性与公平性**：
    -   **充分性**：实验规模大（589个应用），覆盖了从发现到性能剖析的完整链路，并通过消融实验明确了各组件的贡献。
    -   **客观公平性**：在对比中，与基线公平地使用相同的预言机和初始种子，时间预算也均为4小时。对误报和漏报的分析也采用人工专家研判和抽样验证相结合的方式，较为严谨。

### 6. 论文的主要结论与发现

-   **有效性与影响力**：Proton 成功在 589 个应用中发现了 **23 个零日 MPV**，其中 **22 个可导致远程代码执行 (RCE)**，影响包括 GitHub 星标超过 5 万的热门项目，并获得了 11 个 CVE 编号和一笔漏洞赏金。
-   **高检出率与低误报**：工具报告的确认漏洞误报率仅为 **11.5%**（3/26）。漏报评估显示其具有较低的漏报率。
-   **性能优越**：
    -   静态分析中位数时间约 2.4 分钟。
    -   分段模糊测试能在相对短的时间内（中位数 28 分钟，82% 在 30 分钟内）发现漏洞。
    -   与端到端模糊测试相比，Proton 多发现了 **17 个** 零日漏洞，并在代码覆盖率上实现了显著提升（中位数提高 87%）。
-   **LLM 的关键作用**：LLM 生成的驱动和种子对于原型检测至关重要。移除 LLM 生成组件会导致检测率大幅下降（从23/23降至6/23）。

### 7. 优点

-   **方法创新性强**：首次形式化定义了 MPV，并提出了“分段定向模糊测试”这一新颖且高效的解决思路，将代数级的复杂性从**乘法级**降低为**加法级**。
-   **高度自动化与实用**：构建了从 LLM 静态分析、驱动生成到模糊测试、漏洞验证的全自动流水线，并成功产出大量高危零日漏洞和 CVE，证明了极高的实用价值。
-   **实验全面扎实**：实验设计环环相扣，规模大，不仅有最终效果验证，还通过详细的消融实验和对比实验，清晰论证了每个核心模块（分段策略、LLM驱动/种子）的必要性和贡献。
-   **开源与可复现性**：承诺开源框架、数据集、LLM交互流程等完整制品，极大地促进了后续研究。

### 8. 不足与局限

-   **静态分析依赖LLM**：方法的第一阶段严重依赖 LLM 的代码理解能力，对于包含非标准库、模板引擎或复杂逻辑的应用，可能存在漏报。论文也承认这是其固有局限。
-   **威胁模型特定性**：漏洞利用的起点假设为系统注册的自定义 URI 处理，这可能限制了其在无此特性的应用上的泛化能力。
-   **操作系统与平台限制**：实验仅在 macOS (Apple Silicon) 上进行，其在其他主流平台（如 Windows, Linux/x86）上的有效性和性能尚未验证。
-   **人工依赖性**：最终的漏洞确认和误报/漏报分析仍需要人类安全专家的介入，尚未实现完全无人的闭环。
-   **性能天花板**：虽然相比于端到端测试有巨大优势，但对于代码量极大、路径极复杂的应用，其静态分析和模糊测试时间（最长约3小时）仍有优化空间。

（完）
