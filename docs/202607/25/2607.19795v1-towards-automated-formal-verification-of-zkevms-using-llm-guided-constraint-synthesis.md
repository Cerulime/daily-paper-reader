---
title: Towards Automated Formal Verification of zkEVMs Using LLM-Guided Constraint Synthesis
title_zh: 面向使用LLM引导约束合成的zkEVM自动形式验证
authors: "Shichen Huang, Zhenghe Jiang, Yi Jiang, Ling-I Wu, Jingyang Li, Guoqiang Li"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.19795v1"
tags: ["query:sys-security"]
score: 8.0
evidence: LLM引导的约束合成用于zkEVM形式化验证，防止安全漏洞。
tldr: "零知识以太坊虚拟机（zkEVM）中的实现错误可能生成证明错误语义的有效证明，使密码学保证失效，而形式化验证的规范编写仍依赖人工。本文提出VeriSynth，利用LLM作为形式化前端将Rust代码翻译为符号约束，结合SMT求解器仲裁和验证引导的自动修复，实现从代码到验证模型的自动合成。在首个zkEVM源码级验证基准上，VeriSynth的bug检测率超过90%，大幅超越LLM直接生成和手写测试套件。该工作为zkEVM的自动化形式化验证开辟了新路径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.19795v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1483, \"height\": 716, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19795v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1864, \"height\": 480, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19795v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 912, \"height\": 438, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.19795v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 933, \"height\": 636, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19795v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 890, \"height\": 192, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19795v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 741, \"height\": 193, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19795v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 808, \"height\": 319, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19795v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 947, \"height\": 316, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19795v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 621, \"height\": 318, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19795v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 926, \"height\": 227, \"label\": \"Table\"}]"
motivation: zkEVM实现错误可导致有效证明认证错误状态，而将Rust代码转换为验证模型仍依赖不可持续的人工规范。
method: 提出VeriSynth框架，采用LLM作为形式化前端翻译代码为符号约束，SMT求解器仲裁，并集成语义分解、检索增强提示和自动修复形成闭环。
result: "在首个zkEVM源码级基准上，VeriSynth实现超90%的bug检测率，显著优于LLM基线和生产级手写突变测试套件。"
conclusion: VeriSynth证明LLM引导的约束合成能自动化zkEVM验证，提供形式化保证，且各组件不可或缺。
---

## 摘要
零知识以太坊虚拟机（zkEVM）通过生成零知识证明来保证链下执行的正确性，从而为以太坊汇总提供安全支持。然而，微妙的实现缺陷（例如不正确的Gas计算）可能导致生成有效的证明来认证语义上有错误的状态，从而无声地破坏了密码学保证。通过SMT求解器进行形式验证可以防止这种情况，但受到了规范说明的瓶颈限制：当前的zkEVM开发实践缺乏将Rust操作码处理程序自动转换为验证模型的方法。当前做法依赖于不可持续的人工撰写规范，而基于大语言模型的方法则存在幻觉问题且缺乏形式化保证。为了解决这一问题，我们提出了VeriSynth，一个从Rust编写的zkEVM代码中合成可执行的Python/Z3验证模型的框架。VeriSynth采用了一种混合范式：大语言模型严格作为形式化前端，将代码转换为符号约束，而SMT求解器则充当正确性的仲裁者。为了处理复杂的多组件状态转换，VeriSynth将语义分解、检索增强提示和验证引导的自动修复集成到一个闭环流水线中。我们在首个源代码级别的zkEVM验证基准上评估了VeriSynth，该基准包含了正确与有缺陷的操作码实现。VeriSynth实现了超过90%的缺陷检测率，显著优于直接生成和对话式的大语言模型基线，以及生产级别的手写变异测试套件。消融研究证实，流水线中的每个组件对于框架的整体有效性都至关重要。

## Abstract
Zero-Knowledge Ethereum Virtual Machines (zkEVMs) secure Ethereum rollups by generating zero-knowledge proofs that guarantee off-chain execution correctness. However, subtle implementation bugs (e.g., incorrect gas accounting) can lead to valid proofs certifying semantically faulty states, thereby silently defeating cryptographic guarantees. Formal verification via SMT solvers can prevent this, but is bottlenecked by specification: current zkEVM development practice lacks automated methods to translate Rust opcode handlers into verification models. Current practices rely on unsustainable manual specifications, while LLM-based approaches suffer from hallucination and lack formal guarantees. To address this, we propose VeriSynth, a framework that synthesizes executable Python/Z3 verification models from Rust zkEVM code. VeriSynth enforces a hybrid paradigm: an LLM acts strictly as a formalization frontend to translate code into symbolic constraints, while an SMT solver serves as the correctness arbiter. To handle complex multi-component state transitions, VeriSynth integrates semantic decomposition, retrieval-grounded prompting, and verification-guided auto-repair into a closed-loop pipeline. We evaluate VeriSynth on the first source-level zkEVM verification benchmark, encompassing both correct and faulty opcode implementations. VeriSynth achieves a bug detection rate of over 90%, substantially outperforming direct and conversational LLM baselines, as well as a production-grade handwritten mutation-testing suite. Ablation studies confirm that each pipeline component is critical to the framework's overall effectiveness.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的结构化中文总结。

### 1. 论文的核心问题与整体含义

*   **核心问题**：零知识以太坊虚拟机（zkEVM）的实现中若存在细微缺陷（如错误的Gas计算或状态更新），可能生成看似有效、实则认证了错误语义状态的有效性证明。这会导致加密保证被无声地破坏。现有的形式验证方法虽能发现此类缺陷，但其瓶颈在于**验证模型的构建仍高度依赖不可持续的人工编写**，无法实现自动化。
*   **整体含义**：本研究旨在弥合zkEVM的Rust源码实现与可被SMT求解器验证的符号约束模型之间的自动化鸿沟。论文的核心主张是，将大语言模型（LLM）作为“形式化前端”（仅负责翻译转换），而将SMT求解器作为“正确性仲裁者”（负责最终决断），这种混合范式是实现自动化、可靠的形式验证的关键。

### 2. 论文提出的方法论

*   **核心思想**：提出VeriSynth框架，遵循“大模型提议，求解器裁决”的分离式设计。
    *   **LLM角色**：严格作为形式化前端，将Rust源码翻译为用Python/Z3语法编写的、可执行的符号约束模型。LLM本身不被信任为正确性判官。
    *   **SMT求解器角色**：作为最终的正确性仲裁者，对LLM生成的模型进行语法检查、类型检查、可执行性验证和语义一致性检查（通过公式 `C_impl(u) ∧ ¬ Φ_u` 的可满足性来寻找反例）。
*   **关键技术细节与流程**：
    *   **语义分解**：将复杂的Rust代码（操作码处理器）分解为模块化的语义单元（如SSTORE操作），便于LLM处理，并为每个单元定义明确的接口。
    *   **检索增强提示**：不把LLM当作模板复制器，而是从已验证的模型数据库中检索语义最相似的参考模型（`M_r`），作为“语义锚点”来稳定符号变量词汇、状态模式和转换关系的生成。
    *   **闭环保修**：生成的模型按顺序经历解析、排序、执行、语义四个阶段的验证。任一阶段失败，则根据结构化的错误类别（`E_parse, E_sort, E_exec, E_sem`）将针对性诊断信息反馈给LLM，进行最多3轮的自动修复，直至模型通过所有检查。
*   **公式/算法流程**：
    *   **验证目标**：对于语义单元u，验证查询为 `C_impl(u)(σ, σ') ∧ ¬ Φ_u(σ, σ')`。其中 `C_impl(u)` 是从代码提取的实现约束，`Φ_u` 是根据标准语义构建的一致性义务。若公式可满足，则SMT求解器产生的模型即为一个反例，揭示了一个语义缺陷。
    *   **工作流程**：`解析程序P -> 分解为语义单元{u} -> 对于每个u：检索参考模型 -> LLM合成候选模型 -> 顺序验证 -> 失败则自动修复 -> 重复直至通过或预算耗尽`。

### 3. 实验设计

*   **数据集与场景**：
    *   **基准**：自行构建的**首个zkEVM Rust源码级验证基准**，包含95个缺陷样本。
    *   **样本构造**：在正确的操作码逻辑单元中注入局部性缺陷（如错误算术、缺失状态更新、Gas逻辑错误）。
    *   **场景覆盖**：缺陷覆盖五大操作码语义族：算术/位运算、内存/存储、Gas/退款、调用/创建路径、环境/上下文。
*   **对比方法**：
    *   **LLM-only**：直接让LLM识别缺陷，不做任何架构支持。
    *   **对话式LLM**：允许LLM根据编译器/求解器的原始反馈进行修改，但无检索和结构化修复。
    *   **手写Rust测试**：Scroll项目使用的生产级人工变异测试套件。
    *   **自有方法的消融变体**：分别移除语义分解、检索增强、操作码感知提示、自动修复和Z3验证。

### 4. 资源与算力

*   **论文中未明确提及**使用了哪些具体的GPU型号、数量或硬件训练时长。
*   **成本分析**集中在API调用成本（Token消耗）上。
    *   **后端模型**：GPT-4o。
    *   **平均Token消耗**：每个样本约100K Tokens。
    *   **总消耗**：在95个样本的基准上，总消耗约为950万Tokens。作者认为这属于离线、安全攸关场景下的可接受成本。

### 5. 实验数量与充分性

*   **实验组数**：至少进行了**5组**核心实验来回答5个研究问题（RQ1-RQ5）。
    1.  **整体有效性**：在95个缺陷样本上对比3种方法。
    2.  **跨语义族泛化性**：将95个样本按5大家族细分，分别报告检测率。
    3.  **消融实验**：与**5个**变体进行对比，量化各组件的贡献度。
    4.  **Token成本分析**：统计平均和总Token消耗。
    5.  **实践价值对比**：与工业界的7个手写测试套件进行对比。
*   **充分性与客观性评价**：
    *   **较为充分**：实验设计全面，包含了整体性能、泛化性、组件贡献度（消融）、成本和实践价值等多个维度。基准和对比方法的选择也清晰且具有直接针对性（如对比工业界手写测试）。
    *   **客观公平**：通过隔离同源文件以避免检索泄露，涵盖了从易到难的多种语义族，对比基线从简单LLM到工业实践，较为公平。

### 6. 论文的主要结论与发现

*   **VeriSynth极其有效**：在95个缺陷样本中检测率达**91.6%**，远超LLM-only（46.3%）和对话式LLM（61.1%）。
*   **泛化能力强**：在五大语义族上均保持高性能，即使在最复杂的调用/创建路径上，检测率也达到**85.7%**。
*   **自动修复是最大贡献者**：移除后，检测率从91.6%暴跌至66.3%，表明许多初始模型失败于本地语法或类型错误而非深层语义误解。
*   **检索增强至关重要**：作为“语义锚点”，对稳定模型生成有显著贡献，移除后检测率降至71.6%。
*   **实践价值显著**：相比Scroll的手写测试套件（57.9%检测率），VeriSynth多检测出**32个**缺陷。

### 7. 优点

*   **设计范式创新**：明确分离LLM（前端翻译）和SMT求解器（后端验证）的职责，巧妙利用各自优势，避免了LLM幻觉的风险。
*   **技术集成巧妙**：语义分解、检索增强、结构化闭环保修的组合有效解决了将非规范化的源码转换为严格形式化模型的多重挑战。
*   **基准贡献**：填补了zkEVM源码级验证基准的空白，为未来的研究提供了衡量标准。
*   **实验全面扎实**：通过多维度的对比和消融实验，有力地证明了框架的有效性和各组件的价值。

### 8. 不足与局限

*   **正确性保证的局限性**：VeriSynth的正确性保证是**相对于其生成的验证模型和内置的一致性义务（Φu）的**。它不能证明整个zkEVM实现的绝对正确，也无法保证义务本身没有被遗漏。
*   **只关注缺陷检测**：论文聚焦于负样本（有缺陷代码）的检测，对正样本（无缺陷代码）的**误报率未做系统评估**，这是衡量其可用性的关键指标。
*   **数据集偏差风险**：自行构建的基准可能存在偏差，缺陷类型和注入方式可能不完全代表真实世界的缺陷分布。
*   **单一项目验证**：仅在Scroll一个zkEVM实现上进行了评估，其结论的泛化性有待在更多开源zkEVM项目中进行交叉验证。
*   **成本与效率**：依赖GPT-4o导致Token消耗不菲，限制了其在大规模或频繁变更场景下的应用。未来工作也提到了降低成本的需求。

（完）
