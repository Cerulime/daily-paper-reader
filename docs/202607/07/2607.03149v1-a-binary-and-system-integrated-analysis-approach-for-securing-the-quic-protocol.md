---
title: A Binary and System Integrated Analysis Approach for Securing the QUIC Protocol
title_zh: 面向QUIC协议安全的二进制与系统集成分析方法
authors: "Maitha Alshaali, Wanqing Tu, Gaofei Huang, Mthandazo Ndhlovu"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.03149v1"
tags: ["query:sys-security"]
score: 9.0
evidence: 对已编译QUIC服务器进行二进制级别安全分析
tldr: "QUIC协议在IoT中广泛用于安全传输，但现有安全分析仅限网络流量，无法确认二进制实现中防御是否生效。本文提出BSISA方法，结合二进制与系统级分析，在四种实现上测试六种攻击。集成方法总体准确率45.8%，优于单独分析，并能发现声明但未执行的防御。picoquic在部分攻击下故障率高，其他实现在0.5%以下。该工作为QUIC安全评估提供新视角。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03149v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 822, \"height\": 621, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03149v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 827, \"height\": 748, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03149v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 909, \"height\": 965, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03149v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 921, \"height\": 944, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.03149v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1231, \"height\": 230, \"label\": \"Table\"}]"
motivation: 现有QUIC安全分析仅关注网络流量，忽略编译二进制层面的验证，难以确保防御机制的实际激活状态。
method: 本文提出BSISA方法，对四个生产级QUIC服务器进行二进制与系统级联合分析，覆盖六种攻击场景。
result: "集成分析准确率45.8%，优于单独二进制(37.5%)和系统级(25.0%)，并能定位编译存在但未执行的防御。"
conclusion: 证明BSISA能有效评估QUIC安全实现，为IoT固件部署提供重要参考。
---

## 摘要
快速UDP互联网连接（QUIC）协议越来越多地用于为物联网（IoT）固件和应用程序提供安全传输。现有的QUIC安全性分析侧重于捕获的网络流量，而QUIC实现的二进制级分析仍未被探索，留下了QUIC标准规定的防御措施是否既出现在编译后的二进制文件中，又在服务器遭受攻击时处于活跃状态的问题。本文在六种攻击场景下，针对四种生产级QUIC服务器实现，评估了二进制与系统集成安全分析（BSISA）方法，该方法将编译后的QUIC服务器的二进制级分析与捕获的网络流量的系统级分析相结合。在24个单元中，组合分类器配置是唯一在每个攻击场景中正确分类至少一个单元的配置，总体准确率达到45.8%，而二进制级配置为37.5%，系统级配置为25.0%。BSISA还识别了编译后二进制文件中吸收每次攻击的具体防御函数，并标记了声明但静默的防御措施，即存在于编译后二进制文件中（四个协议栈中的三个中的重试令牌验证，quiche中的反放大）但在攻击期间从未执行的例程，这是仅通过网络捕获无法产生的一类发现。在效率方面，picoquic在slowloris和连接ID耗尽攻击下丧失合法客户端可用性，失败率分别为72.4%和73.3%，而其他三种实现的失败率保持在0.5%或更低。我们希望这些见解能为物联网固件部署中的QUIC安全性评估提供参考。

## Abstract
The Quick UDP Internet Connections (QUIC) protocol is increasingly used to provide secure transport for Internet of Things (IoT) firmware and applications. Existing security analyses of QUIC focus on the captured network traffic, while binary-level analyses of QUIC implementations remain unexplored, leaving open the question of whether a defence specified by the QUIC standard is both present in the compiled binary and active when the server is under attack. This paper evaluates the Binary and System Integrated Security Analysis (BSISA) approach, in which a binary-level analysis of the compiled QUIC server is combined with a system-level analysis of the captured network traffic, on four production QUIC server implementations under six attack scenarios. Across 24 cells, the combined classifier configuration is the only configuration that correctly classifies at least one cell on every attack scenario, achieving 45.8% overall accuracy compared with 37.5% for the binary-level configuration and 25.0% for the system-level configuration. BSISA also identifies the specific defence function in the compiled binary that absorbed each attack, and flags declared-but-silent defences, routines that are present in the compiled binary (Retry-token validation in three of four stacks, anti-amplification in quiche) but never execute during attack, a class of finding that network capture alone cannot produce. In terms of efficiency, picoquic loses legitimate-client availability under slowloris and connection- ID exhaustion with failure rates of 72.4% and 73.3% respectively, while the other three implementations hold the failure rate at or below 0.5%. We hope these insights will be informative for QUIC security evaluations in IoT firmware deployments.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究背景**：QUIC 协议正在被广泛用于物联网（IoT）固件和应用的安全传输，尤其在智能楼宇、车联遥测、工业监控等场景。QUIC 服务器常以预编译二进制形式部署于网关、边缘节点等资源受限设备中。
- **核心问题**：现有 QUIC 安全分析仅基于网络流量（系统级），缺少对编译后二进制（binary）的防御实现与运行时激活状态的验证。这导致无法回答：标准规定的防御是否真实存在于二进制中，且在攻击发生时是否实际执行？
- **研究意义**：该工作填补了 QUIC 安全研究中二进制与系统集成分析的空白，尤其适用于仅有预编译二进制可审查的 IoT 固件部署场景。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **整体思路**：提出二进制与系统集成安全分析（BSISA）方法，将编译后 QUIC 服务器的静态二进制分析与运行时网络流量、qlog 日志和系统调用追踪相结合，对攻击进行联合分类。
- **二进制分析（ORCA）**：设计多 Agent 系统对二进制进行反汇编、符号解析、字符串扫描、API 交叉引用和功能聚类，生成结构化的“防御清单”（例如重放令牌验证、反放大、空闲超时等），明确每个防御在二进制中的函数实现及子系统归属。
- **系统级分析**：通过网络抓包和 qlog 日志提取连接速率、握手完成率、防御事件（如 Retry-sent、packet-dropped）等运行时特征，并计算异常分数。
- **集成与分类**：
  - 第一层：为每种攻击生成五字段结构记录（攻击确认、二进制防御实现、运行时防御激活、操作影响、总结）。
  - 第二层：使用相同的大语言模型（Claude Sonnet 4）分别基于系统级证据、二进制证据、以及二者结合的证据进行攻击分类，输出标签和理由。
- **检测准确率公式**：`A(p, X) = (1/|X|) * Σ 1[ŷ(p)_i = G_i]`，其中 ŷ(p)_i 为配置 p 在单元 i 上的预测标签，G_i 为真实标签。
- **攻击覆盖度公式**：`C(p) = (1/|A|) * Σ 1[∃ i∈X_a: ŷ(p)_i = a]`，即配置 p 在每种攻击上至少有一个单元分类正确。

### 3. 实验设计：使用了哪些数据集/场景，它的 benchmark 是什么，对比了哪些方法
- **实验平台**：四台物理机（服务器、合法客户端、攻击者、路由器）通过 Wi-Fi 连接，服务器运行不同 QUIC 实现。
- **测试对象**：四种生产级 QUIC 服务器：picoquic 1.1.48.0、msquic 2.5.5、ngtcp2 16.2.3、quiche 0.28.0。
- **攻击场景**：六种攻击（连接洪水、Slowloris、连接 ID 耗尽、中间人注入、0-RTT 重放、重试令牌滥用），外加一个正常基线。共 28 个（实现，场景）单元。其中重试令牌滥用因未触发 Retry 模式而从准确率计算中排除，最终使用了 24 个单元。
- **对比配置**：
  - 系统级分类器（仅看网络特征）
  - 二进制级分类器（仅看二进制防御清单与 Agent 记录）
  - 组合分类器（结合两者）
- **性能指标**：检测准确率、攻击场景覆盖率、合法客户端平均端到端延迟和失败率。

### 4. 资源与算力
- 论文未明确提及 GPU 型号、数量或训练时长。分析过程使用的是基于大语言模型的推理（通过 API 访问 Claude Sonnet 4），单次分析平均耗时约 7.7 分钟，无需训练阶段。

### 5. 实验数量与充分性
- **实验组数**：24 个有效单元（4 种实现 × 6 种攻击场景，排除重试令牌滥用的 4 个单元）。每种攻击含 11 个攻击强度水平（10%～100%），每个水平 50 次交易。因此总实验次数较大，覆盖了多种实现与攻击的交叉。
- **充分性与公平性**：
  - 对比公平：分类器使用相同的底层大模型和温度参数，仅证据源不同。
  - 攻击方案和测试台驱动均开源，便于复现。
  - 统计检验：报告了 Wilson 置信区间，并指出因样本量限制，二进制与组合方法在单元级精度上的差异在统计上未分离，但组合方法在攻击场景覆盖率上是唯一实现全覆盖的配置。
  - 整体实验设计比较系统，但样本规模（24 个单元）可能限制统计效力，作者也明确说明了这一点。

### 6. 论文的主要结论与发现
- **检测能力**：组合分类器总准确率 45.8%，高于二进制级（37.5%）和系统级（25.0%），且是唯一在六种攻击场景下都有正确识别的配置。
- **二进制独有发现**：定位了实际吸收攻击的防御函数，发现了“声明但静默”的防御（例如三个栈的重试令牌验证和 quiche 的反放大例程），这些防御在二进制中存在但从未执行，网络流量分析无法揭示。
- **通信效率影响**：尽管延迟变化不显著，但 picoquic 在 Slowloris 和连接 ID 耗尽下合法客户端失败率高达 72% 以上，而其他三款实现失败率维持在 0.5% 以下。

### 7. 优点：方法或实验设计上有哪些亮点
- **创新性**：首次将静态二进制分析与动态系统观测结合用于 QUIC 安全评估，填补了仅依靠网络流量分析的盲区。
- **系统性**：ORCA 多 Agent 设计对二进制进行细粒度解析，形成结构化的防御清单，可与运行时证据直接对照。
- **实用性**：方法可直接用于无源码的 IoT 固件场景，提供防御真实执行情况的证据。
- **开源与可复现**：代码、数据集、攻击实现等均已公开。
- **发现独特**：揭示了“存在但未激活”的防御，对实际安全审计极具价值。

### 8. 不足与局限
- **样本规模有限**：仅分析 4 种实现、6 种攻击、24 个有效单元，统计置信区间较宽，部分性能提升非统计显著。
- **攻击范围局限**：未覆盖版本协商、连接迁移等高级攻击；重试令牌滥用攻击未能有效触发，导致部分数据被排除。
- **推理成本**：单个单元分析需约 7.7 分钟，虽可分摊，但对实时或大规模部署仍有开销。
- **大模型依赖**：分类器效果受底层 LLM 能力影响，可能存在偏差或幻觉。
- **硬件通用性**：实验仅在 x86 服务器上完成，未在真实 IoT 硬件（如 ESP32）上验证。

（完）
