---
title: A Modular Framework for Stack-Heap and Value Abstractions (Extended Version)
title_zh: 一种用于栈-堆与值抽象的模块化框架（扩展版）
authors: "Giacomo Boldini, Luca Negrini, Luca Olivieri, Pietro Ferrara"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15932v1"
tags: ["query:sys-security"]
score: 9.0
evidence: 面向内存错误检测的静态分析框架，支持执行前发现缓冲区溢出等漏洞
tldr: 高级静态分析需应对指针与复杂数据结构，不同编程语言的栈堆分配行为各异，准确内存模型对在运行前检测缓冲区溢出、释放后使用等关键错误不可或缺。本文提出一个基于抽象解释的参数化通用内存框架，采用分离状态抽象，将值分析与内存分析分为两个模块化抽象域，借助内存标识符及域操作交互。该框架支持C、C++、Java、Python等多种语言的内存行为，允许独立选择和组合不同分析，能够捕获值信息与结构内存关系，实现对程序行为的可靠近似。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15932v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1242, \"height\": 690, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.15932v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 788, \"height\": 646, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15932v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 835, \"height\": 236, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15932v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 810, \"height\": 341, \"label\": \"Table\"}]"
motivation: 不同编程语言的栈堆内存管理差异显著，现有静态分析缺乏通用、可组合的内存模型，难以准确检测跨语言的内存错误。
method: 基于抽象解释，提出分离状态抽象，将值域和内存域模块化，通过内存标识符与域操作实现交互。
result: 该框架可实例化支持多种编程语言，允许独立组合内存与值分析，有效建模不同内存模型，捕获值信息与结构内存关系。
conclusion: 提出的模块化内存框架基于抽象解释，实现了可靠且可定制的内存分析，有助于在执行前检测内存错误。
---

## 摘要
高级静态程序分析需要对非平凡程序行为（例如指针和复杂数据结构如列表、集合、函数及对象）的语义及其对内存的影响进行推理。在大多数编程语言中，静态和动态分配通常分别由栈和堆管理。然而，根据所分析的编程语言不同，分配的行为以及内存在运行时的管理方式可能会有显著差异。正确恰当地处理这些方面至关重要，因为准确的内存模型能够在执行之前（即在错误行为发生之前）检测出关键问题，如缓冲区溢出和欠冲、释放后使用错误以及空指针异常。在本文中，我们提出并形式化了一个通用的内存框架，用于在分析过程中处理栈和堆内存，该框架能够支持不同编程语言（例如 C、C++、Java 和 Python）的各种行为，同时保持参数化，允许独立选择和组合不同的内存分析与值分析。它基于抽象解释理论，能够对不同内存模型和程序行为进行可靠的近似。我们引入了一种分离状态抽象，将值分析和内存分析分离为两个模块化的抽象域。这些域通过一组内存标识符以及域定义的一组操作进行交互，从而操纵它们，使框架能够同时捕获值信息和结构化的内存关系。

## Abstract
Advanced static program analysis requires reasoning on the semantics of non-trivial program behaviors (e.g., pointers and complex data structures such as lists and sets, functions, and objects) and how they affect the memory. In most programming languages, static and dynamic allocations are typically managed by the stack and the heap, respectively. However, how allocations behave and how the memory is managed at runtime can vary significantly depending on the programming language being analyzed. Proper handling of these aspects is essential, as an accurate memory model enables the detection of critical issues such as buffer overflows and underflows, use-after-free errors, and null pointer exceptions prior to execution, that is, before such erroneous behaviors occur. In this paper, we propose and formalize a generic memory framework to handle stack and heap memory during the analysis, that is able to support various behaviors from different programming languages (e.g., C, C++, Java, and Python), while remaining parametric, allowing different memory and value analyses to be independently chosen and combined. It relies on the Abstract Interpretation theory and enables sound approximation of different memory models and program behaviors. We introduce a split state abstraction that separates value and memory analyses into two modular abstract domains. These domains interact through a set of memory identifiers, along with a set of operations defined by the domains to manipulate them, allowing the framework to capture both value information and structural memory relationships.

---

## 论文详细总结（自动生成）

好的，这是一份基于给定论文的详细中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现代程序的静态分析需要同时精确处理值（如整数、布尔值）和内存结构（如指针、对象、堆分配）。然而，值分析和内存分析（特别是堆分析）通常是独立发展的，缺乏一个统一、通用且模块化的框架将二者有机结合。处理C/C++这类需要手动管理内存的语言和Java、Python这类具有自动垃圾回收的语言时，对内存模型的要求差异巨大。

- **整体含义**：论文提出了一个参数化、模块化的框架，用于在静态分析中结合栈和堆的内存分析与值分析。其核心目标是提供一个“即插即用”的平台，允许分析工具开发者根据目标程序语言的特性，独立选择和组合特定的值抽象域和内存抽象域，而无需为每种组合重新设计整个分析，从而更灵活、更精确地检测如缓冲区溢出、释放后使用、空指针解引用等内存错误。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：基于抽象解释理论，将程序状态（内存与值）进行**分离状态抽象**。这种设计将传统的混合状态分离为两个独立的、模块化的抽象域：**值域** 和**内存域**，二者通过一套标准化的接口（一组内存标识符和操作）进行通信和协同。

- **关键技术细节与流程**：
    - **语言与具体语义**：定义了一个名为 **μLL** 的小型命令式语言，它扩展了μJS，显式地支持指针、取地址、解引用、字段访问和堆分配。并给出了其**具体操作语义**，为形式化证明奠定基础。
    - **分离状态**：引入一个与传统具体状态同构的**分离状态**。该状态将原始内存映射切分为四个子映射：`ν_ih`（基值）、`ν_f`（字段值）、`μ_ih`（基地址）、`μ_f`（字段地址）。这种分离确保了值信息和指针信息在逻辑上完全分开，且通过证明与具体状态构成**伽罗瓦同构**，保证了这种分离无精度损失。
    - **抽象状态**：最终的抽象状态是两个独立抽象域（`V♯`和`M♯`）的 smash 积。它们的交互依托于一个核心概念——**内存标识符**。内存域负责内存结构（如指向关系），并为每个抽象内存位置创建一个唯一的`MemID`。值域则使用这些`MemID`作为变量，来跟踪存储在这些位置上的抽象值。
    - **域间接口与语义**：框架为一对抽象域定义了严格的交互协议。内存域需提供如 `pointsTo_M♯`（查询指向信息）、`access_M♯`（字段访问）、`assign_M♯`（更新内存结构）等操作，并可能返回**替换**来重整标识符。值域则需提供`assign_V♯`（更新值）等标准操作。整个语句的抽象语义通过像 `RM♯` （重写表达式，将内存操作替换为值域可理解的`MemID`操作）这样的辅助函数，协调两个域的顺序更新，并在框架层面证明了这种组合性语义的可靠性（Soundness）。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- **论文性质**：本文是一篇**理论框架论文**，重点在于形式化定义、证明和通过实例展示框架的能力，**没有进行传统的、在大规模Benchmark上的量化实验评估**。
- **实例展示与分析**：
    - **用例场景**：使用了一个来自SV-COMP基准测试的C代码片段（涉及双向链表的复杂指针操作）作为持续性示例，展示框架如何处理别名和堆操作。
    - **模块化演示**：通过一个简单的堆分配和非确定性赋值程序，展示了在**固定内存域**（Andersen风格的指向分析）的情况下，仅切换**值域**（常量传播、区间域、常量集）对分析精度产生的直接影响，证明了框架的模块化优势。
    - **组合性演示**：在持续性示例的扩展版本中，演示了指向分析（内存域）与非关系型数值域（符号域、区间域）之间如何通过框架接口，在处理堆对象整数字段时协同工作，包括强/弱更新的区分和不动点计算。

### 4. 资源与算力

- 本文为理论性论文，主要贡献在于形式化框架和证明，**未涉及任何具体实现、实验运行或GPU/CPU等算力资源的描述**。

### 5. 实验数量与充分性

- **实验数量**：作为理论文章，没有进行多组对照实验。其“实验”部分由**3个逻辑严密的实例分析**构成。
- **实验充分性与客观性**：
    - **充分性**：对于一篇框架论文，实例的选择具有代表性，覆盖了其声称的核心能力，包括：处理复杂的C风格内存操作、展示框架的模块化（替换不同值域）、以及展示域间协同处理混合（值-内存）属性的能力。
    - **客观性与公平性**：由于不涉及与现有工具的性能或精度对比，不存在传统意义上的公平性问题。实例分析客观地展示了框架的运作机制。主要局限性在于缺乏对大规模、真实世界程序的可扩展性或性能评估。

### 6. 论文的主要结论与发现

- 论文成功提出并形式化了一个**通用的、参数化的内存和值分析组合框架**。
- 该框架基于严格的抽象解释理论，通过**分离状态抽象**，实现了值域和内存域的清晰解耦。
- 通过定义一套标准的操作接口，该框架**支持独立选择和组合不同的值分析域（如间隔、常量传播）和内存分析域（如Andersen指向分析）**，具有高度的模块化和可重用性。
- 通过理论证明和实例分析，论证了该框架能够**可靠地**对多种内存模型和复杂程序行为进行近似，为构建面向不同语言的精确静态分析工具提供了灵活的基础。

### 7. 优点：方法或实验设计上有哪些亮点

- **真正的模块化**：框架设计将值和内存的关注点彻底分离，这是相比于以往将两者紧密耦合或需要手动扩展进行分析的方法的重大进步。开发者可以复用现有域和接口，像“搭积木”一样构建分析器。
- **泛用性强**：不局限于特定的目标语言（如Java或C）或特定的抽象域，从理论上证明了支持从底层到高层语言以及不同精度和效率域的潜力。
- **坚实的理论基础**：整个框架建立在严格的数学基础之上，提供了从具体语义、分离状态、抽象状态到组合语义的完整形式化链条和完备性证明，保证了所提出方法的可靠性。
- **示例驱动且有效**：通过精心设计的、由简到繁的代码示例，直观地展示了框架在处理复杂指针操作和模块化能力上的优越性，使得抽象的理论概念易于理解。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **缺乏实证评估**：作为一篇理论论文，最大的局限性在于缺乏对框架实现的性能、精度和可扩展性的大规模量化评估。没有在诸如SV-COMP这类标准测试集上与现有工具进行对比，其实际应用价值有待验证。
- **示例相对简单**：尽管动机示例来自基准测试，但用于演示框架机理的实例程序体量较小。可能无法完全暴露在大规模、百万行代码项目中应用该框架会遇到的工程挑战。
- **未讨论性能和效率**：文中未讨论模块化设计引入的抽象层和域间通信可能带来的性能开销，也未与其他分析技术（如TVLA或MOPSA）在分析时间上进行理论或实证的复杂度比较。
- **缺乏具体实现和工具支持**：论文停留在理论层面，未提供一个开源的、可运行的实现，这可能会限制其在社区中的快速传播和实际应用。虽提及作为LiSA框架未来工作的基础，但尚未实现。

（完）
