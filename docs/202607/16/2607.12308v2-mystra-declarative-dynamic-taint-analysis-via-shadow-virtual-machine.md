---
title: "Mystra: Declarative Dynamic Taint Analysis via Shadow Virtual Machine"
title_zh: Mystra：通过影子虚拟机的声明式动态污点分析
authors: "Zhuohao Zhang, Junkun Liu, Rui Yang, Yinzhi Cao, Ziyang Li"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12308v2"
tags: ["query:sys-security"]
score: 9.0
evidence: 提出影子虚拟机用于运行时动态污点分析
tldr: "针对解释型语言的动态污点分析缺乏运行时无关抽象，Mystra提出影子虚拟机与声明式语言分离污点语义。工具Shar在三大运行时上实现，在JavaScript漏洞检测中召回率达95.5%且零误报，开销仅为原生1.85倍，比现有方法降低22.7倍并提升召回33.2%。首次支持声明式高阶函数污点传递，为自动化规则合成提供基础。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12308v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1884, \"height\": 762, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12308v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1751, \"height\": 519, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12308v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1873, \"height\": 1399, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12308v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 910, \"height\": 332, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12308v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1798, \"height\": 703, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12308v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 850, \"height\": 291, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12308v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 851, \"height\": 454, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12308v2/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 709, \"height\": 161, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12308v2/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 854, \"height\": 311, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12308v2/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 799, \"height\": 605, \"label\": \"Table\"}]"
motivation: 现有DTA紧耦合于插桩机制，导致高开销或引擎绑定，缺少运行时无关抽象。
method: 设计影子虚拟机跟踪污点元数据，结合Mystra声明式语言编译为高效规则，在V8、SpiderMonkey、CPython上实现Shar。
result: "Shar在SecBench.js上获95.5%召回率零误报，性能开销1.85×原生，比NodeMedic-FINE低22.7倍且召回高33.2%。"
conclusion: 提出可扩展、准确、高性能的DTA范式，分离语义与运行时，支持自动化生成，为多语言动态分析奠定基础。
---

## 摘要
对于JavaScript和Python等解释型语言，动态污点分析（DTA）需要三种能力：观察宿主运行时操作、维护并行污点状态，以及定义污点如何传播。现有系统将这些能力耦合在插桩机制中——源码重写或引擎原生——要么导致高运行时开销，要么要求针对特定引擎的嵌入。目前尚缺乏一种与运行时无关的通用DTA抽象，能将污点语义和状态转换与宿主运行时的执行方式分离。我们着手开发一个可扩展、高性能且准确的DTA引擎。为此，我们引入了一个与宿主运行时并行执行的影子虚拟机，用于跟踪多级污点、来源和跨调用上下文。我们设计了Mystra，一种具有形式化操作语义的声明式污点规范语言。Mystra的设计对语言模型友好，并配备了验证器，支持可信的规则自动合成。Mystra也是首个以声明方式表达高阶函数污点传递的语言。此外，Mystra规则被提前编译为二进制表示，并在常数时间内分派。我们将这一设想实现为名为Shar的工具，它包含一个共享核心引擎，并在三个运行时上实例化：Node.js和Chromium（嵌入）中的V8、SpiderMonkey（引擎）以及CPython（语言）。在准确性方面，在SecBench.js（四个CWE类别中的493个范围内CVE）上，我们的V8实例化在修补版本测试中实现了95.5%的召回率，且零误报。在性能方面，在NodeMedic基准测试中，Shar的运行时开销较原始Node.js为1.85倍，在相同工作负载下比NodeMedic-FINE低22.7倍，同时在所支持类别中的召回率高出33.2%。

## Abstract
Dynamic taint analysis (DTA) for interpreted languages like JavaScript and Python requires three capabilities: observing host-runtime operations, maintaining parallel taint states, and defining how taint propagates. Existing systems couple these capabilities within an instrumentation mechanism -- source-rewriting or engine-native -- either incurring high runtime overhead or demanding engine-specific embeddings. There is yet to be a runtime-independent abstraction of a general DTA that separates taint semantics and state transitions from how a host runtime executes them.   We set out to develop a DTA engine that is extensible, performant, and accurate. To achieve this, we introduce a Shadow Virtual Machine executing alongside host runtimes that tracks multi-level taint, provenance, and cross-invocation context. We design Mystra, a declarative taint specification language with formal operational semantics. Mystra is designed to be language model friendly, and is equipped with validators enabling trustworthy automated synthesis of rules. Mystra is also the first to express higher-order function taint transfer declaratively. Further, Mystra rules are compiled ahead of time to a binary representation and dispatch in constant runtime.   We implement our vision into a tool named Shar, which contains a shared core engine and instantiations on three runtimes: V8 in both Node$.$js and Chromium (embedding), SpiderMonkey (engine), and CPython (language). Accuracy wise, on SecBench$.$js (493 in-scope CVEs across four CWE categories), our V8 instantiation achieves 95.5% recall with zero false positives on patched-version testing. Regarding performance, the runtime overhead of Shar is 1.85$\times$ over vanilla Node$.$js on NodeMedic's benchmarks, and is 22.7$\times$ lower than NodeMedic-FINE on identical workloads, all the while producing 33.2% higher recall in its supported categories.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：现有的动态污点分析系统在解释型语言中，将污点语义、状态管理与宿主运行时的插桩机制紧密耦合（源码重写或引擎内嵌），导致运行时开销巨大或缺乏跨运行时的可移植性。缺乏一个与运行时无关的、能将污点逻辑与执行观察分离的统一抽象。
- **研究动机**：动态污点分析是现代软件安全检测的关键技术，广泛应用于注入攻击检测、漏洞挖掘、大模型引导的安全分析等领域。然而，现有工具在性能和架构上存在碎片化问题，阻碍了高效、可复用的污点分析。
- **整体含义**：论文提出了一种新型的动态污点分析范式，通过设计一个与运行时无关的影子虚拟机和一种声明式污点规范语言，实现了分析逻辑与执行机制的解耦，从而达成高性能、高准确率与跨语言/引擎的可移植性。

### 2. 论文提出的方法论：核心思想、关键技术细节、算法流程
- **核心思想：影子虚拟机抽象**
  - 提出一个抽象的**影子虚拟机**，与宿主运行时并行执行，专门负责维护分析状态：影子栈、影子堆、来源图和规范上下文 。这种设计分离了“在哪里观察操作”与“如何更新污点状态”。
  - 设计了一个通用的**宿主接口**，涵盖操作事件、操作数寻址、操作身份、对象生命周期、运行时查询和异步连续性同步六大能力。不同的运行时通过实现此接口适配器来接入影子虚拟机核心。

- **关键技术：声明式语言Mystra**
  - 设计了**Mystra**，一种具有形式化操作语义的声明式污点规范语言。分析人员通过编写规则来定义污点的源头、传播、净化以及安全检查点。
  - **核心动作（Actions）**：
    - `source`/`sink`：定义污点引入源与危险汇聚点。
    - `propagate`：描述数据流经操作时，污点如何从输入传播到输出。
    - `clear`：描述净化操作，移除污点。
    - `inject`/`extract`：**首次以声明方式**处理高阶函数（如 Array.map）的污点传递。`inject`将容器元素的污点注入到回调函数的参数，`extract`将回调函数的返回值污点收集回容器结果。
    - `set`：利用规范上下文在跨边界操作（如文件写入再读取）中持久化污点。
  - **编译与执行**：Mystra规则在启动前被提前编译为二进制表示`.tbin`，在运行时以恒定时间复杂度进行分派，避免了运行时解析的开销。

- **系统实现：工具Shar**
  - 将上述设计实现为工具 **Shar**，包含一个共享的污点引擎核心（污点引擎、Mystra解释器、来源图、规范上下文），并在三个运行时上实例化：**V8 (Node.js & Chromium)、SpiderMonkey (引擎)、CPython (语言)**。
  - 在V8实现中，不仅覆盖了解释器，还通过11个节点将影子虚拟机操作下沉到Maglev JIT编译器，确保了JIT优化代码中的污点跟踪性能。

### 3. 实验设计：使用了哪些数据集/场景，它的benchmark是什么，对比了哪些方法
- **检测准确性实验**：
  - **数据集**：
    - **SecBench.js**：包含493个经过验证的CVE，覆盖代码注入、命令注入、路径遍历和原型污染四类。
    - **NodeMedic基准数据集**：包含20个真实Node.js包（17个漏洞，3个无害样本）。
    - **19个近期真实漏洞** (2024-2026)：覆盖Node.js、浏览器 (Chromium/SpiderMonkey) 和CPython (Python) 多平台场景。
  - **对比方法**：NodeMedic-FINE（仅在其支持的命令注入和代码注入类别进行召回率对比）。
- **性能开销实验**：
  - **基准工具**：NodeMedic的`node-medic-bench`性能测试套件。
  - **对比方法**：基准Node.js，以及源码重写工具Jalangi、NodeMedic、NodeMedic-FINE。
- **规则表达能力实验**：
  - 评估了Mystra规则对11个API类别的覆盖度。
  - 将LLM合成的Mystra规则与CodeQL的Node.js污点规则进行API覆盖度和代码行数(LoC)的对比。
  - 探索通过LLM自动合成规则，并利用内置验证器进行一次反馈修正的有效性。
- **可移植性实验**：
  - 从V8移植到同引擎不同宿主（Chromium）、同语言不同引擎（SpiderMonkey）、不同语言（CPython）三个维度进行验证，并报告了各适配层的代码行数。

### 4. 资源与算力
- **计算环境**：所有实验在一台配备**16核 x86_64 vCPU 和 64GB RAM** 的虚拟机上运行，操作系统为 Ubuntu 24.04。
- **额外算力**：论文中未提及使用GPU或大规模分布式计算的训练配置。所涉及的大语言模型辅助实验仅用于规则生成，未披露其具体算力需求。

### 5. 实验数量与充分性：大概做了多少组实验，这些实验是否充分、是否客观、公平
- **实验数量**：实验设计包含**六大类评估**，覆盖了召回率、误报率、性能开销、规则表达力、LLM辅助合成和跨平台移植性，较为全面。
- **实验充分性**：
  - **多维度评估**：从准确性、效率、表达能力和可移植性四个核心维度对系统进行了验证，逻辑完整。
  - **大规模与细粒度结合**：既使用了SecBench.js（近500个CVE）这样的大规模基准测试，又对19个近期真实漏洞进行了详细的案例分析。
  - **公平性对比**：
    - 性能对比中，明确指出NodeMedic等工具需在特定Node版本运行，并以归一化开销而非绝对时间进行比较。
    - 召回率对比中，仅在NodeMedic-FINE支持的CWE类别上进行，排除了不支持的类别，确保了对比的公平性。
    - 在141个修补版本上进行了零误报测试，有力地证明了其精确性。
- **客观性**：所有对比均基于公开基准和可复现的步骤，且作者指出了自身工具的漏报模式（如字符串分解、跨持久化存储），体现了客观的分析态度。

### 6. 论文的主要结论与发现
- **高效且准确**：Shar在SecBench.js上实现了 **95.5%的召回率**和在修补版本测试中的**零误报率**。在性能上，较原版Node.js的开销仅为 **1.85倍**。
- **性能显著优于现有工具**：在相同工作负载下，其开销比NodeMedic-FINE **低22.7倍**，同时在所支持的类别中，召回率**高出33.2%**。
- **声明式语言成功解耦**：提出的影子虚拟机抽象和Mystra语言成功地将污点语义与运行时执行机制分离，允许通过对操作进行简单重绑定，将同一套规则集跨V8、SpiderMonkey、CPython复用。
- **具备自动化潜力**：Mystra的声明式设计对LLM友好，结合内置验证器，展现了通过AI自动合成高质量污点规则的可行性。

### 7. 优点：方法或实验设计上有哪些亮点
- **架构创新的解耦设计**：首次为DTA引入影子虚拟机和通用宿主接口，从架构层面解决了污点逻辑与运行时实现耦合的长期问题。
- **Mystra语言的强大表达能力**：**首次以声明方式原生支持高阶函数的污点传递**，并提供跨操作边界的状态持久化机制。形式化的操作语义为其可靠性提供了理论基础，而预编译和常量分派则保障了高性能。
- **跨层级的JIT支持**：在V8实现中将影子虚拟机操作下沉到Maglev JIT节点，确保在JIT优化代码中也能高效追踪污点，这是性能领先的关键。
- **严谨全面的实验评估**：实验设计极为系统，覆盖了大规模基准（SecBench.js）、业界标准（NodeMedic）、近期真实CVE以及多语言环境。通过修补版本零误报测试展示了极高的准确性。
- **多维度可移植性验证**：不仅验证了在不同JS引擎间的移植，更验证了跨语言（从JS到Python）的移植，直接证明了其设计理念的普适性。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **分析范围局限**：
  - **CWE覆盖有限**：当前主要针对6类CWE，对其他类型漏洞的检测需要新增规则。
  - **隐式流缺失**：系统明确不跟踪隐式流，虽然作者认为这对此类漏洞检测收益有限，但仍是分析完备性的一个缺失。
- **精度与状态维护问题**：
  - **字符串分解**：`String.split`等操作会导致污点关联丢失，这是所有显式流DTA的共性问题。
  - **跨持久化边界**：除非编写像文件读写那样的桥接规则，否则污点无法在数据库存取等持久化操作后幸存。
  - **过度污染**：在n8n案例中，由于TypeORM的对象合并，出现了污点“溢出”到非预期的文件系统操作中。
- **性能评估局限性**：性能测试基于短端到端的包分析任务，未表征在CPU密集型长时负载下的表现。对属性访问和函数调用的全量挂钩，可能在高计算量应用中带来更高开销。
- **潜在的偏差**：
  - SecBench.js主要收集2017-2022年的CVE，且多为小型单文件库，可能无法完全代表现代大型应用的复杂度。作者通过增加NodeMedic数据和近期大型应用漏洞对此进行了部分缓解。
- **未实现特性**：选择性插桩（仅在从源头可达的代码上开启挂钩）和完整的Turbofan JIT集成尚待未来工作实现。

（完）
